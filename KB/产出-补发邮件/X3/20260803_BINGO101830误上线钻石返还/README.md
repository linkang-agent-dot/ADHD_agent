# BINGO 101830 误上线钻石返还

## 结论

- 事故活动：`ActvOnline 101830 / ActvPuzzle 1830`
- 异常实例窗：`2026-07-01 08:00:00 ~ 2026-09-29 07:59:59`（TC1831 误配长窗）
- 本次精确查询窗：`2026-07-30 15:23:00 ~ 2026-07-31 13:43:56`（101830 玩家实例首次出现 → 正式拼图 101829 首次出现）
- 受影响口径：在上述窗口内，获得 `马戏勋章 1210`（`activity_reward`）的同一会话中，发生 `activity_puzzle_task_fast_finish` 且每笔扣除 `1000 钻`。
- 查询结果：`264` 名玩家、`99` 个服务器、`1745` 次立即完成，需返还钻石合计 `1,745,000`；单人返还 `1,000 ~ 23,000`。
- 对应异常产出：匹配会话内共获得 `5,215` 个马戏勋章、`36` 张纪念卡。
- 补偿建议：逐人全额返还实际扣除钻石，玩家已经获得的勋章/纪念卡不回收。

## 防误算

- 公共 reason `activity_puzzle_task_fast_finish` 同时被其他拼图使用，不能直接全量取。
- 101830 使用任务组 109，所有可跳过任务的 `FinishCost` 均为 1000 钻。
- 两名玩家同会话出现 4 笔 1500 钻扣除，与 101830 配置不符，已排除。
- iGame 最近 1000 封发件箱中未发现 `BINGO / 拼图 / 101830` 专项补偿单；`马戏节开启公告_87服_20260731` 不是补偿单。

## 文件

- `BINGO101830_受影响玩家明细.csv`：UTF-8 BOM，逐人扣钻/勋章/纪念卡与时间。
- `BINGO101830_钻石返还_iGame导入.csv`：GBK，X3 六列格式，道具列 `[1002*数量]`。
- `content_draft_cn_en.json`：待用户确认的中英文邮件文案；确认后再扩散 20 语。
- `summary.json`：机器可读汇总与补偿分布。
- `export.py`：可复跑查询与导出脚本。
- `BINGO101830_补偿核对台.html`：离线核对页，含口径、分布、邮件预览、逐人筛选与金额复算；不连接 iGame。
- `generate_audit_html.py`：从现有明细、汇总和文案重新生成核对页。

## 发送状态

尚未提交 iGame。已完成生成器 dry-run 与独立交叉校验：264 行、玩家集合一致、六列 GBK 格式正确、逐人金额满足 `立即完成次数 × 1000`、附件合计 `1,745,000` 钻。文案与补偿口径经用户确认后再执行金丝雀 → 他人审批 → 全量 → 数仓入账复核。

## 2026-08-04 暂不上拼图服筛选版

- 拼图本期投放范围来源：`C:\ADHD_agent\KB\方法论\tools\circus_launch_gen.py` 的 `SERVERS`，D35+ 共 87 服；用户确认其补集是“暂时不上拼图”的服务器批次，并非“不上节日”。
- 筛选规则：从原始 264 人中排除上述 87 个本期上拼图服；不是用服号范围猜测。
- 筛选结果：121 人、31 个服务器、713 次立即完成，应返 `713,000` 钻；与节日服清单交集为 0。
- 已排除本期上拼图服玩家：143 人、`1,032,000` 钻。
- 正式文件：`BINGO101830_受影响玩家明细_暂不上拼图服.csv`、`BINGO101830_钻石返还_iGame导入_暂不上拼图服.csv`、`summary_puzzle_paused_servers.json`、`BINGO101830_补偿核对台_暂不上拼图服.html`。
- 旧的 `_非马戏节服` 命名文件为废弃审计底稿，不用于发送。
- iGame 新导入表 dry-run 已通过：121 人、道具合计 713,000、20 语言。
- 已于 2026-08-04 提交 iGame：`mailId=4785631`，`status=2`（待他人审批放行），`receiverCount=121`，`sentAt=null`，玩家行 `failReason` 均为空。
- 邮件口径最终确认为“活动曾异常开放”，不是“提前开放”；remark=`BINGO101830异常开放钻石返还_暂不上拼图服_121人_20260804`。
