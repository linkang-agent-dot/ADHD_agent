# 英雄养成手册 · 二选一弹窗 UI 切图交付

> 全部透明底 PNG，**grfal remove_background 抠图**（非双底差分），已过透明验证闸门。目标 prefab：`UIActvLoginChoice.prefab`（豪华列 `ColLuxury`）。
> Unity 落点：`client/Assets/Res/UI/Spirits/ActvLoginChoice/`

## 切图清单

| 文件 | 用途 | 落点 / 替换目标 | 备注 |
|------|------|----------------|------|
| `panel_luxury_v2.png` | 豪华列 panel 背板（华丽版） | 替换 `ColLuxury/RewardList` 所在列的旧 `panel_luxury_final` | 9-patch 可拉伸，深金+紫+雕花+宝石+内发光 |
| ✅`badge_x2_bonus_fromreal.png` | 「×2 BONUS」翻倍徽章（**定稿·用这个**） | 挂 `ColLuxury` 右上角 | 从真实界面精修版切+改BONUS·X3风紫金星芒；语言中性不烧中文 |
| `tag_discount_-17.png` | 「-17%」折扣角标 | 挂豪华 `$49.99` 价格旁 | -17%=相比买两份普通版($59.98)省16.7% |
| `tagline_ribbon_2x_luxury.png` | 2X 标语丝带条（空底·叠文字） | 铺豪华列空白处，上叠 TFWText | 文案「每日奖励X2！」走 i18n 不烧图 |

> ⚠️**徽章定稿 = `badge_x2_bonus_fromreal.png`**。以下 4 个是**废弃候选，别用**（待清理）：`badge_x2_bonus.png`(立体带剑)、`badge_x2_bonus_nosword.png`(去剑立体)、`badge_x2_bonus_flat.png`(扁平)、`badge_x2_bonus_x3style.png`(重画X3风)。收口时 fromreal 改名为正式 `badge_x2_bonus.png` 并删这 4 个。

## 2X 标语文案（给客户端 TFWText / i18n，配丝带条用）

丝带条是空底装饰，文字由客户端叠（多语言）。**最终文案只用一句**：

> **每日奖励X2！**

（一条丝带 + 这句即可，不铺多行。i18n key 建议 `TXT_ActvLogin_Luxury_Tagline`，cn=「每日奖励X2！」）

## 基础列 panel

保持现有朴素版**不动**（做豪华的反衬，强化档位对比）。

## 集成步骤（要用时）

1. 切回分支：`git -C C:/x3-project switch X3NEW-hero-handbook-deluxe`（手册 feature 分支）
2. 拷贝本目录 PNG 进 `client/Assets/Res/UI/Spirits/ActvLoginChoice/`（panel 二选一，留一个）
3. Unity Inspector：
   - `ColLuxury` 的 panel Image 换成 `panel_luxury_v2`
   - `ColLuxury` 右上角加 Image = `badge_x2_reward`
   - 豪华 `$49.99` 价格旁加 Image = `tag_discount_-17`
   - 豪华列空白处铺 `tagline_ribbon_2x_luxury` + TFWText（i18n key）
4. 提交遵循 [reference_x3_project_repo] 三铁律：只 `git restore` 丢噪音**禁 git clean**；内嵌 gdconfig 脏先在子仓提交；proto 改动 proto+cs+bytes 一套。
