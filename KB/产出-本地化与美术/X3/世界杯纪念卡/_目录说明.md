# 世界杯纪念卡 卡面 — 目录说明（2026-06-16 校正版）

> 经历多轮返工 + sub-agent 实测校正。**以本文件为准**，正文里早期的"正放/0.7/合成/v5"等说法全是错的，已废。

## ✅ FINAL（用这两张）
- **256 道具图标** `WC_MemorialCard_Aerisia_FINAL.png`（256×256 RGBA）= 对齐 `icon_card_image_*`
- **384 大图** `WC_MemorialCard_img_big_79.png`（384×523 RGBA）= 对齐 `img_card_image_*`
- 两张都=正面爱莉希雅(白金啦啦队装+10号+花球) + 绿茵球场+大力神奖杯 + 金框。

## ★★两格式真实规格（实测，反复返工的根因都在这）
| 格式 | 尺寸 | 卡区/构图 | 倾斜 | 比例 |
|---|---|---|---|---|
| **256 道具图标** icon_card_image | 256×256 RGBA | 卡居中、四角透明、alpha_bbox≈(34,20,222,235)≈189×216 | **斜放 ~9°顺时针**(不是正放!) | **~0.88(偏方,不是0.7瘦)** |
| **384 大图** img_card_image | 384×523 RGBA | **正放满幅**(bbox=0,0,383,522整张铺满,只圆角透明) | 正放(不斜) | 0.734 |

- ⚠️**我反复踩的两个坑**：①把 256 做成**正放**(历史是斜9°) ②以为历史 256 是**瘦比例0.7**(实测**0.88偏方**)。两条都错过,导致一直"对不上"。
- 历史 256 斜放方向/角度：顺时针约 9°（rotate +9° CCW 可扶正轴对齐验证）。

## 产出方法（标准流程，下次照做）
1. **源用正放正面卡**（`候选_轮3\WC_r3_front_v1.png` 这种正放带框的），不要用 AI 直出的斜卡(`_src_img11`/`alt1` 斜~9°但角度不准、不可控)。
2. **256**：源裁成 upright 小卡(命中 189×216 footprint 比例~0.88) + 圆角 → **旋转 9° 顺时针** → 居中放 256 画布，命中历史 bbox(34,20,222,235)。
3. **384**：源 cover 到 384×523(保宽高比,裁上下不拉伸) + 圆角(r≈22)，正放满幅。
4. 自检：和 `icon_card_image_76`/`img_card_image_76` 并排,比 bbox/倾斜/比例/构图,丢进历史堆里不违和才算过。

## 直出真源 / 候选（留底）
- `候选_轮3\WC_r3_front_v1.png`（正面正放,FINAL 实际用源）、`候选_轮2\`、`WC_MemorialCard_正面_*`（轮1）= 各轮正面候选。
- `WC_MemorialCard_Aerisia_v4直出_alt1.png` / `_src_img11.png` = 斜放直出版,**不直接用**(角度不可控)。
- `参考_历史纪念卡卡面\`(78张256) + `参考_历史正面大图\`(72张384) = 历史标准库。

## _废弃/（错误路线，留档不用）
- 初版直缩压扁 / v2主稿PIL合成 / v3卡片图合成 / v4_格式化cover变形 / v5(正放0.73瘦,曾误当FINAL) + 各废对比图。
- 教训链：直缩→压扁；PIL套框合成→用户否决走直出；cover硬塞比例→变形；**正放+瘦比例→对不上(历史是斜9°+0.88方)**。最终对的=正放正面源+裁0.88+旋转9°(256)/cover满幅(384)。
