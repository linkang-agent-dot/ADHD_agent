# 测试任务书：X3 兵种术语统一改动 · Unity 运行时验证

> 交给带 unity-mcp 的 CC 会话执行。目标：验证 gdconfig dev 上三笔 i18n 改动经「导表 → 客户端 → 运行时」链路后显示正确、无回归。数据层（tsv 内容本身）已由上游会话断言式验证过，**你要验的是链路和渲染，不用重复审 tsv 逻辑**。

## 一、被测变更

gdconfig（git.tap4fun.com:x3/gdconfig）dev 分支三个 merge，**只动了 `tsv/i18n/Text__Text.tsv` 一个文件**：

| merge | 内容 |
|---|---|
| `40ef0fc` | 英文 7 行：斗士 Fighter/Brawler → Warrior（含霍普金斯技能 9037、航线先锋路线名） |
| `4e33cd3` | 10 语言 80 处：斗士按各语言主流词收敛 + 病句修复（9037 句式 / 1043 补主语和回合数 / fr 220613 残句） |
| `8704504` | kr/jp/id 三兵种 583 处：斗士=투사/ウォリアー/Petarung、射手=소총수/アーチャー/Penembak、猎人=사냥꾼/ハンター/Pemburu |

## 二、前置条件

1. 拉 gdconfig `origin/dev` 最新（确认 `git log --oneline` 包含 `8704504`）。
2. **跑导表**（这是关键——git ≠ 生效）：`Tools/table_exporter/ExportTable.py` 直接读 `tsv/i18n/Text__Text.tsv` 生成 i18n 二进制；确认客户端工程吃到新产物。⚠️ **导表产物只留本地测试用，不要 commit**（合分支只合代码不合 bytes，仓库红线）。
3. Unity Editor 打开 X3 客户端工程 + MCP for Unity Start Server。三个已知坑：
   - unity-mcp 配置是 **project-scoped**，必须在挂了 UnityMCP 的工程目录里起 `claude`；
   - server 命脉是弹出的 mcp-terminal 黑窗口，别关；误关去 Unity → Window → MCP for Unity 重启 server；
   - CC 会话缓存 MCP 探测结果，server 重启后要 `/exit` 重开会话。

## 三、测试执行（三层，按序）

### L1 · 导表产物断言（不开 Play）
对导表生成的 i18n 数据（或运行时 i18n 字典），逐语言断言下面「四、期望值」全部命中。优先用脚本/MCP scripting 批量查 key，别肉眼翻。

### L2 · 运行时全语言扫描
Play mode 下用 i18n API 遍历 16 语言 × 期望值表中的 key：
- 值 == 期望（或包含期望片段）；
- **无 fallback**（没有取到 en/cn 兜底——注意空列回退 en→cn 是机制，本次改动的 cell 都非空，出现回退=链路没吃到新表）；
- Console **零新增异常**，重点盯 `FormatException`（改动保留了全部 `{0}`/`{1}` 和 `<color>` 标签，但需运行时确认）。

### L3 · UI 抽查（en / kr / jp 三语言即可）
1. **霍普金斯技能页**：英雄 9037（Black Gold Contract · Hopkins）→ Skills 页第三技能。en 应显示 "…your Warrior attack and defense are boosted by 15% simultaneously"（不再是 Brawler/activate together）；升级预览行 en=`Warrior Atk/Def: +…`、kr=`투사 공/방`、jp=`ウォリアー攻防`。截图。
2. **航线先锋（Route Pioneer）**：六个路线名。en 两处 `…Route—Warrior`（不再是 Fighter）、kr `지휘관 항로-투사`/`파도타기 항로-투사`、jp `…航路-ウォリアー`。截图。

## 四、期望值表（L1/L2 用）

**Key 1：`TXT_HeroSkillInfo_NewDescUp_9037`（一个 key 覆盖 16 语言）**

| 语言 | 期望值 |
|---|---|
| en | `Warrior Atk/Def: +{0}` |
| sp | `Atk/Def Guerrero: +{0}` |
| fr | `Atk/Déf Guerrier : +{0}` |
| de | `Krieger Angriff/Verteidigung: +{0}` |
| kr | `투사 공/방: +{0}` |
| tw | `鬥士攻防：{0}` |
| ru | `Атк/Защ Воина: +{0}` |
| ua | `Атк/Зах Воїна: +{0}` |
| jp | `ウォリアー攻防: +{0}` |
| it | `Atk/Dif Guerriero: +{0}` |
| pt | `Atk/Def Guerreiro: +{0}` |
| th | `นักรบ โจ/ป้อ: +{0}` |
| id | `Atk/Def Petarung: +{0}` |
| tr（未改，应仍为） | `Savaşçı Sld/Sav: +{0}` |
| pl（未改，应仍为） | `Atk/Obr Wojownika: +{0}` |

**Key 2：`TXT_Route_Name_5000`** — en `Commander Route—Warrior` / it `Rotta del Comandante—Guerriero` / jp `指揮官航路-ウォリアー` / kr `지휘관 항로-투사` / id `Jalur Komandan-Petarung`。`TXT_Route_Name_6000` 同理（Wavebreaker / 파도타기 항로-투사 / 波砕き航路-ウォリアー）。

**Key 3：`TXT_HeroSkillInfo_NewDesc_1043`** — en 含 `Every other turn, Warriors deal an extra`；sp 含 `Cada 2 turnos, los guerreros`；ru 含 `Каждые 2 хода воины`；de 含 `die Krieger`。⚠️ 语义变化点：原文各语言错译成「每1回合」，现统一为「每间隔1回合」（=every other turn），实际技能逻辑没变、只是描述纠错。

**Key 4：`TXT_BuffTemplate_AffixName_220613`** — fr 必须以 `…tous les marins sont des guerriers` 结尾（修复前句子在 sont 处断掉）。

**Key 5：`TXT_ActvOnline_ActvName_100546`** — ru `Великий Разгром — Командир Воинов`、ua `Великий Розгром—Командир Воїнів`（修复前 ru 丢兵种词、ua 一半英文）。

**Key 6：`TXT_MemorialCard_GetTips_60`** — tw `鬥士營地達到14級可獲取`（修复前是简体「斗士」）。

## 五、回归检查（这些必须**没变**）

1. 职业标签行（本次拍板基准，不允许被波及）：`Text_Soldier_fighter`=Warrior/투사/ウォリアー/Petarung、`Text_Soldier_hunter`=Hunter/사냥꾼/ハンター/Pemburu、`Text_Soldier_shooter`=Archer/소총수/アーチャー/Penembak。
2. `TXT_HeroSkillInfo_NewDesc_1046` kr 仍含 **광전사**（狂战士，含전사子串，负向断言保护过——若变成 광투사 = 严重误伤，立即上报）。
3. `TXT_BuffTemplate_AffixName_120023` id 仍为 `Serangan prajurit petarung tunggal`（prajurit 在译「士兵」，受保护未动）。
4. `TXT_HeroLabel_Tip_4`（竞技场英雄）各语言未变。
5. cn / en 之外未在改动范围的语言列（如 Key1 的 tr/pl）与表中「未改」值一致。

## 六、通过标准与报告

- **PASS**：四、五节全部命中 + Console 无新增 i18n/Format 异常 + L3 两张截图正确。
- 报告格式：每个 Key × 语言的 ✓/✗ 表 + 截图 + Console 摘录；有 ✗ 时附实际值。发钉钉给 Zoe，不用修复——只报告，改动决策在上游会话。
- 若 L1 就发现值是旧的：先检查导表是否从 dev 最新跑的（这是最常见假阴性，排查口径见仓库 CLAUDE.md「git≠生效」）。
