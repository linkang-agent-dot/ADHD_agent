# 异国美酒储蓄罐 · 可重复购买改造 策划案

> 单玩法改造案 — X3 新一期 3 活动中的第 2 个。把现有「只能买一次」的异国美酒储蓄罐改造成「**可多次购买 + 越买越贵 + 购买间 CD 重置储蓄**」的付费深度款。
> 复用现有储蓄罐界面 prefab 改造 + 服务端逻辑新增。
> 立项：2026-06-18 ｜ 状态：策划案 v1（待评审 / 待落 GSheet）

---

## 〇、包装层 ★

| 项 | 内容 |
|---|---|
| 活动名称 | 异国美酒储蓄罐（沿用现名，无需改皮） |
| 界面主标题 | 异国美酒储蓄罐 |
| 副标 / 卖点 | 「越攒越多 · 砸罐折扣换取异国美酒」 |
| 砸罐按钮文案 | `折扣砸罐`（显示当前档价格） |
| 主题视觉 | 沿用现有储蓄罐底图 `DK_ui_Howtogetit_piggybank_3`，无新增主题美术 |

> 本案是**机制改造**而非换皮，包装层基本不动；新增的视觉元素见「美术需求」节（次数指示 / CD 倒计时 / 越买越贵的价签）。

---

## 一、设计目的 ★

1. **核心目的（why）**：把异国美酒储蓄罐从「一次性折扣礼包」升级为「**可重复、越买越贵、有节奏（CD）的付费深度引擎**」，承接中高 R 对【异国美酒（招募专用货币 Item 7002）】的持续需求，拉长单活动付费深度与生命周期。
2. **付费点**：折扣砸罐 5 档递增（$4.99→$9.99→$19.99→$49.99→$99.99）；性价比基本持平，让高 R「越买越多但不亏」，靠 CD 控制冲顶节奏。
3. **目标人群**：有持续招募/抽卡需求的中高 R 玩家（异国美酒消耗大户）。
4. **最终大奖**：第 5 档（$99.99）一次性折扣获取最大批异国美酒。

---

## 二、玩法说明 / 概述 ★

核心循环：**攒酒 → 砸罐（付费折扣领取累积量）→ 储蓄清零 + 进入 CD → CD 期间不攒 → CD 结束恢复攒酒 + 解锁下一档（更贵、给更多）→ 直到第 5 档封顶**。

- 储蓄来源**沿用现有逻辑不变**（异国美酒按既有方式累积进罐），本案**唯一改动是「砸罐后清零并暂停累积，直到 CD 到期才恢复」**。
- 「越买越贵」= 5 档顺序解锁，每档价格递增、产出也递增（单位价值率拉平）。
- 封顶：买满前 4 档后，**永久停留在第 5 档（$99.99）可无限循环复购**（每次仍走 CD），不再往上爬也不停止。

---

## 三、规则说明 ★

### 3.1 基础设定 ★
- **载体**：复用现有储蓄罐系统（`Pack.PackType = 21` 储蓄罐礼包类型 + `PiggyBank` 配置表 + `UIPiggyBankContent.prefab`）。
- **资源**：异国美酒 = `Item 7002`（定义「招募特定英雄专用的美酒」）。
- **入口/解锁**：沿用现有解锁条件（现配 `Pack.PlayerLv` 解锁等级；储蓄罐入口走道具获取/活动入口，按现状不变）。
- **活动开启**：随本期活动排期上线（TimeCycle / ActvOnline 按本期 3 活动统一配，本模块不新增独立活动类型）。

### 3.2 越买越贵（5 档顺序解锁）★ — 新增
1. 异国美酒储蓄罐由现状 **1 档** 扩成 **5 档（档 1→5）**，初始仅档 1 可买。
2. 砸罐购买档 N（付费）→ 立即发放该档累积的异国美酒（折扣后批量）→ **储蓄清零** → 进入 CD → 档 N 标记已购、解锁档 N+1。
3. 档 N+1 价格 > 档 N，产出量 > 档 N（见 §数值）。
4. 档 1–4 各只可购买 1 次（`Pack.BuyCount = 1`），顺序不可跳档。
5. **第 5 档（$99.99）不限购（`BuyCount = 0`）**：买满前 4 档后永久停留在第 5 档，可无限循环复购（每次仍受 CD 约束）。

### 3.3 CD 重置储蓄 ★ — 现状已有（复用，不开发）
> 实测：异国美酒罐当前**已在走 CD**（`Pack.ColdTime`）。本节为现有行为，本案不改动，仅在档位提升后让 CD 串进每档之间。
1. 砸罐后进入固定 CD（**沿用现状 24h**，走 `Pack.ColdTime`；可调）。
2. **CD 期间**：储蓄罐显示冷却倒计时，**异国美酒不累积**（清零后保持 0）、不可砸罐。
3. **CD 结束**：恢复异国美酒累积（沿用原攒酒逻辑），下一档可砸。
4. CD 复用 `GiftMeta.cs` 现有 `ColdTime` 校验（`now - lastBuyingTime < ColdTime`，按 `Pack.Group` 记录组冷却 → 5 档共用一个 Group，串成同一条 CD 时间线）。

### 3.4 红点提示 ★
- 储蓄罐入口红点：**当前档可砸罐**（非 CD 中 + 储蓄已到可领条件）时亮。
- CD 中、已封顶 → 不亮。

### 3.5 #其他通用功能
- 名称/描述/时间/背景沿用现有储蓄罐通用支持，无特殊定制。

---

## 四、配置表（内嵌 schema）★

> 真源 = `C:\x3\gdconfig\tsv\`（改 tsv 不碰 xlsx）。下列为本案**改动/新增**的表。

### 4.1 `PiggyBank__PiggyBank.tsv` — 异国美酒由 1 档扩成 5 档
现状仅 1 行（ID=46，ResourceID=7002，Level=1，**PackID=500031**，GroupId=260）。对应 Pack 500031：Price 107($9.99)、ColdTime 24h、BuyCount 空(不限)、PlayerLv 3、Group 8。
> 改造时为 5 档分配独立新 PackID（落表扫 tsv 防撞号）。

| schema | ID | ResourceID | Level | PackID | Name | ValueText | MainBg | Num | GroupId |
|---|---|---|---|---|---|---|---|---|---|
| 类型 | int | int | int | int | string | string | string | int | int |
| 引用 | — | Item | — | Pack | TXT_ | — | DK_ | — | Grade.GroupId |
| 中文 | 编号 | 资源ID | 分档(=购买档) | 礼包ID | 存钱罐名 | 超值文本 | 背景图 | 资源数量(废) | 组id |
| 英文 | ID | ResourceID | Level | PackID | Name | ValueText | MainBg | Num | GroupId |

新增 5 行（档 1–5），ResourceID 均=7002，PackID/GroupId 各独立递增（详细 ID 落表时分配，扫 tsv 防撞号）。

### 4.2 `PiggyBank__Grade.tsv` — 5 档产出（价涨量也涨）
为 5 个新 GroupId 各配产出 Num。异国美酒为定值产出（不随酒馆等级 scale，沿用现 GroupId=260 的恒定写法，Grade 区间填满 3–35）。

| schema | ID | CoinGrade | GroupId | Grade | Num |
|---|---|---|---|---|---|
| 类型 | int | int | int | int | long |
| 引用 | — | — | PiggyBank.GroupId | — | — |
| 中文 | 编号 | 金币系数(废) | 组id | 酒馆等级 | 资源数量 |
| 英文 | ID | CoinGrade | GroupId | Grade | Num |

数值见 §数值设计（默认 10 / 20 / 40 / 100 / 200，🔧待数值复核对齐异国美酒养成线价值）。

### 4.3 `Pack__Pack.tsv` — 5 个新储蓄罐礼包（递增价格 + CD + 限次 + 顺序链）
每档一个 Pack。相关列 4 行 schema（横铺）：

| schema | col1 | col8 | col10 | col21 | col22 | col24 | col25 | col44 |
|---|---|---|---|---|---|---|---|---|
| 类型 | int | int | int | int | int | int | string | int |
| 引用 | — | PackPrice | — | — | — | Pack(前置档) | — | — |
| 中文 | 编号 | 价格(点id) | 礼包类型 | 购买次数限购 | 玩家等级 | 前置礼包 | 冷却时间 | 组(CD分组) |
| 英文 | ID | Price | PackType | BuyCount | PlayerLv | Prepack | ColdTime | Group |

各档取值：

| 档 | col10 PackType | col8 Price | col21 BuyCount | col25 ColdTime | col24 Prepack | col44 Group | col22 PlayerLv |
|---|---|---|---|---|---|---|---|
| 含义 | =21 储蓄罐 | 价格点id | 单档限购 | 冷却 | 前置礼包 | CD分组 | 解锁等级 |
| 档1 | 21 | 105($4.99) | 1 | 24h | — | G | 沿用现状 |
| 档2 | 21 | 107($9.99) | 1 | 24h | 档1 | G | 同上 |
| 档3 | 21 | 111($19.99) | 1 | 24h | 档2 | G | 同上 |
| 档4 | 21 | 116($49.99) | 1 | 24h | 档3 | G | 同上 |
| 档5 | 21 | 115($99.99) | **0**(不限) | 24h | 档4 | G | 同上 |

> Price 价格点 id 参照 `Pack__PackPrice.tsv`：105=$4.99 / 107=$9.99 / 111=$19.99 / 116=$49.99 / 115=$99.99。
> 5 档共用同一 `Group`(G) → 复用 `ColdTime` 的 `groupBuyingTimes` 串成同一条 CD 时间线。
> `Prepack` 顺序链能否原生实现「顺序解锁 + 不跳档」需**程序确认**（见 §实现路线 开放项）。

### 4.4 全局变量 / ConstConfig
- 若 CD 时长不走 Pack.ColdTime 而要独立全局变量，落表时确认（默认直接用 ColdTime）。

---

## 五、实现路线（配置 vs 程序）★

> ★实测校准（2026-06-18，用户在 dev 看过）：异国美酒罐**当前已经有 CD、能重复砸**——「只能买一次」的真实含义是**只有单档、买完同价重复、没有档位往上爬**。所以**唯一真新增 = 档位提升（越买越贵 + 量也涨）**；CD / 可重复 / 储蓄清零 / CD 内暂停攒酒都是**现状已有，不开发**。

| 需求 | 落点 | 复用 / 新增 |
|---|---|---|
| 购买间 CD | `Pack.ColdTime=72h`（现状已生效） | **现状·复用**（ColdTime 校验 GiftMeta:1290） |
| 多次购买（重复砸罐） | 现状已支持（CD 间隔重复买） | **现状·复用** |
| 储蓄清零 + CD 内暂停攒酒、CD 到期恢复 | 现状已是此行为 | **现状·复用**（不改） |
| **档位提升：越买越贵 + 量也涨（封顶 5 档）** | 按购买次数推进档位：每次砸罐后下一档价更高、产出更多，至第 5 档封顶 | 🔧**唯一真新增**（详见下） |

**档位提升实现路线（唯一开发项，两方案，待程序选）**：
- **方案 A · 多档顺序链（偏配置）**：异国美酒罐由 1 档扩成 5 个 PiggyBank 行 + 5 个 Pack（Price 递增 / Grade.Num 递增），用 `Pack.Prepack`(前置档) 串成顺序解锁、各档 `BuyCount=1`、共用 `Group` 复用 CD。前提=`Prepack` 对 PackType=21 生效（🔧程序确认）。
- **方案 B · 购买档 index（偏代码）**：服务端记录该罐已购次数 N，按 N 选当前档（价格/产出查档表），砸罐后 N++、推进到下一档，封顶 N=5。比 A 更内聚、界面更易表达「第 N/5 档」。

**开放项（落地前确认）**：
1. ~~档位提升走方案 A 还是 B？~~ → **已定 B**。查代码实测：方案 A **无法纯配置实现自动换档**——客户端 `CheckCanShowGift`(`Entity/Player/Gift/GiftMeta.cs:446`) 对储蓄罐只判玩家等级 + 时间窗，**不判 BuyCount(买满不隐藏)、不判 Prepack(前置不解锁)**；`UIPiggyBankContent.PrefixData`(`UI/ItemObtain/UIPiggyBankContent.cs:68`) 选显示档是**按「还差多少量 needCount」选最小够用档，不是按购买次数**。所以 5 档 Pack 全配上去不会「砸完跳下一档」。→ 既然必须改代码，走 B。
2. ~~封顶后行为？~~ → **已定：买满前 4 档后永久停留在第 5 档可无限循环复购**（第 5 档 `BuyCount=0`），不停止。
3. ~~现状 CD 时长？~~ → **已实测：现状 Pack 500031 = `ColdTime 24h`、`BuyCount 空(不限购)`、Price 107($9.99)、PlayerLv 3、Group 8**。即现状已是「不限次 + 24h CD + 同价 $9.99 同量 10」。**改造沿用 24h CD**（除非另定）；档 1–4 改成 `BuyCount=1` 顺序解锁、档 5 `BuyCount=0` 永久。
> ★关键修正：现状储蓄罐**本就无购买数量上限**（BuyCount 空），唯一限制=24h CD + 储蓄需攒满(0/10) + 活动时长。「只能买一次」实指「只有单档同价、没有越买越贵」。本案 PackID 真值 = **500031**（非早期误记的 500000）。

### 5.1 方案 B 实现要点（唯一开发项）
- **配置**：异国美酒罐配 5 档（5 个 PiggyBank 行 + 5 个 Pack），Price 递增 / Grade.Num 递增、共用 `Group` 复用 CD；**档 1–4 `BuyCount=1`，档 5 `BuyCount=0`(不限购)**。
- **客户端改 1 处**：`UIPiggyBankContent.PrefixData` 选档逻辑由「按 needCount 选」改为「**当前档 = 第一个未买满(purchaseNum < BuyCount)的档**」。因档 5 BuyCount=0 永不满足「买满」→ 买完档1–4 后**自动永久落在档 5**，无需额外封顶判断。
- **服务端**：BuyCount 限购校验现成(`GiftMeta.cs:1279`)、ColdTime CD 现成(:1290)，无需新增；砸罐发放/储蓄清零/CD 沿用现状。
- **界面**：★用户定 **下一档不做任何表现，砸完直接切数据**——同一张储蓄罐卡换价(Pack.Price)换量(Grade.Num)，**不加「第N/5档」指示、不加进度条**。

---

## 六、界面交互 ★（沿用现有储蓄罐卡，零新增界面元素）

> ★用户定（2026-06-18，看过 dev 原生界面）：**下一档不做任何界面表现，砸完直接切数据**。储蓄罐卡的现有元素（罐体/累积进度 0/10/CD 倒计时/打破按钮 $价/产出预览/超值折扣角标）全部沿用，砸完后**同一张卡只是价格(Pack.Price)、产出量(Grade.Num)换成下一档的值**。不加「第N/5档」指示、不加新进度条、不出新美术。

主界面：「獲取途徑(道具获取)」面板 `UIItemObtain`（item 7002 入口）内的储蓄罐卡 `UIPiggyBankContent`。

| # | 元素 | 显示内容 | 改动 |
|---|---|---|---|
| 1 | 储蓄罐主图 + 累积进度(0/10) | 罐体 + 当前累积异国美酒/需求量 | 复用 |
| 2 | CD 倒计时(如 23:57:03) | 距下次可砸时间 | 复用（现状已有） |
| 3 | 打破按钮 + 价签($X.XX/宝石) | 当前档 Pack.Price | 复用（值随档自动切）|
| 4 | 产出预览(打破可得 N) + 额外奖励 | 当前档 Grade.Num | 复用（值随档自动切）|
| 5 | 超值折扣角标 | 现状 | 复用 |

> 唯一“界面侧”改动其实在**数据层**：选档逻辑改为「当前档=第一个未买满档」（见 §5.1），界面本身不动元素。买完档 1–4 后**永久停留在第 5 档**（$99.99 可无限循环复购），储蓄罐卡始终显示第 5 档数据。

---

## 七、数值设计（默认值，🔧待复核）

| 档 | 价格 | PackPrice id | 异国美酒产出(Num) | 单位价值率 |
|---|---|---|---|---|
| 1 | $4.99 | 105 | 10 | 基准 ×1 |
| 2 | $9.99 | 107 | 20 | ≈×1 |
| 3 | $19.99 | 111 | 40 | ≈×1 |
| 4 | $49.99 | 116 | 100 | ≈×1 |
| 5 | $99.99 | 115 | 200 | ≈×1 |

- 产出 ∝ 价格（10:20:40:100:200 ≈ 4.99:9.99:19.99:49.99:99.99），实现「价涨量也涨 · 性价比持平」。
- 绝对量 10/20/… 为占位，🔧需对齐异国美酒（招募货币）的养成线/付费价值口径复核后定稿。
- CD：沿用现状 **24h**（Pack 500031 实测值），可调。

---

## 八、美术需求 ◇

**无新增美术**。界面零新增元素（§六），沿用现有储蓄罐卡全部资源（罐体/CD倒计时/价签/折扣角标）。5 档仅数值不同，无需新图。

---

## 九、本地化 ◇

**无新增本地化 key**。沿用现有储蓄罐卡文案（打破按钮/CD提示/产出文案等均为现成 key）；5 档名称若需区分另议，默认沿用「異國美酒儲蓄罐」单一名称。

---

## 十、活动事件（打点）◇

| 事件ID | 名称 | 触发条件 | 参数 |
|---|---|---|---|
| 🔧待分配 | 储蓄罐入口曝光 | 打开异国美酒储蓄罐界面 | 当前档位N / 是否CD中 / 当前累积异国美酒 |
| 🔧待分配 | 储蓄罐砸罐购买 | 砸罐购买成功 | 档位N(1-5) / 价格档(PackPrice id) / 产出异国美酒数 |
| 🔧待分配 | 储蓄罐砸罐失败 | 点砸罐被拦（CD中/封顶/储蓄不足） | 失败原因码 / 当前档位 |

> 关键转化漏斗：入口曝光 → 各档砸罐成功率 → 5 档逐档流失，用于评估「越买越贵」是否在某档断崖。

## 十一、日志需求 ◇

| 日志类型 | 内容 | 频率 | 说明 |
|---|---|---|---|
| 付费订单 | 砸罐购买（复用现有 Pack 礼包购买日志，带档位字段） | 每次购买 | 沿用现有礼包订单日志，需能区分到具体档位 |
| 资源流水 | 异国美酒发放（reason = 储蓄罐砸罐） | 每次砸罐 | 走 `StorageMeta` 发放日志，便于核查到手量与 Grade.Num 一致 |
| 储蓄重置 | 储蓄清零 + CD 开始时间 | 每次购买后 | 用于运营核查 CD/储蓄异常（如 CD 内仍累积、清零未生效） |

---

## 十二、验收 Checklist

| # | 验收项 | 通过 |
|---|---|---|
| 1 | 异国美酒储蓄罐由 1 档扩成 5 档，初始仅档 1 可买 | ☐ |
| 2 | 砸罐档 N → 发放对应产出 → 储蓄清零 → 进 CD → 解锁档 N+1 | ☐ |
| 3 | 5 档价格递增（4.99→99.99）、产出递增（量也涨） | ☐ |
| 4 | CD 期间不累积异国美酒、不可砸罐、显示倒计时 | ☐ |
| 5 | CD 结束恢复累积、下一档可砸 | ☐ |
| 6 | 买完档 1–4 后永久停留第 5 档($99.99)，可无限循环复购(仍受 CD) | ☐ |
| 7 | 端到端：tsv 改动 + 服务端逻辑 + prefab 三层一致，导表验证通过 | ☐ |

---

## 附：现状证据（供程序/接手参考）

- 配置：`PiggyBank__PiggyBank.tsv` 行46 = 异国美酒(ResourceID 7002, Level1, GroupId260, **PackID500031**)；Pack 500031=$9.99/ColdTime24h/BuyCount空/PlayerLv3/Group8；`Grade.tsv` GroupId260 = 恒定 Num 10。
- 服务端：`GameServer.Hotfix\PlayerMeta\Gift\GiftMeta.cs`
  - PACK_TYPE_PIGGY 买入校验 line 1199；发奖 line 2292（产出走 `GiftUtils.CalBankResNum(piggyCfgId, level)`）。
  - BuyCount 限购校验 line 1279–1288；ColdTime 冷却校验 line 1290–1300（按 `Pack.Group` 走 `groupBuyingTimes`）。
- 客户端：`client\Assets\Res\UI\Prefab\ItemObtain\UIPiggyBankContent.prefab` + `UICoinPiggyBankTips.prefab`。
- 对照：航海之路 piggy（`ActivityMeta.Voyage.cs`，count++ 累计、服务端不拦次数、判定交客户端）是另一套实现，不直接套用。
