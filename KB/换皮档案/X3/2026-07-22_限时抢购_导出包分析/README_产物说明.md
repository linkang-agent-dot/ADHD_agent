# 限时抢购 X2→X3 搬运 · 分析产物说明

唯一入口 = `KB\换皮档案\X3\2026-07-22_限时抢购(X2搬运).md`（决策与理由都在那，本目录只是证据附件）

## FINAL（现行有效）
- `依赖清单_X3已有vs必须拷.txt` — 160 guid → X3 已有 108 / 必须拷 52
- `手动导出vs上次AI落地_漏19个.txt` — 路A vs 路B 差距物证（漏的全在 shader→材质→贴图链）
- `落点映射规则验证_108件.txt` — 落点别照 X2 路径平移（只 68% 命中）的实证
- `落地执行日志_52件.txt` — 实际落地记录
- `资产_断链GUID自检_7个.txt` + `资产_断链GUID反查X2侧身份.txt` + `资产_断链三类判读_字段反推.txt`
  + `资产_5个未知guid来自嵌套特效件.txt` — 断链 7 个的完整判读链（2 真漏搬 / 5 X2 自己断 / 1 内置）
- `资产_同名重复件检查_guid差集盲区.txt` + `资产_替换前引用与shader冲突核查.txt`
  + `资产_重复图9处引用节点定位.txt` — 换皮无忧 REPLACE 那一半的范围
- `代码_旧Auto节点路径对新prefab验证_35条.txt` — 18命中/13相对路径误报/4真缺(全在Top组)
- `脚本引用体检_原件恢复后.txt` + `脚本guid清单_43条.txt` — 43 个脚本 X3 认得 42
- `localization_keys_MERGED.tsv` + `文案34条_中英预览.txt` + `i18n_*` 五份 — 文案落表全过程
- `manifest_FlashSale*.txt` ×4 — 四个导出包的依赖清单原件

## 存档（仅备查，别当现行）
- `FlashSale_Unity重序列化版_20260731.prefab.bak`
  = Unity 导入后重新序列化的版本（比源包小 149,403 字节，MD5 不同）。
  ⚠️ 曾误命名为「被换皮无忧改写过」——**证据表明不是**：换皮无忧的 PatchGuid 没动过它
  （重复图 guid `a38a9594…` 在里面仍是 9 处未替换）。是 Unity 自己的重序列化。
  **组件零丢失**（脚本 35 种 / 组件实例 2724 个，与源包完全一致）⇒ MD5 变属正常，不是损坏。
- `旧Auto_UIActvFlashSale.cs.bak` / `旧Auto_ShelfItem.cs.bak`
  = 撤除前那版 Auto_ 代码。⚠️ **不是代码生成的，是 AI 凭 X2 层级猜写的**，文件自己注释了
  「导入 prefab 后由 UI 工具重新生成本文件校准」——那步从没做过。恢复代码时这两个要重新生成，别直接用。

## 废弃（别用）
- `D:\newX2\Copy\FlashSale\` （不在本目录，提醒用）
  = 7/22 旧路B包 + 一次误导出的混合体，两次导出掺在一起拆不干净。**要用源包请走 `D:\newX2\<包名>\`**
