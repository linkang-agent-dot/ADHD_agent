---
tags: [kind/方法论, domain/配置换皮, proj/X3, year/2026-07]
---

# 换皮档案 2026-07-22 · X3 限时抢购（X2→X3 搬运）

> 项目：X3 ｜ 本轮：马戏节**第 17 活动 = 限时抢购**（7.29 版本）｜ 换皮性质：**X2→X3 新搬运**（非 X3 内克隆，用户 2026-07-22 拍板走 A 方向）｜ 最后更新：2026-07-22
>
> **本档案 = 本轮单一真源。开工先读；查到/决定/踩坑随手回写，别留对话里。**

---

## 🔴 结案状态（2026-07-30·**先读这段再读下面任何内容**）

**本案已全量撤除，下方所有"已落地/待解"内容只作历史存档与重做参考，不再代表仓库现状。**

| 撤除批次 | 范围 | 证据 |
|---|---|---|
| 2026-07-28 用户拍板推倒重做 | x3-project 代码 + prefab + 共享注册全清（120 文件 / 19.7 万行纯删除） | `973997a92ce`（dev_festival；等价 revert c329167a+abcc2e4f+b39aa389+e3122596） |
| 2026-07-30 用户令「删配置 + QA/DEV 同步」 | gdconfig 配置数据全清，三分支各一批纯删除（105 行，零新增） | `dev` a0fad296+ed5e8921 / `qa` a1236f9b / `dev_festival` f5eeaa13；导表 #2330 / #2332 / #2333 **全 SUCCESS** |

**删掉的配置**：AO 101029 · Item 1214/1215 · Pack 211101-211108 · Reward 30 行 12 组（8202101-04 + 211101-08，seq 15930189-15930218）· RuleTips 40003 · i18n 31 key（16 个带数字 ID + 15 个纯前缀 `TXT_ActvFlashSale_*`/`Text_ErrCodeActivityFlashSale*`）· 三张专属表 tsv。

**故意留下的（重做时直接接着用，别重新造）**：
- `Tools/table_exporter/def/actvflashsale{,pack,reward}_def.py` —— 三表 schema 原样在，字段定义/校验不用重写。
- `actvonline_def.py` 的 `ACTV_TYPE_FLASH_SALE=84`（+ SINGLE_SERVER + register_depend elif）与 `PostProcessData.py` 的 SKIP_TIMECYCLE —— **占住 84 号段防别人复用**；无 type84 活动行时是惰性代码。
- x3-project 里 18 个导表生成物（`ActvFlashSale{,Pack,Reward}` 的 proto/bytes/cs + .meta）仍在 dev/qa —— 导表只写不删既有生成文件，07-29 用户拍板不动；已 `git grep` 复核**无手写代码引用**，故删表不会引起编译问题。**看到它们不要以为没清干净，也不要再去清。**
- 旧实现完整存档：代码 `feature/flash-sale` · 配置 `feature/flashsale-tables`（=fa46c392）。

**重做入口（三条命令拿回全部结构）**：
```bash
git checkout c43303f7 -- tsv/ActvFlashSale__*.tsv        # 三表表头+数据行（gdconfig）
git diff c43303f7~1..c43303f7 -- Tools/                  # def 侧当初怎么登记的
git log --oneline feature/flash-sale                     # 代码侧四个提交（x3-project）
```

### 📌 X2 侧 prefab ↔ 代码 绑定映射（2026-07-30 GUID 反查实证，重做直接照这张搬）
四个 prefab 全在 `x2client/client/Assets/x2/Res/UI/Prefab/Activity/`，与代码**一对一**：

| prefab（GUID） | 唯一加载方 | 作用 |
|---|---|---|
| `FlashSale.prefab`（`0902ea9ddc9284741ab682cc3cbe05fa`） | `x2/Runtime/UI/Activity/UIActivityFlashSale.cs` + `Gen/Auto_UIActivityFlashSale.cs`（assetPath **不带 .prefab 后缀**）；另 `Editor/UITools/SaveData/FlashSale.asset`＝UI 工具绑定存档 | 活动主面板 |
| `FlashSaleItem.prefab`（`bce41049db034bc4294aae5968c32749`） | `P2/GameScript/Activity/UIActivityMayFestivalLimitTimeBuy/UIActivityMayFestivalLimitTimeBuyPkgItem.cs`（+`Gen/Auto_`） | 货架礼包格子 |
| `FlashSalePop.prefab`（`2ab46a0198f17114c8c9e98f4d512e09`） | `P2/GameScript/GameScriptOther/UI/UIActivityMayFestivalLimitTimeBuyModulePop.cs`（+`Gen/Auto_`） | 购买/详情弹窗 |
| `FlashSaleRewardGet.prefab`（`e429fcf7acf40db42ab447b77b27dcfb`） | `P2/GameScript/GameCommonRender/UI/Bag/UIFlashSaleRewardBox.cs`（继承 `UIValentineBox`；入口 `UIItemHelper.cs:660` 的 `UIValentineBox.Show<UIFlashSaleRewardBox>`） | 奖励获得弹窗 |

**三条结构结论（看 prefab 看不出来，只有反查得出）**：
1. 四个 prefab **互不嵌套**，各自由代码按 assetPath 独立加载 → 动一个不连带另一个。
2. prefab 上**零专属脚本**（只有 ButtonScale / UIConfig / RectTransformScaler / TFWOutline 等通用件）⇒ **绑定全靠代码里的节点路径**，节点改名即断。
3. 主面板壳 `UIActivityFlashSale` 只有 64 行，真逻辑在 module `UIActivityMayFestivalLimitTimeBuyModule.cs`（530 行）+ `Auto_`（144 行），它 `SetPanelRoot(gameObject)` 直接挂 FlashSale.prefab、**自己不出 prefab**；X2 命名沿用 P2 五月节「限时购」，活动 ID 注释 `21201441`，**别按 FlashSale 去搜逻辑代码**。

**FlashSale.prefab 内被硬编码的节点路径（换皮必须原样保留）**：
`Content/Grid`(货架容器)·`Content/Grid/Item1`(模板格) · `Layout`(待抢提示) · `Tip1`/`Tip2` · `TopLeft/Title`·`TopLeft/Sub` · `Rss/sp_icon`·`Rss/lab_num` ·
`Btn`＋`Btn/RedPointTips`＋`Btn/icon_text/{icon,text}`(抽奖按钮) · `SpecialBoxBtn` ·
`Countdown/Time/Layout1{,/Text1}`(开抢倒计时)·`Countdown/Time/Layout2{,/Text1}`(待开抢) ·
`ChestProgress/Cont/{ValLayout/Num, Progress/Progress/Fill, Progress/Chest1|Chest2|Chest3, Btn}`(助力进度三档宝箱+分享按钮) ·
`Pop`(分享弹窗＝**面板内子节点，≠FlashSalePop.prefab**)＋`Pop/Touch`(点击关闭)＋`Pop/centre_other/animation/Scroll View/Viewport/Content/ContentDetail/Title/BG (1)/BG (1)`·`BG (2)/PortName`·`BG (4)/ButtonSmall2`·`BG (5)/ButtonSmall2`。
RewardShow 侧：`CentreOther/Board/Layout/Num/Num`·`CentreOther/Board/Layout/txt`。

⚠️**一个缺口别当第 5 件套搬**：`UIActivityMayFestivalLimitTimeBuyRewardShow.cs` 的 assetPath 是 P2 老式逻辑路径 `Activity/Module/Ui21201400RewardGet`，**该 prefab 在 x2client 全仓按文件名+`21201400` 双向搜均查无**（同类只有 P2 的 `Ui_2024ValentineRewardGet.prefab` / `Ui_RewardGet.prefab`），但它在 module 第 395 行抽奖回调里**确实被调用** ⇒ 要么运行时走别的 AB（静态查不出），要么这条分支在 X2 就是断的。**重做的抽奖奖励展示建议直接走 `FlashSaleRewardGet`（UIValentineBox 体系）**。

**已导出的验收包**：`D:\newX2\Copy\FlashSale\`＝四件套＋依赖（20 个支撑 prefab 如 Button1/ItemMid/Progress/Fx_Ui_BoxAnim_Rotate、8 个 anim、48 张图），`_manifest.txt` 的 Source prefabs 就是这四个，**不用再导**。
**反查工具已固化**：`C:\ADHD_agent\skills\unity-prefab-tools\prefab_code_binding_map.py`（GUID 双向反查 + 节点路径清单，任何换皮开工先跑它）。

**为什么撤**（决策，别只看时间线）：X2→X3 是新搬运而非克隆，骨（协议+服务端状态机+分享助力破冰）要在 X3 从零实现，一周内没能把界面外显/直购 IAP 端到端/抽奖场次推送三块同时收干净（下方 §未完清单 A/B/C/D 就是当时的欠账）→ 用户判定不如推倒，等有完整档期重做。**结论：重做时先解 A/B（外显 + IAP 端到端），那是上次翻车点，不是配置数值。**

**踩坑沉淀去处**（通用规律已出档案，别在这找）：导表禁空表（X3NEW-20）+ csv.writer 污染裸引号 → `memory/reference_x3_tsv_export_migration.md`；批量删行工具 → `skills/x3-config-export/scripts/tsv_delrows.py`。

---

## 🔧 重做操作单 · 客户端 prefab 侧（2026-07-30 定，**要动手就照这份**）

### 第 0 步：先认清现状（30 秒，别做冤枉功）

| 事 | 状态 | 结论 |
|---|---|---|
| X2 侧四件套导出 | ✅ 已完成 → `D:\newX2\Copy\FlashSale\`（4 主 prefab + 13 支撑 prefab + 48 图 + 8 anim + 2 字体 + `_manifest.txt`，含 **52 个 UNRESOLVED guid** 清单） | **不用再导**；也**不用一个个 prefab 走 Unity 导出流程**（打包器一次吃了 4 个，`_manifest.txt` 的 Source prefabs 就是这四个） |
| X3 侧落地 | ❌ 已全清（`973997a92ce`）——`Res/UI/Prefab/Activity/FlashSale*` 与 `Res/UI/Sprite/UIActvFlashSale/` 现在都不存在 | 要重来 |
| 上一版落地成果 | ✅ 完整存在于 `973997a92ce~1`（=`e3122596` 的树） | **可以整包 git 取回**，见路线 A |
| 主仓 `C:\x3-project` | 在 `dev_festival`，工作区仅 3 个 `.meta` churn + `gdconfig` 子模块 + 未跟踪 `wt_dev_spec/` | 干净，可直接动；**动之前仍先 `git status` 亲眼确认没有别人 WIP** |

⚠️ **本案不适用录屏里那套「Unpack Completely」流程**。两个原因：① prefab 上**零专属脚本**，绑定全靠代码 `GetChild("节点路径")`，**节点名/层级动一下就断**；② 打包器是文件级拷贝、**保留了嵌套 prefab 结构**（上一版落地也是这个策略：`Fx_Ui_BoxAnim_Rotate` / `Public/BtnInfo` / `Public/Progress` 各自独立落地）。录屏那套是给「主面板要改名、要自包含」的场景用的（扭蛋机），不是这里。

### 第 1 步：选路线（这一步决定后面省不省事）

| | 路线 A · git 取回上一版 | 路线 B · 从 bundle 重新落地 |
|---|---|---|
| 工作量 | **10 分钟**（1 条命令 + Editor 验） | 半天到一天 |
| 拿到的东西 | 已经过 **4 轮 Editor 实机验收**的 prefab（界面能开 / 货架 8 盒渲染 / 倒计时 / 分享 / 抽奖全渲染）+ 已剥离 X2 特效 Graphic×14/Spine×1/LoopScroll×1 + 24 张已归位的图 | 原始 X2 结构，剥离/断链/公共件替换全部重做一遍 |
| 适合 | **默认选它**——上次翻车点是 A(卡片外显驱动错) + B(直购 IAP 端到端)，**不是 prefab 迁移做坏了** | 确定要换界面结构/重新设计外显，旧 prefab 结构不想要了 |

> 「用户拍板推倒重做」清的是**代码+prefab+配置**，理由是骨（协议/服务端/IAP）一周内没收干净。prefab 本身是通过验收的资产，取回来接着修 A 类问题最快。**要走 B 就明确说一声**，别默认。

### 第 2 步（路线 A）：取回 37 个资产

```bash
cd /c/x3-project
git status                      # 亲眼确认没有别人在途改动
git checkout dev_festival       # 已经在就跳过

# 一条命令取回：7 个 prefab + 5 个 anim + 24 张图（含 .meta，共 74 文件）
git checkout 973997a92ce~1 -- \
  "client/Assets/Res/UI/Prefab/Activity/FlashSale.prefab" \
  "client/Assets/Res/UI/Prefab/Activity/FlashSaleItem.prefab" \
  "client/Assets/Res/UI/Prefab/Activity/FlashSalePop.prefab" \
  "client/Assets/Res/UI/Prefab/Activity/FlashSaleRewardGet.prefab" \
  "client/Assets/Res/UI/Prefab/Activity/Fx_Ui_BoxAnim_Rotate.prefab" \
  "client/Assets/Res/UI/Prefab/Public/BtnInfo.prefab" \
  "client/Assets/Res/UI/Prefab/Public/Progress.prefab" \
  "client/Assets/Res/UI/Sprite/UIActvFlashSale" \
  "client/Assets/Res/UI/Animation/BoxAnim.anim" \
  "client/Assets/Res/UI/Animation/ui21201451BoxMain.anim" \
  "client/Assets/Res/UI/Animation/ui21201451BoxMainDefault.anim" \
  "client/Assets/Res/UI/Animation/RewardGetItemShow.anim" \
  "client/Assets/Res/UI/Animation/monthly_pkg_Switch.anim"

git status --porcelain | wc -l   # 期望 ≈74（每个资产带 .meta）
```
**权威清单怎么来的**：`git show --name-status 973997a92ce | awk '$1=="D"'` 里 `Res/UI/{Prefab,Sprite,Animation}` 那一段就是上一版实际落地的全部客户端资源（24 图 + 5 anim + 7 prefab）。`Public/ProgressBarTemp.prefab` 在 `~1` 里但**没被删** ⇒ 是 X3 原生件，别一起取。

⚠️ 代码侧**不要**顺手取回（`UIActvFlashSale*.cs` / 服务端 / proto）——那部分是重做的正题，且 `feature/flash-sale` 存着；这一步只恢复「皮」。

### 第 3 步（路线 B）：从 bundle 落地

只在明确要重来时走。落点照上一版（已验证的正确落点）：

| bundle 里的 | 落到 X3 |
|---|---|
| `Prefabs\FlashSale{,Item,Pop,RewardGet}.prefab` + `Fx_Ui_BoxAnim_Rotate.prefab` | `client/Assets/Res/UI/Prefab/Activity/` |
| `Prefabs\BtnInfo.prefab` `Progress.prefab` | `client/Assets/Res/UI/Prefab/Public/` |
| `Images\*`（48 张，上一版只用了 24 张） | `client/Assets/Res/UI/Sprite/UIActvFlashSale/Images/` |
| `Animations\*`（8 个，上一版只用了 5 个） | `client/Assets/Res/UI/Animation/` |
| `Other\*.ttf`（2 字体） | **先别落**——X2/X3 同源同 guid，多半原地解析（见下） |
| `Prefabs\` 里的 X2 公共件（`Button1/2/Small/Small2/Gray/button_Payment/Check1/ClickToClose/ContBubble/ItemMid`） | **先别落**，走第 4 步替换工具 |

**🔑 落之前必须知道的一条**：**X2/X3 同源同 guid**——上一版实测「24 图 / 11 公共件 / 2 字体**原地解析、零拷贝**」，断链数约为预估的一半。所以正确顺序是：**先只落 4 个主 prefab + 专属图 → 在 Editor 里看哪些真的断了 → 只补断的**，而不是把 48 张图 + 17 个 prefab 一股脑倒进去（会造出一堆和 X3 原件重复的副本）。

**落完再用清理工具**：`Tools ▸ Prefab换皮`（窗口「换皮无忧」）。本案 4 个 prefab 互不引用，白名单只认一个根 prefab ⇒ **别把 4 个包合一个目录用 root=FlashSale 清**（会把 Item/Pop/RewardGet 的专属图当"未引用"删掉）。要么 4 个目录各清各的，要么建个壳 prefab 把 4 个当嵌套实例挂上、用壳当 root 清一遍。用法/规则见 `KB\方法论\活动程序开发\X2到X3_UI资产搬运_PrefabAssetExport说明书.md` Part B。

### 🔑 结构事实：X3 侧只加载 `FlashSale.prefab` 一个（2026-07-30 grep 存档分支实证）

`git grep -l "<prefab名>" feature/flash-sale -- "client/Assets/Scripts/*"` 结果：

| prefab | X3 代码引用 | X3 实现怎么做的 |
|---|---|---|
| `FlashSale.prefab` | ✅ `Auto_UIActvFlashSale.cs` + `UIActvFlashSale.FlashSaleShelfItem.cs` | 主面板，assetPath 写死 |
| `FlashSaleItem.prefab` | ❌ **零引用** | 用主面板内**静态节点** `Content/Grid/Item1..9`，由 `FlashSaleShelfItem` 包装，不 Instantiate 子 prefab |
| `FlashSalePop.prefab` | ❌ **零引用** | 用主面板内子节点 `Pop` |
| `FlashSaleRewardGet.prefab` | ❌ **零引用** | 奖励展示走别的路 |

⇒ **手动拼装只需要动 `FlashSale.prefab` 一个**，另外三个是备用参考件（真要做详情弹窗/奖励弹窗再拼）。上一版把 4 个全落地是整包迁移的副产物，不是必需。

### 📋 Auto_ 节点契约（代码已按这些路径预绑，路径对不上＝空白/不响应，且**框架静默容错不报错**）

主面板 `FlashSale.prefab`：
```
Content/Grid                                      货架容器（下含 Item1..Item9）
Countdown  /  Countdown/Time/Layout2              倒计时组 / 备用态(常隐)
Countdown/Time/Layout1/Text1                      label「距开抢/距结束」
Countdown/Time/Layout1/Text2                      时间数字
ChestProgress/Cont/Progress/Chest1|Chest2|Chest3  助力三档宝箱
ChestProgress/Cont/ValLayout/Num                  助力进度数字
SpecialBoxBtn                                     抽奖入口(点击绑此节点)
SpecialBoxBtn/SpecialBox/Num                      剩余抽奖次数
BoxAnim / BoxAnim2 / Pop                          开箱动画层 / 内嵌说明弹层
Top/txt_title · Top/ActvDesc · Top/Time/txt · Top/Time · Top/btn_info   ← 需从 UIActvLaborGacha 移植
BtnShare                                          分享按钮，X2 原版没有，自己摆且名字必须叫这个
```
每个货架格子（`Content/Grid/Item1` 等，`FlashSaleShelfItem` 绑）：
```
BoxNormal（点击也绑此节点） / BoxSoldOut / BoxClose / Remain / Limit
BoxNormal/Card/Icon    道具图标  ← A 类翻车点在这
BoxNormal/Card/Value   道具数量
Remain/Num             全服余量        Limit/Text   限购文案
```

### 路线 C · 👐 手动走一遍（想亲眼看 AI 自动搬漏了什么，2026-07-30 用户选此路）

**为什么值得手动**：上次翻车点分类后，**A 类（卡片外显）和"绕过 Editor 直接改磁盘"那类确实是全自动的锅**——人在 Inspector 里点开 `BoxNormal/Card/Icon` 一眼就看见挂了 3 个组件；而 B 类（直购 IAP 端到端）和空货架（服务端漏钩子）跟自动化无关，手动也解决不了。

**C-1 先重导一遍（路 A，为跟 AI 包 diff）**——导出清单：

| # | Project 路径 | GUID | 必要性 |
|---|---|---|---|
| 1 | `Assets/x2/Res/UI/Prefab/Activity/FlashSale.prefab` | `0902ea9ddc9284741ab682cc3cbe05fa` | ✅ 必导 |
| 2-4 | 同目录 `FlashSaleItem` / `FlashSalePop` / `FlashSaleRewardGet` | `bce41049…` / `2ab46a01…` / `e429fcf7…` | 🔵 为比对 |

全仓 `find -name "FlashSale*.prefab"` 只此 4 个、**无同名重复件**（不像扭蛋机 `21201326` 有三份），Project 搜 `FlashSale` 不会选错。
操作＝**断链路线**（本案定：X3 侧重写，不保 x2 guid）：拖场景 → `Unpack Completely` → 拖回 `Assets/` 根 → `Tools ▸ X2 ▸ Prefab Asset Export`。落地名仍叫 `FlashSale` 故「改名」可跳；导完把 `Assets/FlashSale.prefab` 从 Unity 里删掉（别在 Explorer 删，留 meta 孤儿）。验收＝manifest `[Prefabs 0]` 是**正确**结果。三个开关全默认开（`导出 TFWText 本地化 TSV` 是 AI 包没有的东西）。
🔴 **输出目录每次必须填本次专属目录**：默认 `D:\newX2\Copy` 会写进 `...\Copy\FlashSale\` ＝ 7/22 旧包。工具 `GetUniqueDestinationPath` 重名**不覆盖**、加 `_1` 后缀塞旁边 ⇒ 旧包不丢但混成一坨拆不干净（07-31 踩过一次）。实际最终落在 `D:\newX2\<包名>\`。
**导完 diff 三样**：①`_manifest.txt` 对 AI 包那 **52 个 UNRESOLVED guid** 究竟是什么 ②`localization_keys.tsv`（AI 包完全没有，上次"满屏 LC_ 裸 key"缺的就是它）③四个子目录 `Images/Textures/Materials/Shaders` 计数（AI 包只有 `Images 48`、无后三个目录）。⚠️ 路 A **不导字体/TMP FontAsset**，AI 包 `Other/` 那 2 个 `.ttf` 得从 B 包补。

---

#### ✅ C-1 已完成（2026-07-31）· diff 结果＝**AI 漏了 19 个，全是特效链**

四包实测导出（走**断链路线**：拖场景+Unpack+存 `Assets/` 根，因 X3 侧要重写不保 x2 guid）：

| 包 | 资产 | 独有 | `[Prefabs]` | UNRESOLVED | 主件 |
|---|---|---|---|---|---|
| FlashSale | 108 | 59 | 0 ✅ | 0 | 28.3MB `48aa782b…` |
| FlashSaleItem | 43 | 4 | 0 ✅ | 0 | 0.95MB（⚠️导了两遍，44 个 MD5 相同的 `_1` 孪生，落地只取不带 `_1` 的） |
| FlashSalePop | 24 | 19 | 0 ✅ | 0 | 0.56MB |
| FlashSaleRewardGet | 72 | 24 | 0 ✅ | 0 | 2.37MB |
| **合并去重** | **156 条路径 / 160 guid** | — | — | **0**（旧路B包 52） | — |

合并分类：Images 85 / Textures 27 / Materials 24 / Animations 15 / Shaders 5。重叠：106 单包用、9 双包、41 三包共用。

**X3 现状比对**（扫 36109 个 `.meta`）：**108/160 已有同 guid → 零拷贝原地解析；52 个必须拷**。X3 已有件全是 1guid↔1路径无重复；落在 FlashSale 相关目录的 **0**（上次撤除清得干净，无残留要处理）。

**🔴 与上次 AI 落地（撤除 commit `973997a92ce` 删掉的 36 个美术件）逐件 diff ⇒ AI 漏 19 个**：

| 类型 | 漏 | 文件 |
|---|---|---|
| Textures | 9 | `Flare038` `Mask_009.tga` `Mask_347` `Obj_316_Ss` `Spur_115.tga` `Spur_183` `Trail_274` `Ui_096` `naozhong` |
| Materials | 7 | `Flare038_1a_UI` `Obj316_Ss_1a_UI` `Spur_183_a_UI` `Trail_274_1a_UI` `Ui252_1a_UI` `Ui_096_a_UI` `naozhong_Material` |
| Shaders | 1 | `FX_AddBlend_DistortionDissolve_Flow_New_UI.shader`（**三包共用**） |
| Animations | 2 | `BoxReward.anim` `BoxReward2.anim` |

AI 还**多搬 3 个 prefab**（`BtnInfo` / `Fx_Ui_BoxAnim_Rotate` / `Progress`）——断链路线公共件已内联，这 3 个是污染。两边一致 33 个（24 张 png 完全对上）。

**🎯 根因裁定：不是"AI 全自动"的锅，是用错了工具。** 漏的 19 个全在 **Shader→Material→Texture 三层特效链**上，一层不落地整条断 ⇒ 就是 A 类外显翻车（`Content/Grid/Item1/BoxNormal/Card/Icon` 那 3 个组件报错）的物证。路 B 正则扫 `guid:` 看不见 `.mat`/`.shader` 内部引用（产出 52 个 UNRESOLVED 正是这批），Unity `GetDependencies` 看得见。**同一个 AI 拿这次的包去搬，19 个也不会漏。**

**文案**：34 条唯一 key（原始 20+1+8+7=36，`lc_ui_guaranteed` 三包共用），繁中+英文齐全 → `localization_keys_MERGED.tsv`。⚠️ `LC_EVENT_strong_consume_info_desc_2` 在 X2 侧就是**空文案**（FlashSalePop 引用），落 X3 要么删节点要么补新文案。上次没有 tsv ⇒ 满屏 `LC_` 裸 key。

📁 全部产物归档：`KB\换皮档案\X3\2026-07-22_限时抢购_导出包分析\`
（`依赖清单_X3已有vs必须拷.txt` 含 52 件完整清单 · `手动导出vs上次AI落地_漏19个.txt` · `文案34条_中英预览.txt` · `localization_keys_MERGED.tsv` · 四份 `manifest_*.txt`）
源包在 `D:\newX2\{FlashSale,FlashSaleItem,FlashSalePop,FlashSaleRewardGet}\`（⚠️ 不是 `D:\newX2\Copy\`，那个是 7/22 旧路B包 + 一次误导出的混合体）。

---

#### ✅ C-2 落地已完成（2026-07-31）· 52 资产 + 52 `.meta` = 104 文件进 X3

分支 `dev_festival`，X3 的 Unity 当时未开（只有 x2client 开着 + 它的两个 AssetImportWorker）。全部 untracked ⇒ 回退＝`git clean -fd` 那 5 个目录。

| 落点 | 件数 |
|---|---|
| `Res/UI/Prefab/Activity/` | 4 prefab（代码硬编码此路径，**不能改名换目录**） |
| `Res/UI/Sprite/UIActvFlashSale/Images/` | 24 图 |
| `Res/UI/Animation/UIActvFlashSale/` | 7 anim |
| `Res/UI/Materials/UIActvFlashSale/` | 7 材质 |
| `Res/UI/Materials/UIActvFlashSale/Textures/` | 9 贴图 |
| `Res/Shader/Effect/` | 1 shader |

**两个落地决策（为什么这么放）**：

1. **anim 走活动子目录而非平铺** —— X3 已有**两份**同名不同 guid 的 `BoxReward.anim`（`Res/Ani/` + `Res/UI/Animation/`，都 8048B），X2 这份是第三变体（8360B, guid `7bd5df35…`）。平铺会**覆盖 X3 原件**。先例＝X3 自己的 `Res/UI/Animation/HeroClub/`。已核 X3 那两个原件 git 未被改动。
2. **落点不照 X2 路径平移** —— 拿 108 个已有件验过：`Assets/{P2,x2}/Res/…`→`Assets/Res/…` 只 **68%** 命中（7% 仅大小写差、26% 真不同）。暴露两条 X3 约定：① `Effect/Material` 在 X3 叫 **`Materials`**（复数）② 换皮带进来的公共件塞进本活动自己的目录（`UIActvCircusGacha/Images`、`UIActvLaborGacha/{,Textures}`、`UIMonopolyPigBanck/Images` 都这么干）⇒ 照最近先例 `UIActvLaborGacha` 走自包含布局。

**验证**：guid 抽查 5 件（prefab/图/anim/材质/贴图）全 MATCH；无一件覆盖、无一件跳过。⚠️ `copy2` 保留源 mtime，用 `find -newermt` 数会得 0，要用 `git status --porcelain`。

**遗留**：x2client 根目录 4 个中间件（29M+972K+576K+2.4M）待清 —— 用户选 `git clean -f client/Assets/FlashSale*.prefab*`（**别用裸 `git clean -fd`**，会连 `client/Assets/x2/Editor/MigrationConfig/` 一起删，那是 Migration 换件工具的配置不是本次产物）。

🛠️ 上述差集+落地已固化成可复用工具：`KB\方法论\活动程序开发\tools\x2x3_asset_port.py`（`index` 建 guid 索引 → `plan` 预演 → `--go` 执行）。

---

#### ✅ C-3 首次开 Unity 验证（2026-07-31）· 资产层确认干净

**Prefab 模式截图实证：无白图、无紫块、版式成形**（货架/玻璃橱/木架/9 箱/顶部助力进度条+3 宝箱/英雄卡框/货币芯片全部正常渲染）。⇒ **上次 A 类外显翻车的根因裁定成立：是工具选错（路B 扫不见 .mat/.shader 内部引用），不是"AI 全自动"。**

**脚本层：43 个脚本 guid 全部认得，真缺 0 个。**
⚠️ 我先后两次误报（先说 19 缺、再说 4 缺），根因＝算 missing 时**没扫全三处**。正确做法必须扫：
`Assets/`（项目脚本 24 个）+ `Packages/`（内嵌包，UGUI 的 `LayoutElement` 145实例 / `ContentSizeFitter` 115 / `Shadow` 70 / `Mask` 27 都在这）+ `Library/PackageCache/`（注册表包实际落地处，Spine `SkeletonGraphic`、UIEffect `Bevel`+`UIParticleSystem` 在这）。
X2/X3 UI 框架层 guid 几乎全共享：`TFWImage` 1284 实例 / `TFWText` 340 / `ExOutline` 194 / `EventTriggerListener` 175 / `X2BetterOutline` 141 …

**剩余问题（都是预期内、有解的）**：
1. **满屏 `LC_` 裸 key**（截图可见 `LC_IAP_flashsale_my_boost`、`LC_EVENT_flash_sale_main_gacha_button`）→ 34 条文案已备好在 `localization_keys_MERGED.tsv`，需 `LC_*` → `TXT_*` 转换进 X3 i18n
2. **卡片是占位数据**（`222/500`、`9999`、重复英雄头像）→ X2 item 显示组件按序列化默认值画的，走第 4 步 X2 Migration 换 X3 等价件

#### 🔴🔴 C-4 事故与恢复（2026-07-31 17:34）· 换皮无忧删掉 143 个 X3 原生 prefab

**现象**：`Res/UI/Prefab/Activity/` 从 256 文件变 1 个；git 显示 286 个文件被删（143 资产 + `.meta`），含 `ActvRank`/`ActvTaskTemplate`/`ActvWonderTemplate`/`BattlePassRwdItem`/`DailyGift`/`GlassBox`/`HeroChampionList`/`SlgMeteorWar/*` 等**全是 X3 原生件**；根 `FlashSale.prefab` 被 PatchGuid 改写（小了 149KB），另 3 个 prefab 被删。

**根因**：换皮无忧清理面板「资源目录」填成了共享目录 `Res/UI/Prefab/Activity`。它的白名单＝`GetDependencies(根prefab,true)`，不在白名单的一律删；而**断链路线的 prefab `[Prefabs 0]`＝不引用任何 prefab** ⇒ 同目录 143 个 prefab 全成"未引用"。**断链路线 + 共享目录 = 必然全灭。**

**已完整恢复**：`git checkout -- client/Assets/Res/UI/Prefab/Activity`（tracked 全复原，git 现 0 个 D）＋ 4 个 prefab 从导出包重拷（MD5 + guid 全核对一致）。被改写的那份已备份留证。其余 48 件落地资产未受影响。

**铁律（进方法论 §9）**：① 「资源目录」只填**本次搬运专属新目录**，**禁填** `Res/UI/Prefab/Activity`、`Res/UI/Animation`、`Res/Shader/Effect` 等共享目录 ② **必须先点「预览」看 `将删 N`**，N 远大于本次搬入件数＝立刻停手（本次 143 vs 4）③ 走断链路线时该工具对 prefab 目录基本不可用。

---

#### 🔎 C-6 代码侧现状 + 「替换」欠账（2026-08-03 摸清，**续做直接照这段**）

**代码侧：9 个业务 `.cs` 全不在工作区，但可从 `973997a92ce~1` 完整恢复**（proto/bytes/CfgProtos 已被导表重新生成回来了，别重复恢复）。存档分支 `feature/flash-sale` 本地+远端都还在。

| 文件 | 大小 | 处置 | 为什么 |
|---|---|---|---|
| `UIActvFlashSale.cs` | 20.7KB | **恢复** | 手写业务逻辑（货架填充/购买/抽奖/助力/分享），跟 prefab 层级无关，只引用 Auto_ 字段 |
| `UIActvFlashSale.FlashSaleShelfItem.cs` | 13.9KB | **恢复** | 同上 |
| `ActivityMeta.FlashSale.cs`(客户端) | 8.0KB | **恢复** | 数据层 |
| `ActivityFlashSaleCondition.cs` | 4.4KB | **恢复** | 触发条件 |
| 服务端 3 个 | 43.7+3.4+0.7KB | **恢复** | 跟 prefab 完全无关 |
| `Auto_UIActvFlashSale.cs` | 6.2KB | **补完 prefab 后重新生成** | 见下 |
| `Auto_...FlashSaleShelfItem.cs` | 3.9KB | 同上（13 条相对路径全对，大概率原样通过） | |

**🔑 别重写业务代码。** 用户问过「是不是该重写，prefab 不一样问题会不会很多」——**实测答案：不多**。旧 `Auto_` 引用 35 条节点路径，对新落地 prefab（2107 条可寻址路径）逐条验：**18 条直接命中 / 13 条是相对路径（`mGo.FindByFullPath("BoxNormal")` 相对每个 `Item1..Item9` 根，绝对路径比对是误报，实际九个全在）/ 只有 4 条真对不上**。

**4 条真缺全在 `Top/` 组**（`Top/txt_title` `Top/ActvDesc` `Top/Time` `Top/Time/txt`），而 `Top`/`BtnShare`/`btn_info` 这几个节点在 X2 prefab 里**根本不存在** —— 这不是「prefab 不一样」造成的，是**已知欠账**：`Top` 是 X3 风格标题栏，本来就要在 X3 侧补建（对应下方「第 5 步 prefab 侧三个已知欠账」）。

**⛔ 上次真正的坑在这**：旧 `Auto_` 文件自己写着「⚠️ prefab 尚未迁入工程——代码先行：节点路径按 X2 原版 prefab 层级预绑定，缺失节点走框架默认容错，**Editor 导入 prefab 后由 UI 工具重新生成本文件校准**」。**那步从没做过**，加上框架静默容错（`FindByFullPath` 返回 null 不报错、`AddListener` 只打日志）⇒ 绑错了界面就是「某块不动/空白」但零报错。重新生成 Auto_ 就是把这隐患一次消掉。

**「替换」欠账（用户提醒才发现，我按 guid 算差集查不到这类）**：详见方法论 §9「不跑这工具时 REPLACE 那一半的活要自己补」。本案只剩 **2 个动作**：
1. `Common_icon_FrequencyBg1.png` —— 被 `FlashSale.prefab` 引用，X3 已有同内容原件 ⇒ prefab 里 `a38a959435ace524ebdbabbfc9646a86` → `51daedaeb951f344db1e8e6b63523d61`，删我们那份
2. `Mask_009.tga` —— **无人引用**（依赖它的 `Vein005_Mask009_1a_UI.mat` X3 本来就有、指向 X3 自己那份贴图）⇒ 直接删，不用改引用

比录屏里「64 张砍到 21 张」小得多，因为按 guid 算差集时 108 件同 guid 的压根没拷进来。
**🔑 先理解「资源目录」是什么（2026-08-03 用户点破，之前我一直没说透）**：它＝**本次搬进来的新资产的「暂存目录」**。工具敢在里面无差别删，是因为假设「这个目录里的东西都是你刚搬进来的」。所以录屏范式的正确顺序是 **整包倒进一个暂存目录 → 跑工具收敛 → 【然后才】挪到规范目录**。
**⚠️ 我把顺序反了**：算完 guid 差集就直接拷进 5 个规范目录，跳过暂存 ⇒ 没有单一「新资产目录」可填，只能分 3 次跑；且 `Prefab/Activity`、`Shader/Effect` 是共享目录**根本没法交给工具**，那部分（尤其断链自检）只能自己补 —— C-7 那个断链检查就是这么补出来的。

**✅ 裁决：走「分 3 次跑」，不重做落地。** 回到录屏范式的收益只是"流程统一"，但 `FlashSale.prefab` 的加载路径是**代码硬编码** `Assets/Res/UI/Prefab/Activity/FlashSale.prefab`，它无论如何进不了暂存目录 ⇒ 乙路也做不到"一次管全"，重来成本换不回等价好处。
**📌 下次搬新活动时的选择题（开工就要定，别做到一半才发现工具用不上）**：甲=整包倒暂存目录+跑工具（流程统一、工具管全，但会拷进一堆重复件再删）；乙=算 guid 差集直接拷规范目录（拷得少，但工具基本用不上，断链/同名重复两道检查必须自己补）。

**（以下为纠正我此前"别跑换皮无忧"过宽结论后的操作单）该跑，按专属目录跑 3 次。**
危险的只是「资源目录」这个字段填了共享目录，不是工具本身 —— 它的删除范围只限该字段指向的目录内。用户既定流程里有这一环，不该绕开。

| # | 资源目录 | 预期 |
|---|---|---|
| 1 | `Assets/Res/UI/Sprite/UIActvFlashSale/Images` | 收掉 `Common_icon_FrequencyBg1.png` |
| 2 | `Assets/Res/UI/Materials/UIActvFlashSale`（含 Textures 子目录） | 收掉 `Mask_009.tga`（无人引用） |
| 3 | `Assets/Res/UI/Animation/UIActvFlashSale` | 预期 0 改动 |

**每次先点「预览」看 `将删 N`**：N=0~2 正常，N 几十/几百＝目录填错立刻停手。
**⛔ 禁填**：`Res/UI/Prefab/Activity`（共享，143 件事故）· `Res/Shader/Effect`（共享 + 它对 `.shader` 只比文件名不比内容，会把我们的 UI 变体 `TFW/Effect/**UI**/…` 换成 X3 通用版 `TFW/Effect/…`，特效渲染直接变）。
跑完看 `断链 GUID：M` —— **M≠0 是预期的**（独立扫过是 7 个，工具可能报 8 因未排除 Unity 内置），按 C-7 的三类判读，别当点错了。
📋 操作单已进操作台 HTML **第 3.5 节**（含 3 次的复制按钮 + 预览数字记录框 + 跑完自检三条）。

#### 🔴 C-7 断链自检（2026-08-03）· 抓到 1 个真漏搬，并暴露我一个方法论错误

**⚠️ 我犯的错：审计基准取错了。** 我拿「导出包里有什么」当依赖全集（160 个 guid），但导出包**已被路A 分类表过滤**（字体/`.ttf`/TMP FontAsset/**`.asset`** 静默跳过，连 manifest 都不列）。正确基准是「**prefab 引用了什么**」＝ **181 个 guid**（5127 处引用）。**差的 21 个我全程看不见。**

跑断链自检（扫 X3 `Assets`+`Packages`+`Library/PackageCache` 共 48818 个 `.meta`，索引 48614 个 guid）⇒ **断链 7 个**（已排除 Unity 内置 `0000…f000…`）。按三类判读：

| 类别 | 数量 | 明细 | 处置 |
|---|---|---|---|
| **① 真漏搬** | **2** | `naozhong_SkeletonData.asset`（字段 `skeletonDataAsset`，X2 源 `Assets/P2/Res/Spine/SceenPackClock/`）· `LoopHorizontalScrollRectMulti`（脚本，X3 包版本旧无此类，在 `FlashSaleItem.prefab`） | **要补** |
| **② X2 自己就断** | 5 | 全是 `m_Mesh`（粒子网格），全来自 `Fx_Ui_BoxAnim_Rotate.prefab`（开箱旋转特效） | **不管** |
| **③ Unity 内置** | 1 | `0000000000000000f000000000000000` | 白名单 |

**②的判定证据**（两条同时成立才算）：X2 **原版**（未 Unpack）`FlashSale.prefab` 里**没有**这 5 个引用（它们藏在嵌套特效件内部，**Unpack 展平后才浮到表面**）＋ x2client 全仓 **17 万个 `.meta`** 反查一个都解析不到 ⇒ X2 线上就这么跑着，历史遗留。
⚠️ **推论：断链路线（Unpack）会把嵌套件的陈年断链"浮"到表面，断链数虚高，必须按三类判读，别当自己的锅。**

**①里那个 Spine 是这次最有价值的收获**：之前搬了 `naozhong.png` + `naozhong_Material.mat`，**唯独骨骼数据没搬**（路A 跳过 `.asset`）⇒ 闹钟动画不动，但**不报错、不白图**，肉眼验收和看 Console 都发现不了。**Spine 是三件套（`.asset` + atlas + png），路A 只能搬到后两个。**
⏳ 待补：拷 `naozhong_SkeletonData.asset`（+ `.meta`）—— 拷前先查全 Spine 三件套，别又缺一个。

🛠️ 已固化：`x2x3_asset_port.py dangling` 子命令（含内置白名单 + 三类判读提示 + 非零退出码）。这是**收口必跑项**。

#### ✅ C-8 代码侧接入点重写完成（2026-08-03）· 服务端三工程 0 error

**分支**：x3-project `feature/flash-sale-redo`（从 dev_festival 切；⚠️ 本地 dev_festival 落后远端 1 个提交 `1221c214a90` vs `20989955a35`，提 MR 前要先合远端）。

**做法＝业务实现恢复 + 接入点重写**（不是整份恢复）：
- **9 个业务文件 `git checkout 973997a92ce~1 --` 直接拿回**（15 文件含 `.meta`，104KB）：UI 两个 / ActivityMeta.FlashSale 客户端+服务端 / Condition / ServerActivityFlashSaleMeta / Data / 两个 Auto_
- **13 个共享文件逐段重写 +1485 行**（整份覆盖会冲掉三个月里别人的改动：`msgid.def` 被改 24 次、`MetaConst` 8 次）

**🔴 我第一次只恢复了 `D`（删除）条目就去编译 —— 错。** 撤除 commit 还**修改（`M`）了 13 个共享文件、删掉 1856 行**，那些一行都没回来 ⇒ 报 `CS0246 找不到 ActivityFlashSaleData/FlashSaleBuyAck…` + `CS0117 ActivityConst 没有 TRIGGER_TYPE_FLASH_SALE`。**拿清单必须 `git show --name-status` 连 `M` 一起看。**

**号位全部重查并重分配**：

| 项 | 老值 | 新值 | 原因 |
|---|---|---|---|
| `ActivityItem` tag | 70 | **72** | 70 被「与她同游」占、71 被「深海失忆」占（dev/dev_festival 双分支查过） |
| `WriteTag` / `case` | 562 | **578** | 72<<3\|2 |
| `TRIGGER_TYPE_FLASH_SALE` | 84 | 84 沿用 | 仍空闲（81/82/83 已占） |
| 8 个 ErrCode | 1017222–229 | 沿用 | 全空闲，接当前最大 1017221 连号 |
| 10 个 msgid | — | 沿用 | BKDR-131 复算与老值一致、Req→Ack 差 292005 校验通过、零碰撞 |
| `BIActivityType` | 16 | 沿用 | 16 空闲（CircusGacha=15） |

**编译验证**：`dotnet build` 三个 Hotfix 工程 **全 0 error**（过程 24→6→4→0）。⚠️ 客户端 `_CLIENTLOGIC_` 段 dotnet 不碰，**Unity 编译待验**。

**中途踩 3 次，全是「插入作用域」搞错**（已进 [[reference_x3_new_actvtype_playbook]]）：① `RegisterType` 调用和类定义一起插到方法外 → 24 语法错 ② 用 `rindex('\n}\n')` 定位 namespace 闭合 → 实际插到 namespace **外**（`public class ILRuntime_*` 在 namespace 外面！）→ 类进全局命名空间 → 6 错 ③ 脚本 `print('✅…')` 撞 GBK 崩，**第二个文件没写** → 4 错。**正解＝按行定位顶格 `}` + 写完自检「类行号 < namespace闭合行号」+ 脚本 print 纯 ASCII + 先全算完再统一写盘。**

📁 重写脚本存档 `_导出包分析\接入点重写_gen_patch4.py.bak`（含三作用域纪律注释，下次搬同类活动可照抄方法）

**⏭️ 剩余（配置侧完全是空的，这是跑起来的最大缺口）**：
1. **恢复 3 张配置 tsv** —— 可从 gdconfig `46848d4b~1` 完整取回（`ActvFlashSale__ActvFlashSale/Pack/Reward.tsv`，7/14/11 行）；def schema `actvflashsale*_def.py` **还在没被删**
2. **`ActvOnline` 建活动行** —— ⚠️ **撤除前那棵树里也搜不到 flashsale 行，说明这活动从没在 ActvOnline 登记过、上次也没真跑起来**（memory 记的"空货架/IAP 不通"很可能就是这个原因）。要新建，需拍时间/ActvGroup/RuleTips/MailID
3. 导表 → 起 3080 本地服 + GM 开活动
4. 8 个 ErrCode 各配一条 `Text_ErrCode<常量名>` i18n key（手册铁律，否则弹裸 key）
5. Unity 侧：补 `Top` 组 + 重生成 `Auto_` + DK 注册 7 条 + Spine 骨骼（用户定：表现层先放着）

**原续做顺序**：① 恢复 7 个业务 `.cs` → ② 编译（编译器会把 Auto_ 字段不匹配全列出来）→ ③ prefab 里补 `Top` 组 + `BtnShare` → ④ 用 X3 的 UI 工具重生成 Auto_ 校准 → ⑤ 做上面那 2 个替换动作 → ⑥ X2 item 组件换 X3 等价件 → ⑦ 骨架五件套 + DK 注册 7 条 → ⑧ 空文案决策 → ⑨ 两仓提交 + jolt 导表 → ⑩ 验证六道关。

#### ✅ C-5 文案落表（2026-08-03）· 34 条已闭环 33，prefab 零改动

**分支**：gdconfig `feature/flashsale-i18n`（← 最新 `origin/dev_festival`）。
⚠️ 旧的 `feature/flashsale-tables` 是**空壳**：独有 0 提交、落后 dev_festival 259 个、不含撤除提交 —— 在它上面接着干＝自找冲突，故另切新分支，旧分支不动。

**结果**：`tsv/i18n/Text__Text.tsv` **+30 行，纯新增 0 删除**；官方 `i18n_leak_audit.py` 四个 scope 全绿（EN_LEAK/CJK_LEAK/EMPTY/ZH_RAW = 0）。34 条里 3 条 X3 本就有、30 条已录、1 条待定。

**🔑 核心决定：prefab 一个字都不改。** X3 表里本就有 `lc_ui_guaranteed`/`lc_ui_tap_blank_close`/`LC_MENU_congratulations_cap` —— **`lc_*` 命名 X3 接受，不强制 `TXT_*`** ⇒ 沿用 X2 原 key 直接录表，零 guid 风险。备注列照抄 X3 已有迁移标注格式 `限时抢购迁移-X2;X2:<原key>`。

**只翻了 ua 一种 × 30 条** —— X2 那份自带 15 语，正好覆盖 X3 需要的 16 语里的 15 种（X2 多出的 `ar`/`vi` X3 不用）。

**修掉三个问题**：
1. **术语冲突**：`分享至商会` X2 用 Syndicate 系，X3 用 Company 系 → 按 `TXT_ActvOnline_ActvName_103701` 重写 14 语。
2. **X2 自带英文泄漏 2 条**（原样搬会带进来）：`LC_IAP_vip_super_sale` 9 列照抄 `SUPER SALE`；`LC_IAP_flashsale_value_promotion` 的 cn/zh 是英文。⚠️ **官方审计 `--grep flashsale` 抓不到这两条**（key 里没有 flashsale），靠自己按行做同形检查才发现。
3. **我写坏过表头**：用 `utf-8-sig` 写回无 BOM 的表 → 加了 BOM，首行被判改动。已修，现为纯新增。

**⏳ 遗留 1 条待你定**：`LC_EVENT_strong_consume_info_desc_2` —— X2 侧 18 语全空，FlashSalePop 引用。二选一：删 prefab 上那个节点，或补一句新文案。
**⏳ 未提交**：改动还在工作区，没 commit / push。

🛠️ 已固化成工具：`KB\方法论\活动程序开发\tools\x2_i18n_to_x3.py`（`check` 体检 → `land --go` 落表，九个坑内置断言）。知识进 memory `reference_x3_i18n_workflow.md`。

---

**C-2 原计划**（手动在 Editor 里点，实际改为工具落地，接线仍在 Editor 做）：
1. **关掉 Unity** → Explorer 拷：`FlashSale.prefab`(+meta) → `Res/UI/Prefab/Activity/`；专属图(+meta) → `Res/UI/Sprite/UIActvFlashSale/Images/`；`BoxAnim`/`ui21201451BoxMain`/`ui21201451BoxMainDefault`(+meta) → `Res/UI/Animation/`。**图先只拷 `FlashSale_*` 那几张**（X2/X3 同源同 guid，24 图/11 公共件/2 字体原地解析零拷贝），缺的等 Editor 报白图再补——**这正是手动的价值**
2. 开 Unity，双击 prefab 进 Prefab 模式，**先只看不改**：哪些图白、Console 报哪些 missing script
3. 按上面「Auto_ 节点契约」在 Hierarchy 逐条搜，**缺哪个记哪个**
4. **重点**：选中 `Content/Grid/Item1/BoxNormal/Card/Icon` 看 Inspector → 会看到 TFWImage 底图 + **2 个 X2 item 显示组件**（后者按默认数据画 LV99+555 盖住底图）。决定：留着用 `SetItem(itemId)` 驱动，还是用第 4 步工具换成 X3 `UIItemTemplate`
5. 接第 4 步（X2 Migration 换件）→ 第 5 步（Top 组/BtnShare/DK）→ 第 6 步（验证链）

### 第 4 步：两条路都要做的（X2 残留 → X3 等价件）

在 Unity 里对 4 个 prefab 依次跑（`Tools ▸ X2 Migration`，按需，没命中就跳过）：

| 菜单项 | 处理什么 |
|---|---|
| `CommonItemShow → UIItemTemplate 替换` | X2 道具框（bundle 里的 `ItemMid` 就是这类） |
| `AllianceFlag → UIAllianceSymbolUnit 替换` / `Head1 → UIPlayerAvatar 替换` | 联盟旗 / 头像（本案大概率没有，跑一下确认） |
| `清理 LayoutRtlAlignment(RTL) missing 组件` | X3 无此脚本，missing 会**阻断 SavePrefabAsset**，必跑 |
| `Replace X2BetterOutline → BetterOutline` / `X2 红点组件替换工具` | 描边 / 红点 |
| `多语言键回填（按 fileID）` | X2 静态文本是 `LC_` 字面量，本地化脚本被剥后会**满屏裸 key**（上一版验收轮 3 踩过） |

⚠️ 剩下的 missing script **别用 `RemoveMonoBehavioursWithMissingScript` 一把梭**（会误伤"该修不该删"的组件，官方文档有 `ExOutlineMix` 实证反例）。上一版的处置结论可以直接沿用：**X2 特效 Graphic×14 / Spine×1 / LoopScroll×1 剥离、缺源 FX 整块移除、浮雕 missing script×2 照扭蛋机先例保留**。

### 第 5 步：prefab 侧三个已知欠账（上一版没做完，这次要补）

1. **Top 组移植**：从 `UIActvLaborGacha.prefab` 整棵搬 `CentreOther/Animation/Top`（标题+倒计时+描述），X2 自带顶部骨架样式不对不能复用。
2. **`BtnShare` 分享按钮节点**：节点名当初"待定"，代码侧 `Auto_` 已按真节点路径预绑 ⇒ **先读 `Auto_UIActvFlashSale.cs` 里的路径，再按它摆节点**，别自己起名。
3. **A 类卡片外显（上次翻车点之一，修法已定位）**：`BoxNormal/Card/Icon` 上挂了 3 个脚本 = TFWImage 底图 + **2 个 X2 item 显示组件**，后者按自己默认数据画「LV99 头像 + 555」盖住底图。**正确修法 = 调那 2 个 item 组件的 `SetItem(itemId)`，不要去设底图 sprite**（上一版就是设了底图 → 8 格图标全一样 + 555 占位）。
4. **DK 注册没进过远端**（`973997a92ce` 根本没动 DK 表，`dev_festival` 上 `DisplayKey/` 零 FlashSale 命中）⇒ **7 条 DK 要重新注册**，Display_/Path_ 双表同锚平行插，铁律见 [[reference_x3_client_resources]]。⚠️ DK 表是多人共用文件，**按 hunk 提，别整文件提**。

### 第 6 步：验证链（缺一环就别说"好了"）

```
① Unity Editor 编译过（第一关）
② prefab 打得开、图不白、无 missing script 阻断保存
③ 起本地服 + GM 开活动：GMAddServerActivityByCfgId 101029 43200
④ 重登（不点任何按钮）→ 货架就有 8 盒         ← OnPostInit 填充，重连不触发
⑤ 钻石线买一次通（已售罄/余量减/价格对）
⑥ 卡片 8 格图标各不相同、没有 555 占位        ← 第 5 步第 3 条修对了才过
```
配置侧要跑起来还得先恢复三张表：`cd /c/x3/gdconfig && git checkout c43303f7 -- tsv/ActvFlashSale__*.tsv`（schema/def 和 `ACTV_TYPE_FLASH_SALE=84` 都还在，不用重造）。

---

## 🤝 交接文档（⚠️ 2026-07-28 定稿，**已被上面结案状态取代，仅作重做参考**）

**一句话**：核心闭环(钻石线买卖+服务端全逻辑+B设计货架+繁体+配置真值)已落 dev_festival；剩一堆**界面外显bug + 直购IAP端到端 + DK/服务端接线**没解干净(下方A/B/C/D)。

**✅ 已落地 dev_festival(git可查证,丢不了)**
| 内容 | commit/证据 |
|---|---|
| 配置真值(三表+道具映射+IAP档位107/111/116/115+抽奖池+RuleTips40003+i18n cn/en/繁zh) | gdconfig dev_festival `fa46c392`(并集合别人纪念卡/BINGO/签到未冲)+**jolt导表 #2217 SUCCESS**(=无断引用+整库导得出+客户端bytes已生成) |
| 客户端 prefab四件套+图集+动画+UIActvFlashSale代码+proto giftId | x3-project dev_festival `e3122596b9e`(81文件纯FlashSale) |
| 服务端逻辑代码(阶段1a/2/1b) | dev_festival `b39aa389`(早先);gift化回调ActivityMeta.FlashSale.cs在 e3122596 |

**关键坐标**：ActvType**84** / AO**101029** / CID**8202** / Item**1214**券·**1215**皮肤箱 / Pack**211101-08** / 货架**820201-08**(Group82021) / 奖池**820301-05**(Group82022) / 分支**dev_festival** / 配置feature分支**feature/flashsale-tables**(=fa46c392同点)。

**❌ 没解干净的(下方§未完清单A/B/C/D展开)**：A卡片外显(图标全一样+555=item组件306cc8/1da2a9没驱动,正确修=调它SetItem而非设底图) / B直购IAP(gift化接了但端到端没验通;UIBtnPurchase需Editor在每格加`BoxNormal/BtnPurchase`节点,现有黄钮兜底走ReqBuyGift) / C抽奖·助力·场次推送没系统验 / D皮肤1215开出内容待P5·14语翻译·DK注册+ActivityMeta.cs接线(共享文件混别人WIP没提交)。

**在途worktree(活儿在这)**：代码`C:\x3-wt-flashsale`(feature/flash-sale) / 配置`C:\x3\gdconfig-wt-flashsale`(feature/flashsale-tables=fa46c392)。⚠️主仓工作区多人WIP混着(FlashSale+扭蛋机+签到+Arena),碰共享文件(ActivityMeta.cs/DisplayKey/i18n bytes)必按hunk拆或cherry-pick,别整文件提。

**测试环境**：3080服(telnet26080,`python ~/x3_gm.py "!gm @28297 <cmd>"`),活动101029已开;时间要进抢购窗口(游戏日02/10/18点)才可购,`GMSetServerTimeOffset`推;⚠️改客户端代码/bytes后**必须完全关Unity重开**(热塞无效)。

**新增ActvType全套坑**：[[reference_x3_new_actvtype_playbook]](模板commit/msgid BKDR-131/生成C#手写/worktree链接层/热塞bytes无效/繁中zh列/proto改基础程序集必重启/item组件占位/时间窗登录填充+边界推送)。**协作教训**：[[feedback_report_real_state_not_almost_done]]。

---

## 📦 推送状态（2026-07-28 深夜·哪些进了远端）
- **客户端 x3-project dev_festival**：✅ 推了 `e3122596b9e`（81文件·prefab四件套+图集+动画+UIActvFlashSale代码+proto giftId+服务端ActivityMeta.FlashSale.cs回调，纯FlashSale无他人WIP）。⚠️**故意没带**（共享文件混了别人马戏节WIP，整文件提会带别人的活）：`ActivityMeta.cs`登录填充+gift派发接线(混扭蛋机跨天重置)/DisplayKey注册(混签到DK)/ProtoGen配置bytes(jolt管)。→ **dev_festival上FlashSale=方法定义了但没接线的半成品**；早先阶段1a/2/1b骨架(b39aa389)在。
- **配置 gdconfig**：✅**2026-07-28 21:45 真值已合并推上 dev_festival**(`fa46c392`,merge commit 1438288e 并集合 origin/dev_festival + 收尾校正)——真值/IAP档位/抽奖池/RuleTips/i18n/繁体全进,别人的纪念卡/BINGO/签到按并集保留未冲。合并解128冲突(Reward124)由Opus agent做到解完0冲突,末段ExportTable复验用户催急、主Claude接管停agent直接commit收尾+push(**⚠️push时跳过了最终ExportTable exit0确认,靠jolt服务端校验兜底**——jolt若FAILURE说明有断引用需回补)。**✅ jolt 导表 SUCCESS(build #2217)**——全量导表通过=没断引用(替代了push前跳过的ExportTable)+整库(含别人纪念卡/BINGO/签到)都导得出=并集合并没冲坏任何人的行(等于双向丢行审计通过)+robot已重新生成客户端bytes。**配置线闭环。**
- **🔑 128冲突真相(2026-07-28 合并agent分析出,被用户中途停)**:看着吓人实则干净——**我方对Reward表的唯一改动=4个直购包组(211105-108)把占位换真值(万能传奇信物/1214券/1207门票/1215皮肤宝箱)**;dev_festival侧从没碰这4组、只加了759个自己新组(纪念卡1015/1016、BINGO603805/606、签到59815/816等)。**正确合法=保留dev_festival全部759新组 + 只覆盖我这4组,冲不掉别人任何行**。ActvOnline(我方仅加101029一行)/Text(我方仅加FlashSale keys)同理=纯加法。→ 续合时按此:并集,别人的全留,只叠我的FlashSale行。agent在worktree分析未commit未推,feature/flashsale-tables 数据安全。
- **⚠️这台机器工作区多人WIP搅一起**(FlashSale+扭蛋机+签到+Arena)——碰共享文件(ActivityMeta.cs/DisplayKey/i18n bytes)必按hunk拆或开净worktree cherry-pick我的hunk,别整文件提。
- **让FlashSale真正上dev_festival的剩余动作**：①配置128冲突审慎合+push+jolt ②ActivityMeta.cs/DK按hunk拆提(登录填充+gift派发+DK注册) ③A类卡片外显修(见下)。

## 📍 未完清单（2026-07-28 深夜实况·冷启动先看这个）
**✅ 已验通**：钻石线买卖闭环(已售罄+余量减+价格·截图证)/B设计道具直显/繁体/服务端三工程编译0error/服务端全逻辑/配置真值ROI/jolt导表SUCCESS。
**❌ 剩余(按类)**：
- **A·卡片外显(同一根)**：`BoxNormal/Card/Icon` 节点挂了 3 脚本=TFWImage底图(c81b19)+**2个X2 item显示组件(306cc8/1da2a9)**,后者按自己默认数据画"LV99头像+555"盖住底图。我们`SetItemBasicInfo(道具,底图)`只设底图sprite、**没驱动item组件**→8格图标全一样+555占位。**正确修法=调那2个item组件的SetItem(itemId),别设底图sprite**。同根还有:预告期只显"即將開賣"不露价(B设计取舍,用户可能想改露价)。
- **B·直购线IAP**：gift化代码全接了(CreateFlashSaleGifts/FlashSaleOnBuyGift/OnBuyGiftForGiftId注册/proto giftId/客户端兜底黄钮走BuyGiftClick→ReqBuyGift),但**从没端到端买通一次**;直购价$9.99窗口内没验过;UIBtnPurchase漂亮按钮需Editor拼`BoxNormal/BtnPurchase`节点(可选,兜底能用)。
- **C·功能没系统验**：抽奖(达标免费抽)/助力破冰链(只验过分享1次)/场次自动切换推送。
- **D·收尾全没做**：皮肤宝箱1215开出内容待P5/14语翻译(只cn/en/繁zh,余11语待跑x3-translation-automatic)/**最终commit+推dev_festival+归纳三件套(代码在主仓工作区未commit·配置在gdconfig worktree feature/flashsale-tables多commit未push)**。
> ⚠️反复踩的坑(全入 [[reference_x3_new_actvtype_playbook]]):命令行热塞client bytes对运行中Editor无效(必关Editor重开)/繁中读zh列/proto加字段改基础程序集必须完整重启服务端(热更不够,报MissingMethod)/client ProtoGen bytes是git-tracked会被git操作还原。

## 🚀 冷启动接管摘要（新 agent 从这里入）
- **要做什么**：把 X2 的「限时抢购」玩法搬到 X3，作为马戏节第 17 个活动。X3 没有原生限时抢购 → 程序要在 X3 侧实现协议+配置结构（跟扭蛋机 [[project_x2_circus_strong_consume_reskin]] 一个套路：搬运，不是克隆）。
- **骨很重、皮很薄**：玩法一整套（预告/抢购三场 + 全服限量+单人限次 + 分享→助力→抽奖破冰 + 8 礼包/期）是骨；皮只有 **7 张专属图**（背景/banner/盒子开合/宝箱图标）。美术需求原文：「节日主题主要靠背景 banner 凸显，换 banner 即可」。
- **prefab 已自取**：X2 客户端仓在本机 `D:\UGit\x2client`，四件套 prefab 已打包成验收 bundle（见下「产出位置」），**无 assetPath 动态加载盲区**（比扭蛋机干净）。
- **关联案**：马戏节整节日 [[project_x3_circus_festival]]（骨架=深海节，16 活动）＋扭蛋机 [[project_x2_circus_strong_consume_reskin]]（第 16 类新开发）。三案在货币架构/hub 挂载处交圈，别混。

---

## 一、骨（复用 X2 的知识，别重挖）

### 1.1 老案子（数值/玩法真源）
- **策划案 GSheet**：`1ceogA-kuwkT9E6r-6S4Bp1OCy77g5vq7aLgO076ICDY`
  - 页签：`（2）活动规则`（玩法框架）/ `数值设计`（沙滩节两套礼包+分享+抽奖池）/ `（4）V25美术需求`（极简，靠 banner）/ `（3）本地化`（基本空）
- **X2 限时抢购客户端/配置链路权威知识**：memory [[feedback_x2_flashsale_placeholder_data.md]]（组件构成/奖池 fork/累计道具/开箱代码链，**必读**）

### 1.2 玩法框架（老案子 `（2）活动规则`）
- **限时**：预告期（可看礼包、分享，无购买按钮）＋抢购期（每日 3 场，utc2/10/18，兼容时区）。
- **抢购**：①全服限量（活动范围内所有服共用一个抢购数量）②单人限次（每期每礼包限购 1 次；三场算一期）。
- **购买**：货币（游戏代币/礼包支付）或唱片；每期 8 个礼包可全买。
- **社交破冰链**：分享（首次分享奖）→ 被助力（达标得助力奖）→ **抽奖**（被助力次数达标 + 买过礼包 → 抽 8 礼包之一，抽中免费得、限购在抽奖中不生效）。整体：分享→助力达标→差一次购买就能抽→付费破冰。
- **主界面**：标题栏（标题/倒计时/FAQ/剩余抽奖次数/退出）＋抢购倒计时（预告期倒计时到开始 / 抢购期倒计时到结束）＋开启时间点介绍（时间和时长均配置读取）。

### 1.3 客户端 prefab（四件套，X2 仓路径）
`D:\UGit\x2client\client\Assets\x2\Res\UI\Prefab\Activity\`
- `FlashSale.prefab`（主界面，3.6MB）/ `FlashSaleItem.prefab`（货架格）/ `FlashSalePop.prefab`（弹窗）/ `FlashSaleRewardGet.prefab`（领奖）
- 依赖：123 唯一 guid = 71 资源 + 35 脚本(X3重写不管) + ~15 材质/Spine/嵌套prefab。**无 assetPath 字符串动态加载**（不会有扭蛋机 X3NEW-2366 那种 guid 盲区缺图）。

### 1.4 数值基准（老案子 `数值设计`，用作 X3 定数值的起点）
- 两套并存：**钻石版**（价 100钻~99.99，总 ROI **3.13**）＋ **CD/唱片版**（总 ROI **5.80**）。
- 8 档礼包按 R 级：RO(100钻/2000限购) / R1(9.99·19.99) / R2(19.99) / R3(49.99×2) / R4(99.99×3)。
- 分享首次奖（总价值 1.12）＋ 3 档助力奖（第1/2/3次，含高级抽奖券）。
- **抽奖池 6 项**：占星皮肤 11119504（0.02% 保底钩）/ 万能英雄碎片(15.62%) / 高级抽奖券(29.68%) / 成长线自选宝箱(23.43%) / 2h加速(31.25%)。
- **两个机制道具（X2 侧）**：抽奖道具 `11119549`、皮肤随机宝箱 `11119550`（class=flash_sale_reward_box）。X3 侧要新建对应道具。

---

## 二、皮（要换的，很薄）
**7 张限时抢购专属图**（X2 路径 `Assets\x2\Res\UI\TextureNew\`）：
`FlashSale_Bg` / `FlashSale_Bg_Banner` / `FlashSale_Bg_Box1` / `FlashSale_Bg_BoxClose1` / `FlashSale_Bg_Cupboard` / `FlashSale_Light` / `FlashSale_icon_SpecialBox`
→ 马戏节换皮 = 这几张换成马戏主题（背景+banner 为主）。其余全是复用公共 UI 件。

---

## 三、产出位置
- **验收 bundle**（prefab+依赖，和扭蛋机 UIactvfesstrong 同格式）：`D:\newX2\Copy\FlashSale\` + `_manifest.txt`
  - 已含：Prefabs 17（4 主界面 + 13 公共件）/ Images 48（**含 7 张限时抢购专属图**）/ Animations 8 / 字体 2。
  - **⚠️ 缺口（迁移时补，验收不影响）**：~15 个 **材质(m_Material)/Spine(skeletonDataAsset)/嵌套prefab源** 在 `Res/UI` 之外未进本版；另 35 个是脚本 guid（X3 重写不管）。要补时用打包器把 index-root 放宽到 `Assets/x2/Res`+`Assets/P2/Res`（慢，几分钟）或用 ripgrep 定向解析那 15 个 guid。
- **依赖打包工具**（可复用，本轮新建）：`C:\ADHD_agent\skills\unity-prefab-tools\prefab_dependency_bundler.py`（解析 prefab guid→源文件、分类拷贝、写 manifest、脚本 guid 单列不拷；index-root 越窄越快）

---

## 四、⚠️ 已知雷（X2 限时抢购历史，搬运时必对齐）
来源 [[feedback_x2_flashsale_placeholder_data.md]]（X2-43094 血泪）：
1. **抽奖奖池是间接引用**（藏在 `flash_sale_raffle` 组件的 status 里指向奖池表），换皮**必 fork**，否则"开不了箱"（去开旧节日的包，旧节日下线取不到）。
2. **累计道具（抽奖券/HUD 货币）只改组件没用** —— 发累计道具的 **IAP 礼包也得把机制道具一起换**，否则产出端发旧道具、玩家身上没新道具 → "抽不了奖"。
3. **push≠生效**：配置改完必须构建+部署到测试服 + **重开活动**才会重读奖池；占位数据(限购2222/价555)多是"服跑旧配置"。
4. 换皮残留常见：iap 描述残留旧节日名 / recharge_actv 指旧底座 / raffle 复用底座奖池开出旧包。
- **X3 侧对应结构待程序确认**（X2 是 5 个 activity_special 组件 + 货架包 + IAP + 虚拟货 + 抽奖池；X3 的活动/配置模型不同，搬运时要程序定表结构）。

---

## 五、本轮速记（换人必读）
- **ID 段位**：✅ **1214=限时抢购-高级抽奖券 / 1215=限时抢购-皮肤随机宝箱**（2026-07-24 调查定案，origin/dev_festival，紧接马戏节块尾）。纠错：马戏节实占 **1207-1213**（原记 1209-1213 有误；1211 扭蛋币/1212 高级扭蛋券验证属实）；1214+ 全空、feature 分支无预占。
- **ActvType + proto**：✅ **ActvType=84 + proto tag=67**（2026-07-24 定案）。纠错：扭蛋机实为 **83**（非 82，82=BP_FUND）；proto 聚合 message 实为 **`ActivityItem`**（非 ActivityData）。84/67 是 dev+dev_festival 双分支都空的唯一安全组合，登记进 `SingleServerActivityTypes`。
- **目标分支**：✅ **dev_festival**（马戏节全部道具/扭蛋机都在此）；**动仓库前先 `git branch --show-current`**。
- **排期**：7.29 版本；定档后配 TC（当前填 0）。
- **✅ 决策点全部拍板（2026-07-24）**：
  1. **P1 全服限量** → **单服/schema 组各自限量**（不动 CenterServer，成本小；稀缺感=本服内抢）。
  2. **P4 货币映射** → **唱片 CD 并入钻石**（钻石线花钻石、直购线 IAP 真金已定；CD 槽改发钻石，不引入新货币）。
  3. **P5 抽奖大奖外显** → **马戏节主题皮肤当顶奖**（款待美术/皮肤选型，选型前用占位 ID 跑通结构）。
  4. **P6 助力作用域** → **单服内任意玩家助力**（保留拉新、不限工会、不跨服）→ 活动自建破冰状态机的单服简化版（免跨服 RPC），另需新做分享触点。
  5. 另已解：P3 机制道具（1214/1215）/ P2 ActvType+proto（84/67）/ P11 分支（dev_festival）/ P7 数值版本（钻石 ROI2+直购 ROI7）。
- **✅ 数值定稿（走换皮相对守恒法 + task-checker 复查过）**：直购线 4 档=照抄对标 X2 档(购1←#2/购2←#3/购3←#5/购4←#7)投放结构×k(=7÷ROI_x2)，ROI=7 由构造守住，**无需道具绝对钻值、无待拍值**（v1 的"待拍 V/W"是手搓法伪阻塞，已作废）；钻石线 ROI2；抽奖池保 X2 权重换道具+加速下调。task-checker 10/10 0 blocker。产出=数值映射 §3.2/§4 + review HTML。
- **阻塞（仅剩 2 项）**：① 程序落地 X3 侧协议+配置表骨（决策全清、schema+取证备齐在代码方案 §七/§八，可直接开工）② 配置阶段：X2 道具 ID→X3 对应养成线映射（游戏系统沿用/节日品牌"大富翁骰子"换马戏节对应）+流通度审计（数值层已闭合，这是落配置的活）。皮肤具体款待美术选型（不阻塞数值）。
- **下一步可动**：③ 建 Jira 单树（需求/服务器/客户端/配置/测试）把程序活派下去 ④ 客户端 4 件套 prefab 迁 X3（横→竖+DK，可与程序并行）。

---

## 五·五、🔧 开发在途现场（2026-07-24 晚开工,周末用户验收）
- **目标**:客户端+服务端代码全落 **dev_festival**,周末用户验收(用户拍板)。
- **两个 worktree(已全部推送远端;留着以备周末验收修 BUG,验收收口后按 [[reference_x3_worktree_cleanup]] 三步判定删除)**:
  - 代码:`C:\x3-wt-flashsale`(feature/flash-sale ← origin/dev_festival,sparse=server+client/Assets/Scripts+TFWConfig)。⚠️ 主仓 `C:\x3-project` 挂在 zouhanling 分支,别碰别切。
  - 配置:`C:\x3\gdconfig-wt-flashsale`(feature/flashsale-tables ← origin/dev_festival)。⚠️ 主仓 `C:\x3\gdconfig` 在 debtrecycle 分支有在途改动,别碰。
- **阶段流水线**:1a 骨架 ✅(commit `c329167a6ed`,+1567行,三工程 0 error)→ 1b-config 配置表 ✅(gdconfig commit `c43303f7`,ExportTable exit0)→ 2 客户端代码 ✅(commit `abcc2e4fb87`,+992行:UIActvFlashSale 按真 prefab 节点预绑/镜像partial/Condition接回UI;UI注册=Identifier反射自动,无需手补;**Unity 编译未验待周末 Editor;prefab 拼装需用户手操,分享按钮节点名待定**)→ 1b 服务端全逻辑 ✅(commit `b39aa389c36`,27文件+1851:CfgProtos走真TableProtoGen.exe生成/场次模型UTC口径/ServerActivityFlashSaleMeta单服库存(场次×包平铺字典,Mongo)/Buy三闸先库存后扣钻(ResID.GEM,IAP占位TODO)/Assist离线目标支持(PlayerMgr.GetPlayerAsync)+proto新增field10 supporterIds、field11 totalBoughtCount(跨场购买痕迹,抽奖资格用)/Raffle扣券1214+82022池加权/硬约束①②④落码;三工程0error)→ 合入 dev_festival+push+jolt ✅**全部完成(2026-07-24 晚)**:撞号终检零撞(fast-forward)→主Claude亲自重跑 GameServer.Hotfix **0 error**→代码三commit推 dev_festival(远端HEAD=b39aa389c36)→配置推 dev_festival(b1e9e9a1..c43303f7,pre-push 0冲突)→**jolt 导表 SUCCESS(build #2126)**。
- **🔬 验收实况(2026-07-27 晚)**:主仓已切 dev_festival+Unity 编译通过(Condition 进 76 条件类注册)+本地服 3080 已 GM 开活动 ✅(`GMAddServerActivityByCfgId 101029 43200`,activityId=7667178040541052928,uid=28297,入口在马戏节面板组144 可见)。**界面空=prefab 未迁**(Editor 日志实锤:`Empty bundle path for asset: Assets/Res/UI/Prefab/Activity/FlashSale.prefab`,代码写死此加载路径)→ **prefab 全量迁移 ✅ 完成待 Editor 验证(2026-07-27 晚)**:28 新文件+2 DK 表追加(四件套落 `Res/UI/Prefab/Activity/`,24 图入 `Res/UI/Sprite/UIActvFlashSale/`,5 动画,DK 7 条)。**🔑 关键发现:X2/X3 同源同 guid,24图/11公共件/2字体原地解析零拷贝**(断链≈预估一半,方法论已入 [[reference_x3_new_actvtype_playbook]])。剥离清单:X2 特效Graphic×14+Spine×1+LoopScroll×1、缺源FX整块移除、浮雕 missing script×2 照扭蛋机先例保留。**未 commit**——用户 Editor 验过(界面能开+钻石线能买)后由主 Claude 提交推 dev_festival。已知待补:Top组移植/BtnShare 新摆/光效美术补。
- **验收轮2(2026-07-27 深夜)**:界面已渲染 ✅;缺道具ICON/i18n=**阶段2 留的 TODO(阶段3)**(当时 CfgProtos 未生成,货架只做了三态+余量,Card/Icon/价格/限购文案明确延后)→ **补丁 ✅ 完成(2026-07-28 凌晨)**:①货架渲染补齐(icon=UIHelper.GetPackFirstAndEndowItems+SetItemBasicInfo 照扭蛋机/周特惠惯例;钻石价千分位;直购档 Price=0→「敬请期待」占位;限购文案)②i18n 12 key 录入(worktree commit `525f7576`,cn/en,含 TXT_Pack_Name_211101-08 自动 key 惯例;14 语待翻)③cn/en.bytes+manifest 部署主仓 client+ReloadGameServer 19 tables。**待用户重启 Play 复验**。⚠️环境发现:本地服 3080 起自 piggybank worktree(配置软链指那边,服务端侧 i18n 未拷——无影响,服务端零 TXT 引用),已入 telnet 工作流 memory。教训已入手册:收口前 `grep "TODO(阶段"` 销账 + UI 渲染 API 惯例清单。
- **验收轮3(2026-07-28 凌晨,用户截图)**:界面结构/背景/柜子已对,但 ①**空货架**——根因=RefreshWindow(填packs)只在4个handler里调,登录从不填充→初始空壳直接下发(已入手册:下发数据必须在OnPostInit填充) ②**满屏LC_裸key**——X2 prefab静态文本=LC_ key字面量,本地化脚本被剥后裸露(已入手册)→**修复agent在途**:服务端OnPostInit补填充/客户端接管助力排+黄按钮改分享入口+隐藏0/10000大奖进度(X2特有)+底部大按钮接抽奖/i18n增量/**本地服停→主仓重编重启**(治软链指piggybank的别扭,活动Mongo持久化不丢)。
- **验收轮3中程(2026-07-28 上午,第二张截图)**:①LC_清理✅生效(裸key消失/大奖进度已隐藏) ②**抽奖按钮端到端已通**(点击→服务端正确返 RaffleNoCount 无券错误)——但暴露**新错误码没录 Text_ErrCode* 文案 key**(裸key上屏,惯例已入手册,8个key待最后一批i18n补) ③空货架=用户测在重启窗口期,新服(含FlashSaleMeta)已在主仓起,**待完全就绪后用户完整重登**(OnPostInit 才填货架,重连不触发)。尾巴清单:8个ErrCode文案key+分享按钮文字+14语翻译。
- **✅ 主链路验收通过+正式配置真值落地(2026-07-28 中午)**:货架/倒计时/分享/抽奖全渲染(最后一层窗户纸=用户 Play 会话跑旧编译,重编+重启 Play 即见;埋的 [FlashSaleDebug] 探针两处**收口前删**)。正式配置 commit `e2d2fe8c`(worktree,映射全程在 commit body):**X2→X3 道具映射定案**=万能碎片→万能传奇信物52003(代币交叉换算1万钻)/粉尘→祝福银币5003/骰子→马戏团门票1207/军备图纸→射手武装碎片55007(审计最弱4组)/金羽→高级木板55100/精炼石→神秘金属55101/经验→冒险阅历1008(补盘法);IAP 档位 107/111/116/115;RuleTips 新建 40003;抽奖池 52003+臻选宝箱230005。**⚠️购3 精炼石槽 agent 自查出 ROI16 超标(锚不自洽,教训已入换皮数值 memory ⑦),已令改×55 守回 ROI7,修正部署中**。剩 TODO:1215 开出内容待 P5/14语翻译。**购3 修正定案(commit `806d9640`)**:神秘金属×55,整包 ROI7 闭合;**1215 皮肤宝箱隐含预算 ≈500钻/箱($1/箱)**——P5 选型配开出内容时按此价值靶,别破盘。
- **📋 策划案终审(2026-07-28 下午,用户提供老案子GSheet逐条对照)——5项偏差全裁决,现状全部成立**:①全服限量=**单场刷新**(策划拍板压过文档"整活动一池")②个人限购=**每场1次**(压过文档"每期不重置")③抽奖=**保留5道具池**(压过文档+X2实装的"抽8礼包")④虚拟抢购库存(假消耗曲线)=**不做**,数值控制即可 ⑤点击语义=购买挪黄钮/格子点击置空留预览TODO。评审确认已对齐项:三场UTC 2/10/18/8包全可买/破冰链/抽中免费/结束清券(c44回收)/倒计时两态/时间配置化。**结论:设计评审归零,无新增改动项**(仅点击语义并入在途修复)。
- **🎯 设计变更 B(2026-07-28 晚,策划拍板·主Claude直接实现)**:预告期(不可购态)**不再关箱**,改为**每格直显主打道具卡(icon+价值)**,购买钮转「即将开卖」态不可点(开卖时间点看顶部大倒计时)。改动=`UIActvFlashSale.FlashSaleShelfItem.cs` RefreshPack/RefreshCard 三态逻辑(BoxClose 废弃/BoxNormal 恒显道具卡/售罄切 BoxSoldOut)+ OnBtnBuyTrigger 加 mInWindow 闸 + 新 i18n key `TXT_ActvFlashSale_ComingToSell`(即将开卖/即將開賣)。**已删全部诊断探针**。⚠️反复踩坑根因终定位:**命令行热塞 client bytes 对运行中 Editor 无效**(读的是导入过的 TextAsset)+ **繁中客户端读 zh 列只填 cn 全空白**——两条已入 [[reference_x3_new_actvtype_playbook]]。验收动作=**完全关 Editor 重开**(强制重编+重导)。
- **验收轮4(2026-07-28 下午)**:用户报两问题:①"要点按钮道具才出来"=**场次边界服务端不主动推**(X2每场重发语义没搬全,规律入手册"登录填一次+边界推一次缺一不可")→修复agent在途(服务端定时推+客户端onEnd兜底);②"道具数据错"=疑似客户端bytes未reimport(外部拷入bytes需退Play→回Unity import→重Play),用户复验中,若reimport后仍错则逐包对配置。诊断技=时间线对齐(探针14:08 winIdx=-1在跳变前/14:10 ShareAck已winIdx=1)。
- **验收轮3收口中(2026-07-28 上午续)**:用户重登仍空货架→主Claude下场查日志**实锤服务端数据管线全通**(用户点分享→ShareAck返完整8包+余量+shareDone=true+分享奖已发)——服务端不用再动;剩两个精确点已喂给修复agent:①登录填充钩子没真落(ActivityMeta.cs L2922那个FLASH_SALE case是OnRemove不是登录钩子——⚠️grep命中≠钩子在对的语义位置,验代码改动要读上下文)②客户端Ack→UI刷新链路断(数据到了界面不刷)。完成标准=重登不点任何按钮货架即有8盒(预告期BoxClose态)。
- **📋 周末验收指引**:①`C:\x3-project` `git checkout dev_festival && git pull` 看全部代码(主仓当前在 zouhanling 分支,记得先处理其未跟踪文件)②开 Unity Editor 编译客户端(阶段2代码 Editor 未验,这是第一关)③prefab 拼装:搬运包 `D:\newX2\Copy\FlashSale\`,Auto_ 绑定已按真节点路径预绑,分享按钮节点名待定、Countdown Text1/Text2 语义可能要对调、Top组需照扭蛋机从 UIActvLaborGacha 移植 ④测试环境起服开活动(AO=101029,TC=0 需 iGame 手开)走 test-env-prepare-x3 ⑤验钻石线购买/分享/助力/抽奖全链;直购线 IAP 是占位(Price=0 返错)不用验。
- **落地手册**:新增 ActvType 全套硬知识(模板 commit b68261ca+3ca446c/msgid BKDR-131/生成C#手写惯例/sparse worktree 姿势)已沉淀 [[reference_x3_new_actvtype_playbook]],接手先读它。
- **配置线 ✅ 完成并验证**(commit `c43303f7` @ feature/flashsale-tables,ExportTable exit0)。**号段分配(权威,别重配)**:ContentID=**8202** / AO=**101029**(101028 被酒馆占) / Pack 211101-04 钻石线·211105-08 直购线 / Reward 组:分享 8202101·助力 8202102-04·包内容同 PackID / Reward seq 15930189-216 / 货架行 820201-08(Group=82021) / 奖池行 820301-05(Group=82022 本节日独立组)。三表 schema 见 commit。
- **配置真值口径**:钻石线 4 包=数值定稿真值可跑通;**占位 TODO 7 项**:①直购线 820205-08 iap Price=0+内容克隆钻石档(待守恒映射替换)②1215 开出内容待 P5(现建为普通道具,选型后改 UseEffect=2+drop)③奖池占位 7001/70003(待万能碎片/成长自选映射)④i18n 仅 cn/en 6 键,14 语+Pack 名 TXT 待跑 x3-translation-automatic ⑤ActvRule 需新建独立 RuleTips(禁共用扭蛋机 40002)⑥DK 借图(1214 借 1212 券图/1215 借 Monopoly 宝箱/AO 借扭蛋机),待专属美术 ⑦TC=0 排期后配+累充白名单待付费包定稿。c33 已按 must-check 清残留(TopResource=1214|1215)。
- **验收指引(给周末的用户)**:`C:\x3-project` `git checkout dev_festival` 拉取即见全部代码;配置在 gdconfig dev_festival;想看真 UI 需拼 prefab(搬运包在 `D:\newX2\Copy\FlashSale\`)。

## 六、待办流程（阶段）
1. ✅ 建档案（本文）＋ prefab bundle 验收
2. 🟡 交程序：玩法规格 + prefab bundle + 数值基准，程序在 X3 实现协议+配置结构 ← **当前在这**。**代码方案 v1 已出** → `KB\产出-数值设计\X3_限时抢购\X3_限时抢购_代码方案.md`：选型=**新 ActvType + 可选 customParam 叠加**（同扭蛋机，非竞猜那种纯 customParam）；**头号硬点=P1「全服限量」是否真跨服共享库存**（决定要不要动 CenterServer，成本量级差别大）；共 11 个待拍板见方案 §7。
3. 🟡 数值定稿（X3 口径，走 x3-numerical-design + task-checker）**数值映射 v1 已出** → `KB\产出-数值设计\X3_限时抢购\X3_限时抢购_数值映射.md`：用户拍定钻石线 ROI2(引流)+直购线 ROI7(冲付费)；钻石线 4 档 ROI 精确=2.0 核验通过，直购线 4 档盘子按 ROI7 锁死、养成大头待策划要价（相对守恒不猜价值）。剩：6 类道具待策划要价 + P3 机制道具定名/P5 抽奖大奖/P4 唱片货币映射/抽奖池 6vs5 项口径 待拍板；未过流通度审计+未跑 task-checker 复查。
4. ✅ 新道具定名 + ID 定案（**1214 高级抽奖券 / 1215 皮肤随机宝箱**，dev_festival；建 Item 落地待配置阶段）
5. ⬜ 配置落地（活动主体/组件/货架包/IAP/奖池 fork/累计道具同步换）→ 分批推 dev_festival + jolt_verify
6. ⬜ 客户端 prefab 迁入 X3（横屏→竖屏改造 + DK 注册 + 缺图排查）
7. ⬜ 文案 i18n（多语翻译）
8. ⬜ 美术（7 张专属图换马戏主题，banner 为主）
9. ⬜ Jira 建单树（需求/服务器/客户端/配置/测试）
10. ⬜ 测试（抽奖/开箱触发链 + 奖池 fork 验证 + 端到端）
11. ⬜ 收口接管化（决策记录/换皮清单/目录说明三件套挂本档案）

---

## 变更记录
- 2026-07-24（下午·续2）**数值改走换皮相对守恒法并过 task-checker**（用户点出应走数值换皮 skill 纪律）：作废 v1 手搓凑盘子法（连带作废伪阻塞"待拍 V/W"），直购线改为照抄对标 X2 档投放结构×k(=7÷ROI_x2)、ROI7 由构造守住无需绝对钻值；抽奖池保 X2 权重换道具+加速下调；P8 限购按场。task-checker type=numerical 10/10 0 blocker，修 2 处文档一致性瑕疵。X2 养成线手册单价降为旁证（守恒法本身不需要）。
- 2026-07-24（下午·续）数值框架 v1（后被 v2 守恒法替代）：养成道具单价对标 X2 养成线手册；P8 限购按「场」；抽奖池加速下调、按 5 项。
- 2026-07-24（下午）用户拍板 4 项收口：P1=单服限量 / P4=CD 并入钻石 / P5=马戏节主题皮肤顶奖(待选型) / P6=单服内任意玩家助力(自建破冰单服简化版)。**至此全部决策点清零**，代码方案 §七全绿、数值映射待拍板清零。剩阻塞仅：程序落骨 + 直购线养成待策划要价。已同步回写代码方案+数值映射+本档案。
- 2026-07-24（上午）三路代码/配置调查落定程序侧待拍板：**P2 ActvType=84+proto tag=67**（纠错扭蛋机 83/message 名 ActivityItem）、**P3 机制道具 1214/1215**（纠错马戏节实占 1207-1213）、**P11 目标分支 dev_festival**；**P6 助力系统调查完成**（Union Help 工会助力+ThumbsUp 拜访两套现成，无分享得奖，待拍作用域口径）；**P1 强旁证做单服限量**。取证全在代码方案 §八。
- 2026-07-22 建档：读老案子表 + 打包 prefab bundle + 定性 A 方向搬运；ID/分支/货币映射待核。
- 2026-07-22 出代码方案 v1（`KB\产出-数值设计\X3_限时抢购\X3_限时抢购_代码方案.md`）：定选型（新 ActvType+可选 customParam 叠加）、协议+配置表 schema 草案、4 件套迁移点、复用扭蛋机 vs 独有、4 条雷内联硬约束、11 待拍板（新增头号硬点 P1=全服限量是否真跨服共享库存→CenterServer）。数值定稿待用户定货币映射+数值版本。
