# X2 搬来的按钮换成 X3 标准件 —— 操作说明书

> 场景：X2→X3 搬运的界面，功能跑通了但按钮还是 X2 美术。
> 本文以**马戏节限时抢购（FlashSale）九格货架的三个购买钮**为实例，流程通用。
> 编制：2026-08-04，路径与节点名均已对真 prefab 解析确认（非猜测）。

---

## 0. 先明确：换的是"皮"，不是"骨"

| | 换件影响 |
|---|---|
| 购买逻辑 / 三态切换 / 限购 / 记账 | ❌ 完全不受影响（都在代码里，已实机验证通过）|
| 按钮**外观** | ✅ 这才是要换的 |
| `Auto_` 里的**绑定路径** | ⚠️ 会变（新件的子节点名不同）—— 换完必须改 + 重跑断言 |

**换件前的功能状态必须是"已验证通过"**，否则换完出问题分不清是换件引入的还是原本就有的。

---

## 1. X3 标准按钮件库

位置：`client/Assets/Res/UI/Prefab/Common/Button/`

| 件 | 内部子节点 | 适用 |
|---|---|---|
| `button_Payment` | `txt_price` · `txt_get` | **IAP 内购钮**（真金价格 + "获得"）|
| `button_PaymentClaim` | — | 内购已购 / 待领取态 |
| `ButtonYellowBig` / `ButtonOrangeBig` / `ButtonOrangeMid` / `ButtonGreenMid` / `ButtonGreenSmall` / `ButtonBlueBig` / `ButtonBlueMid` / `ButtonBlueSmall` / `ButtonRedMid` / `ButtonGoldSmall` / `ButtonSmall` | `icon` · `text` · `Text` · `icon_text` | 软货币 / 通用钮（`icon`=货币图标，`icon_text`=数值）|
| `Button_Gray` / `ButtonGray2` | `text` · `Text` · `icon` · `icon_text` | 置灰 / 不可点态 |
| `Common_Btn_3_green` / `Button3` / `Button4` | — | 其它通用样式 |

> ⚠️ **X2 Migration 菜单里没有"按钮替换"工具**。`Tools ▸ X2 Migration` 只有 5 项：
> `AllianceFlag → UIAllianceSymbolUnit` / `CommonItemShow → UIItemTemplate` / `Head1 → UIPlayerAvatar`
> / 多语言键回填 / 清理 RTL missing 组件。**按钮只能手换。**

---

## 2. 本例的对应关系（限时抢购三个购买钮）

| 现有（X2） | 换成 | 绑定路径变化 |
|---|---|---|
| `BoxNormal/BtnBuy`（IAP 直购）| `button_Payment` | ✅ **不用改** —— 子节点名 `txt_price`/`txt_get` 与现绑定完全一致 |
| `BoxNormal/Price`（钻石档）| `ButtonYellowBig` | ⚠️ `Price/Layout/txt` → `Price/icon_text` |
| `BoxNormal/BtnBuyCd`（不可购/售罄）| `Button_Gray` | ⚠️ `BtnBuyCd/Layout/txt` → `BtnBuyCd/text` |

**为什么 `BtnBuy` 不用改**：X2 那个内购钮的子节点命名恰好和 X3 `button_Payment` 一致（都是
`txt_price`/`txt_get`），大概率是同源。这属于运气，**别假设其它界面也这样**，一律按第 4 步实测。

---

## 3. Unity 里怎么换（逐步）

以 `Item1/BoxNormal/Price` 换成 `ButtonYellowBig` 为例：

1. **先备份**：`git status` 确认 prefab 当前状态干净或已提交；`FlashSale.prefab` 若未跟踪，先手动复制一份到别处。
2. Project 窗口定位 `Assets/Res/UI/Prefab/Common/Button/ButtonYellowBig.prefab`。
3. 打开 `FlashSale.prefab`（**Prefab 编辑模式**，不要在场景里改实例）。
4. Hierarchy 里选中 `Content/Grid/Item1/BoxNormal/Price`，**记下它的 RectTransform 数值**
   （anchoredPosition / sizeDelta / anchorMin / anchorMax / pivot / scale）—— 换件后要抄回去，
   否则位置会跳。
5. 把 `ButtonYellowBig` 拖进 `BoxNormal` 下，成为 `Price` 的兄弟节点。
6. 把新件的 RectTransform 按第 4 步的数值填回。
7. **把新件重命名为 `Price`**（原 `Price` 先改名 `Price_old`）——
   🔑 **保持节点名不变是关键**：这样 `Auto_` 里 `BoxNormal/Price` 这一层路径不用动，
   只有内部子节点路径要改（见第 2 步表格）。
8. 确认新件显示正常后，**删除 `Price_old`**。
9. 保存 prefab。

**其余两个钮同法。** `BtnBuy` → `button_Payment` 时同样重命名为 `BtnBuy`。

---

## 4. 换完改代码（精确位置）

### 4.1 改 `Auto_UIActvFlashSale.FlashSaleShelfItem.cs`

```csharp
//  ① 钻石档：换 ButtonYellowBig 后 Layout/txt 变成 icon_text
mGoBuyDiamond = mGo.FindByFullPath("BoxNormal/Price");
mTFWTextBuyDiamondPrice = mGo.GetComponent<TFWText>("BoxNormal/Price/icon_text");   // ← 原 Price/Layout/txt

//  ② IAP 直购：button_Payment 子节点名与原一致，两行不动
mGoBuyIap = mGo.FindByFullPath("BoxNormal/BtnBuy");
mTFWTextBuyIapPrice = mGo.GetComponent<TFWText>("BoxNormal/BtnBuy/txt_price");
mTFWTextBuyIapGet = mGo.GetComponent<TFWText>("BoxNormal/BtnBuy/txt_get");

//  ③ 不可购/售罄：换 Button_Gray 后 Layout/txt 变成 text
mGoBuyCd = mGo.FindByFullPath("BoxNormal/BtnBuyCd");
mTFWTextBuyCd = mGo.GetComponent<TFWText>("BoxNormal/BtnBuyCd/text");               // ← 原 BtnBuyCd/Layout/txt
```

三个 `UIHelper.AddListener(..., "BoxNormal/Price"/"BoxNormal/BtnBuy"/"BoxNormal/BtnBuyCd", ...)`
**都不用动**（节点名保持了）。

### 4.2 可以顺手简化的一处

换成 `Button_Gray` 后，`UIActvFlashSale.FlashSaleShelfItem.cs` 里的
`UIHelper.SetGray(mGoBuyCd, soldOut)` **仍然保留**——因为 `BtnBuyCd` 同时承担
「预告期·即将开卖（正常色）」和「已售罄（灰）」两态，件本身是灰的话预告期就分不出来了。
若决定拆成两个件（预告期用 `ButtonOrangeMid`、售罄用 `Button_Gray`），
则要在 `RefreshBuyButtons` 里多一层显隐，不建议——两态一个件 + SetGray 更省。

---

## 5. 验证（三步，一步不能省）

```bash
# ① 路径断言：把 Auto_ 里每条路径对真 prefab 逐条核，缺一条当编译错处理
python "C:\ADHD_agent\KB\方法论\活动程序开发\tools\x2x3_asset_port.py" paths \
  --prefab "C:\x3-project\client\Assets\Res\UI\Prefab\Activity\FlashSale.prefab" \
  --rel-base Content/Grid/Item1 \
  --from-cs "C:\x3-project\client\Assets\Scripts\UI\Gen\Auto_UIActvFlashSale.FlashSaleShelfItem.cs"
# 期望输出末行: 命中 N / 缺 0  [OK] 全部可绑

# ② 编译（不用等人点 Unity）
python "C:\x3-project\.claude\skills\DebugUtils\scripts\client.py" recompile --timeout 300
# 期望: "hasErrors": false

# ③ 实机：进 Play → 自动登录 → 开界面 → 看三态
python "C:\x3-project\.claude\skills\DebugUtils\scripts\client.py" invoke \
  --type "UnityEditor.EditorApplication, UnityEditor" --member EnterPlaymode --kind call
# 登录后
python "C:\x3-project\.claude\skills\DebugUtils\scripts\client.py" invoke \
  --type "UI.UIHelper" --member OpenActivityPanel --kind call --args <activityId> false
```

**为什么 ① 必须跑**：框架对路径错**静默容错**——`FindByFullPath` 返回 null 不报错、
`SetActive(null)` 静默 no-op、`AddListener` 只打日志。表现是"界面某块空白 + Console 干净"，
肉眼查不出。限时抢购第一版界面大面积不工作，根因就是 `Auto_` 照 X2 层级猜写、从未对真 prefab 校准。

⚠️ 另外：**`grep m_Name` 证不了父子关系**（只能证明"这个名字存在于文件某处"）。
要看真实层级用 `prefab_tree.py`（本目录 tools 下），或运行时遍历 `go.transform`。
嵌套 PrefabInstance 的内部节点**不在父 prefab 文件里**，只能运行时看。

---

## 6. 九格 = 27 处，怎么办

`FlashSale.prefab` 的 `Content/Grid/Item1..Item9` 是**复制出来的静态节点**
（实测 `FlashSale.prefab` 引用 `FlashSaleItem.prefab` **0 次**），所以没有"改一处传九格"。

| 方案 | 做法 | 成本 | 风险 |
|---|---|---|---|
| **① 复制 BoxNormal（推荐）** | Item1 换好三个钮 → 复制整个 `Item1/BoxNormal` → 替换其余 8 格的 `BoxNormal` | 8 次复制粘贴 | 低。九格节点集实测完全一致（`BoxNormal`/`BoxSoldOut`/`BoxClose`/`Limit`/`Remain`），复制不会丢差异 |
| ② 做成模板 prefab | 把货架格抽成独立 prefab，Grid 下放 9 个实例 | 中，且**代码要改**：从「包装静态节点」改成「克隆模板」（`UIWidgetList.Add(go)` → 模板克隆）| 中。结构更干净、以后改一处传九格，但动了已验证通过的代码路径 |
| ③ 不换 | 保持 X2 美术 | 0 | 只影响观感 |

**推荐 ①**：一次性成本最低、完全不动代码结构。
方案 ② 值得做的时机是"以后还要频繁改格子结构"，现在为了换皮去动它不划算。

⚠️ 方案 ① 复制后**仍要跑第 5 步的路径断言**，而且要
`--rel-base Content/Grid/Item5`、`Item9` 各抽验一次（确认九格一致，别只验 Item1）。

---

## 7. 顺带：prefab 里该删的死节点

限时抢购已砍分享/助力，以下节点**代码不再绑定**，留着只会让 X2 裸 key 上屏
（`LC_IAP_flashsale_my_boost` 之类，非 `TXT_` 前缀的缺失 key 会**原样显示 key 名**）：

- `ChestProgress/*` —— 助力进度条 + 三档宝箱 + 分享钮
- `SpecialBoxBtn/*` —— 累计大奖（服务端无对应 handler，X3 未实现）

🔴 **`Btn`（幸运抽奖入口）必须留着** —— 抽奖 2026-08-04 已接入并实机验证通过
（`Btn/{icon_text=券数, Text=文案, Red=红点}`）。`BoxAnim2`（抽奖开箱表现）也建议留，阶段3 要用。
