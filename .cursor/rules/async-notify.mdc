---
description: 飞书异步通知 — 所有多步骤任务完成时必须推送通知
globs: 
alwaysApply: true
---

# 飞书任务完成通知规则（最高优先级，强制执行）

## ❗ 核心规则：每次完成任务，最后一步必须发飞书通知

**只要你完成了任何涉及工具调用的任务（超过 2 个工具调用），任务结束时的最后一个动作必须是运行下面的通知脚本。无一例外。**

不要判断"用不用发"。不要评估"用户是否在线"。任务完成 → 发通知。就这一条规则。

---

## 完成通知命令（每次任务结束时运行）

```powershell
python "C:\ADHD_agent\.cursor\skills\async-notify\scripts\notify_done.py" --task "用一句话描述刚完成的任务" --result "结果摘要（成功/失败/关键数据）" --agent "Cursor"
```

**`--task`**：用一句话写清楚做了什么，例如：
- "P2导表-复活节活动上传"
- "config-reskin 换皮配置生成"
- "分析排期数据并生成报告"

**`--result`**：结果关键信息，例如：
- "成功上传 5 张表到 feature/easter-2026"
- "生成了 3 行配置，已输出到对话"
- "失败：找不到 ID 21001234"

---

## 需要用户决策时：阻塞等待飞书回复

当你遇到需要用户拍板才能继续的情况（不可逆操作、影响范围超预期、多个方案不确定选哪个），**不要在对话框等用户，改用飞书确认**：

```powershell
python "C:\ADHD_agent\.cursor\skills\async-notify\scripts\notify_confirm.py" --task "任务名" --question "需要用户决策的具体问题" --agent "Cursor" --timeout 600
```

脚本返回 `{"decision": "yes"/"no"/"timeout"}` 后再继续。

---

## 唯一例外（不发通知）

- 用户明确说了"不用通知我"或"不用发飞书"
- 单条简单问答（0 个工具调用）

其他所有情况：**发。**
