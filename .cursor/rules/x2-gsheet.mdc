---
description: X2导表、X2配置表同步、X2下载表、X2刷表、X2合并分支、X2分支差异
globs: 
alwaysApply: false
---

# X2 配置表同步规则

当用户提到以下关键词时，自动使用 GSheet 配置表同步 Skill：

- X2导表
- X2下载表
- X2刷表
- X2拉取配置
- X2合并
- X2同步分支
- X2冲突

当用户提到以下关键词时，使用**分支差异汇总 Skill**：

- 分支差异
- 差异汇总
- 对比分支
- diff到表格
- 差异GSheet
- 查看分支改动
- 分支对比

## 分支别名速查

| 别名 | 实际分支名 | 说明 |
|------|-----------|------|
| 掉落转付费 | `dev_X2-39183` | 掉落转付费功能分支 |

## 执行步骤

### 配置表同步（下载/合并/冲突）

1. **切换工作目录**到 `D:\UGit\x2gdconf`
2. **读取主 Skill 文件**：`D:\UGit\x2gdconf\.cursor\skills\gsheet_sync\SKILL.md`
3. **根据用户意图读取对应分 Skill**：
   - 下载/导表/刷表 → `c:\ADHD_agent\.cursor\skills\x2-config-download\SKILL.md`（ADHD仓库维护）
   - 合并/merge → `D:\UGit\x2gdconf\scripts\gsheet_skill_merge.md`
   - 冲突处理 → `D:\UGit\x2gdconf\scripts\gsheet_skill_conflict.md`
   - 参考信息 → `D:\UGit\x2gdconf\scripts\gsheet_skill_reference.md`
4. **按 Skill 规则执行操作**

### QA GSheet 写回强规则

当用户要求修改 X2 配置，且出现以下任一场景时，**不能只改本地 TSV / git 分支**，必须同步写回对应 QA Google Sheet：

- 用户提到 `QA`、`qa页签`、`QA表`、`测试服`、`审核服`、`master_bugfix`、`hotfix`
- 用户给出表号（如 `2011`、`2013`、`2135`）并要求“改配置”“修配置”“传一下”“同步一下”
- 配置用于 QA 验证、上线前验证、bugfix 验证
- 已经修改了 `D:\UGit\x2gdconf\fo\config\*.tsv` 中的配置行，且这些行来自 X2 GSheet 配置表

必须执行的检查/写入流程：

1. 用 `c:\ADHD_agent\.cursor\skills\google-workspace-cli\gsheet_query.py resolve <表号>` 确认该表独立 QA spreadsheetId 和页签名。不要误用总索引表 `1z2-AK4d...` 当作业务表。
2. 写入前用 `gsheet_query.py idrange <表号> <起始ID> <结束ID>` 或 `gsheet_query.py row <表号> <ID>` 确认 QA 页签现值和行号。
3. 用 Google Sheets API / `gws sheets spreadsheets values update|batchUpdate` 写回 QA 页签的目标单元格。对 A 列为空的表，必须显式指定范围（例如 `I4940:J4940`、`M4940:M4940`、`S4940:S4940`），避免 append 导致列右移。
4. 写入后必须再次运行 `gsheet_query.py idrange` / `row` 复查目标 ID 的实际值，并在回复里说明“已写 QA GSheet 并复查”。只说“已提交/已 push”不算完成。

典型例子：修改 `2011(iap_config)` 和 `2013(iap_template)` 的礼包配置时，除了提交 `fo/config/iap_config.tsv` / `fo/config/iap_template.tsv`，还必须写入 `iap_config_x2qa` / `iap_template_x2（qa）`，并复查目标 ID 范围。

### 分支差异汇总到 Google Sheet

1. **读取 Skill 文件**：`c:\ADHD_agent\.cursor\skills\branch-diff-gsheet\SKILL.md`
2. 确认源分支和目标分支（可通过上方"分支别名速查"表匹配）
3. 运行 `c:\ADHD_agent\scripts\create_diff_sheet.py`，或按 Skill 中描述的流程手动执行
4. 返回 Google Sheet 链接给用户
