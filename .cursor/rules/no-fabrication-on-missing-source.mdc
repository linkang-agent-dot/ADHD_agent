---
description: 读取失败时不得编造 — 用户指定的 skill/路径/文档不可用时不应自行捏造替代内容
globs:
alwaysApply: true
---

# 读取失败时禁止编造（通用）

## 适用场景

- 用户**点名**了某个 skill、配置文件、文档或目录路径（例如「用 xxx skill」「路径在 `...\SKILL.md`」）。
- 你用 Read / 列目录 / 搜索等工具**无法读到**该文件，或路径不存在、列表中无该项。

## 必须遵守

1. **不得**在用户未要求「从零新建」的情况下，自行撰写整份 skill、整段规范或「替代版」文档来冒充「已按该 skill 执行」。
2. **应当**明确说明：**读取失败**（写明尝试的路径与错误：不存在 / 无权限 / 不在工作区内等）。
3. **应当**请用户确认：路径是否写对、文件是否已保存/同步到当前工作区、是否希望**新建**一份还是**稍后提供**可访问路径。
4. 若用户随后确认「请新建」或「以某路径为准」，再按确认后的意图创建或同步内容。

## 反例（禁止）

- 「找不到 `game-localization-translator`，我帮你写了一个 SKILL.md」——在用户只要求**使用**既有 skill 且未授权新建时，属于编造替代物。
- 把未经验证的 spreadsheet ID、仓库路径写进新建文件并当作事实来源。

## 正例

- 「`C:\Users\...\SKILL.md` 读取失败；本工作区 `.agents/skills` 下列表未见该目录。请确认文件是否在磁盘上且已被 Cursor 打开/同步，或是否授权我在 `ADHD_agent` 内新建 skill。」
