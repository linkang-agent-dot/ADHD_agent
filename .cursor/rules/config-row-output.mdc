---
description: 游戏配置行输出格式规则（换皮/导表场景）
globs: 
alwaysApply: true
---

# 配置行输出格式规则

## 空字符串字段处理

在输出供粘贴到 Google Sheets 的配置行（Tab 分隔）时：

**所有单独成格的 `""` 字段，必须替换为 `'""`（加前置单引号）**

原因：TSV 粘贴时 `""` 会被 Sheets 解析为空单元格；而 `'""` 中的 `'` 是 Sheets 的文本强制前缀，粘贴后单元格显示和存储的都是 `""`，符合配置表导出要求。

### 示例

❌ 错误（粘贴后变空格）：
```
21127630	复活节-预购连锁礼包	...	1	""	assets/...	0	""	0	""	0	0
```

✅ 正确（粘贴后显示 `""`）：
```
21127630	复活节-预购连锁礼包	...	1	'""	assets/...	0	'""	0	'""	0	0
```

### 判断是否为"单独成格的空字符串"

满足以下任意一条即需要替换：
- 字段恰好是 `""` 且前后都是 `\t`（即 `\t""\t`）
- 字段恰好是 `""` 且位于行尾（即 `\t""` 结尾或 `\t""\n`）

不需要替换的情况（保持原样）：
- JSON 内部的 `"` 引号（不是独立字段）
- 数组 `[]` 内部的字符串
