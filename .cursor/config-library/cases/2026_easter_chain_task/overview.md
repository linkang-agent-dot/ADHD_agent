# 2026 复活节多条件连锁礼包 — 案例概览

活动类型：节日通用多条件连锁礼包（event_chain_task_universal）  
来源模板：劳动节 2025 同类活动（`21127547`）  
涉及表：**2115**（仅修改任务奖励；2112 活动行本体不变）

---

## 换皮范围

| 表 | 操作 | 说明 |
|----|------|------|
| 2112 | **不改** | 活动行整行复用，包括 localization key、banner、礼包引用 |
| 2115 | **原地替换奖励道具 ID** | 保留任务 ID 211560390~211560483，仅改 reward 字段中节日专属道具 |

---

## 活动 ID

- 2112 活动 ID：`21127547`（节日通用-多条件连锁礼包）
- 2115 任务 ID 范围：`211560390` ~ `211560483`（共 94 条）
- 数据来源 GSheet：`1K3-I4gCYKY-Zw5Ms05ozHtHKpOqYI-lp4kuuhqbWajY` tab `activity_task_QA`

---

## 道具替换对照

| 原 ID | 原含义 | 新 ID | 新含义 | 是否替换 |
|-------|--------|-------|--------|---------|
| `11119772` | 2025通用BP礼包随机宝箱（万圣节绑定） | `11112500` | 2025复活节BP随机宝箱 | ✅ 替换 |
| `111110002` | 节日白色保底卡包-科技节 | `111110305` | 节日白色保底卡包-复活节 | ✅ 替换 |
| `11119262` | 连锁任务通用-增加次数道具（本活动进度道具） | — | 不变 | ⛔ 保留 |
| `11111155` | 30分钟训练加速（通用道具） | — | 不变 | ⛔ 保留 |

---

## 替换统计（94条任务）

| 道具 | 涉及行数 |
|------|---------|
| `11112500`（复活节BP箱） | 71 行 |
| `111110305`（复活节卡包） | 10 行 |
| 仅含 `11119262`（付费门槛任务，无普通奖励） | 9 行 |
| 仅含 `11111155`（免费解锁首行） | 1 行 |
| 仅含 `11119262` + `11111155`（同上） | 3 行 |

---

## 操作流程

1. 从 GSheet `activity_task_QA` 用 `gsheet_query idrange --id-col 1` 捞出 94 条任务
2. Python 批量 str replace：`11119772` → `11112500`，`111110002` → `111110305`
3. 输出 TSV，注意 `""` 空字段改为 `'""` 再粘贴 GSheet（Sheets 文本强制前缀）
4. 输出文件：`C:\ADHD_agent\output_2115_easter_reskin.txt`

---

## 踩坑备注

- `gsheet_query idrange` 默认 `--id-col 0`，但 2115 QA 表 ID 在 `col[1]`，必须加 `--id-col 1`
- `--tab` 参数是 `gsheet_query` 主命令参数，不是子命令参数，查 tabs 列表后通过 `alias set --tab` 绑定
- `11119262` 虽然是劳动节进度道具（名称含 `labor`），但用户确认本活动复用该道具，**不做替换**
