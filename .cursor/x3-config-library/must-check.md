# X3 克隆活动换皮自查清单（must-check）

> 来源：2026 世界杯 / 夏日恋语 / 深海节三节日实战踩坑收编（2026-07-09 审计沉淀）。
> 用法：**开工正查**——克隆任何活动时按环节逐项打勾，不要等导表报错/玩家客诉再倒查。
> 每条后括号标注坑源（哪个节日撞的、撞了几次）。字段级细节见同目录 `table-reference.md`。

## 一、配置克隆（ActvOnline / Pack / Reward）

- [ ] **c33/c44 展示道具**：界面顶部展示的「消耗券|兑换币」必须换成本节日道具对。规律=同节日各活动 c33 一致（尼罗 1128|1129 / 夏日 1134|1135 / 世界杯 1146|1147 / 深海转盘族 1200|1201·大富翁族 1057|1202）。拜访/累充/转盘/酒馆/集市/许愿池/BP/拼图都有这两列。（深海撞 **4 次**才全量收口 6 处——本清单头号条目）
- [ ] **新增 SKU / 奖励行漏填 Sort ⇒ 默认 0 ⇒ 排到列表最末**（2026-07-31 马戏节实证，用户抓出）：
      马戏珍宝集市新增的「返场船只皮肤自选宝箱」(`ActvExchange 134316`) 需 **15,000 勋章、全场最贵**，但 `Sort=0` 排在 15 个 SKU 的**最末位**（其余 14 个是 985~999 连续段）⇒ 玩家要翻到底才看得到顶级大奖。
      **自查**：换皮/加档后按 `Sort` 降序把列表打出来，看**价格与位次是否同向**（贵的在前）。同类字段还有 `Priority`/`DisplayOrder`/`Order`。**新增行时 Sort 必须显式填**，别指望默认值。
      对照正例：巡游珍宝集市 `1344` 的 Sort 999→987 按价格严格降序，可作为排序范式。
- [ ] **累充白名单**：新建的连锁/锚点/付费包必须追加进本节日累充白名单（AO 累充行 col50；grep 列名会命中 TaskType 表头=假阳性，按 AO 行号定位）。（世界杯 ChainPack701 五档漏加→付费不计累充；夏日也漏过）
- [ ] **Pack c35 主数据名（Name 列）**：这是 dim.iap 用的纯中文名，与 i18n TXT_Pack_Name 是两个东西。抽奖券系礼包命名=所发奖券道具名，克隆时极易照抄源节日名/带尾空格。（深海 13021-24 整名是尼罗残留「女王恩典卷」+ 12 处尾空格）
- [ ] **Pack c8 PackPrice 档位**：真实 IAP 价走 PackPrice.tsv 的 Dollar，**id↔金额非顺序**（105=$4.99/106=$7.99/107=$9.99/111=$19.99/109=$14.99）。克隆必核 col8 与 col7 显示价对得上。（世界杯竞猜模板配错真金白银价，克隆继承）
- [ ] **Pack MainBg(c30)/BottomBg 留空**：拜访门头包/装饰子包/连锁子包的弹窗背景由 prefab 写死，图字段一律留空；排查图层污染要一次看完 Pack 表全部 5-7 个图字段并与历史同类对比。（夏日 210921 填了→紫色三武器弹窗，排查 3 次；深海 211020 守住）
- [ ] **Pack c19 永久售卖窗**：克隆包别误挂 6001 等永久窗（活动窗外可买）。历代拜访包全空。（深海拜访包 211020 误挂）
- [ ] **RuleTips / 带文本 ID 禁共用**：克隆活动若沿用源活动 RuleTips ID（AO col13），i18n 是同一套自动 key——必须新建独立 ID 并 repoint；正文**每一条逐条核语义**（获取途径/玩法描述会留源活动逻辑），不能只换道具名。（世界杯开箱 16031 蹭元旦：第 2 条获取途径全错+韩语整段没换；深海累充/转盘也挂过世界杯/尼罗残留）
- [ ] **Reward 三铁律**：①同 RewardID 内行 id（col1 seq）必须连续，append 文件尾会打断→整组迁 fresh 连续块（RewardID 不变引用方零改动） ②DropPara 必填 10000 ③col1=内部行号 / col2=RewardID 外键别搞混。（世界杯/夏日/深海全撞过）
- [ ] **新 Reward seq 起点必须用 Python 全表精确查 max，禁用 awk 数值比较**：awk `$1>m` 在该表上会漏报真 max（2026-07-09 马戏节实证：awk 报 15905098、真 max=15920370，差 1.5 万——直接撞了回归BP块 176 个 seq）。写后必跑全表 col0 唯一性校验。
- [ ] **克隆奖励组先查 RewardID 是否跨节日共享**：共享则必须克隆新块再改（改共享块=连源节日一起改）。（世界杯 BP 组 140 与元旦组 132 共享 4028xxx→克隆 4099xxx；深海 BP 同理）
- [ ] **BP 奖励行 id=组×100+等级 硬契约**：BattlePassScoreReward 行 id 违反公式→点升级无响应但奖励显示正常（极易误判成积分错）。（世界杯 142xx 段、深海 14121 段都撞）
- [ ] **锚点礼包(PackType15) 要挂 ItemObtain**：只 clone Pack 行不注册 ItemObtain + 券道具 ObtainID = 「再获取」入口不显示。（世界杯漏、深海补 1200 时记住了）
- [ ] **锚点礼包内容对齐 A 线**：钻石=券×125、VIP=券×1.25（券 20/80/200/400→钻 2500/1w/2.5w/5w+VIP 25/100/250/500）。历史老节日 B 线纯券是 BUG 已全量补齐，别再克隆出纯券包。
- [ ] **跨节日共享 ScoreID/奖池组要改内容先建独立 ID**：酒馆 ScoreID 717 跨节日共享→深海建 719；许愿池奖池组 105 与春节共用（原位改=接受共改）。改前先查引用方。**且别把"原位改共享组"当唯一解**：新活动壳指向新组=纯新增零污染，可当场做、不用等上届节日下线（马戏节 07-10 实证：许愿池 5014→独立组 106，解除了"等深海下线才能改组 105"的排期依赖）。"上线前才能动"只适用于**改上届在用的行本身**（如周卡复用同一 AO 的奖池组行）。
- [ ] **自选池/奖池道具审计五件套**：存在性 + i18n + 图标 + 全表流通度 + 功能出口。骨架照抄≠内容合理。（深海酒馆照抄夏日骨架带出「红宝石之血」「英雄之证」两个零联动死道具）
- [ ] **装饰/门头类投放物先翻外显图库找现成同主题资产**（`KB\产出-本地化与美术\X3\外显图库_表情头像框铭牌\X3节日外显图库_8大模块全集.html`）：克隆源的家具/门头件是源节日的，别照抄——本节日主题的老资产可能早就有（马戏节实证 07-14：装饰应该用 25 愚人节滑稽宝箱/画像而非深海椰风椅；门头地板 152001 马戏团愚人节就有，只需补横梁+墙纸两件）。门头三件套结构=横梁 152x+地板+墙纸。
- [ ] **进度礼包/FinalReward 类必配 MailID(c17) 兜底**：手动领+活动结束零兜底=终奖无人领事故。（深海 102993 → X3NEW-1829 客诉，83 人补偿）
- [ ] **大富翁/航海(ActvType=28)"成就礼包版"换皮必查独立 `Pack__AchievePack` 表**（2026-07-16 航海之路常驻版实证漏整块）：成就礼包**≠存钱罐**(主表 PiggyBankPackID)**≠珍珠贝进度**(ProgressReward)，是**独立 AchievePack 表按圈数(laps)解锁**，靠 **col9 TriggerParameter=活动cfgID** 关联(不在 ActvVoyage 主表里)。只搬 ActvVoyage 主表系列(主表/Island/Event/OtherReward/Level/ProgressReward)会**漏掉整个成就礼包模块**→圈数解锁匹配不上活动→成就礼包永远出不来。克隆清单=① AchievePack 组(改 col5 Target/col9 TriggerParameter=新活动cfgID + col4 PackList 换新档位礼包ID) ② 档位礼包 Pack(克隆源节日 2801xxx→新ID,名去节日词,content 奖励组多为通用道具航海罗盘/钻石/VIP/纪念卡可直接复用不用新建) ③ i18n(组名+各档礼包名)。服务端逻辑=`GiftMeta.AchievePack.cs:CheckAchievementPackUnlockByVoyageLap` + `ActivityMeta.Voyage.cs`(完成一圈调用)。**"成就礼包版"三件套=主表 PiggyBank/ProgressReward + 独立 AchievePack,克隆一个都不能漏**。
- [ ] **cfg 行乱序补在表末尾时，回查所有按线性/顺序假设引用它的下游**。（世界杯 4 队框补表尾→线性公式指错框，「解锁错框」隐蔽 bug）
- [ ] **积分/档位类活动：奖励表和门槛表必须成对搬，别只抄一半**。奖励逐档照抄源、门槛却按目标游戏另定 = 性价比曲线被悄悄改写，且**前段最容易失真**（马戏扭蛋机 07-28 实证：奖励与 X2 完全一致的 430 枚，门槛却是 X2 的 2–9 倍，前 3 档 8–9.3× 而这几档两边都只发 3 枚 → 开局零反馈，玩家直接反馈"太难"）。自查指标=**每单位奖励的门槛成本**（累计门槛÷累计奖励）逐档算一遍，源与目标的曲线形状应同向；源是单调上升而目标中段冒尖=前段配错了。⚠️只调前段时注意别造断层（改完第 N 档要看 N→N+1 的跳跃倍数是否还在源表量级）。
- [ ] **ActvGroup（AO col38，awk 里 $39）必须逐个对源节日同名活动，不是"进了节日 group 就行"**：节日 hub 是**多层**的——大富翁族有自己的子 hub（深海 141「深海航行」= 大富翁本体+拼图+BP+珍宝集市 4 个；主 hub 140 挂其余）。克隆时容易把这 4 个全塞进主 group → 子 hub 空掉、大富翁界面里进不去拼图/通行证。**同时查节日有没有重复 group 行**（同名同图标）：有活动挂着的重复行会让 HUD 多出一个近空入口。（马戏节 07-28 实证：102803/101829/102251 全挤在 144，143「马戏巡游」只剩集市；142 重复 group 挂着许愿池 105014 → 已修 commit `a5014ccc`）

- [ ] **改「承接主体 ID」（换皮肤挂靠英雄 / 换奖励道具 / 换活动主体）必须全表裸 grep 引用链，禁止只顺着"活动→道具→皮肤"那条明链改**：明链只覆盖引用方的一部分，**`Reward__Reward` 是最容易漏的一层**——它按 ItemID 引用，不出现在活动配置里，肉眼顺链推不到。（马戏节 07-29 实证：皮肤承接英雄阿米娜→琥珀，明链 3 表改完自查全绿，裸扫才发现另有 **6 个 Reward 组**仍发旧道具 → 旧道具指向旧皮肤 → 这些渠道发出去的还是旧英雄的皮）。做法=`grep -rlE "(^|[^0-9])(旧ID1|旧ID2)([^0-9]|$)" tsv/ --include=*.tsv` 全表扫 → **逐个定位到列**分辨真引用 vs 同名数字巧合（本案 16 张表命中，真引用只有 5 张；ItemObtain 命中的 102001 其实是 KVK 活动 ID）→ 改完再跑一次正向链路复验（奖池→道具→皮肤→回指道具闭环）。
- [ ] **⚠️裸扫引用链时「只扫 ID」不够，必须同时扫旧主体的全部特征字符串**（2026-07-29 马戏节实证，**ID 扫过一轮仍漏，被代码审查抓出**）。上一条的裸扫我扫的是 `102001`/`5302001` 两个 ID，但**很多引用是字符串不是 ID** —— 漏掉了 `ActvOnline 101026` 的 `col50 DK_Video = DK_video_amina_skin01_sbs`（活动页预览视频仍播前任皮肤，与实际掉落不符）。**扫描词必须两类都覆盖**：①ID（旧皮肤ID/旧道具ID）②特征字符串（旧英文名 `amina`、中文名 `阿米娜`、DK 片段 `_20_Skin01`、模型名 `Role_Pirate`）。⚠️扫出来要**逐条判是不是该改**——本案 `DK_voice_amina_*`（她自己的语音）、她参演的别的活动、26春节开箱，都命中但都不能动。
- [ ] **换英雄/换角色时，DK 命名的"前导零"看本体行、别按位数推**：X3 英雄 DK 用**不补零**的序号——1009 琥珀是 `DK_Img_C_H_9` / `DK_Role_F_9`（不是 `_09`），1020 阿米娜才是 `_20`。写新皮肤 DK 前先 `show` 该英雄的**本体皮肤行**（ID=英雄ID）抄它的命名。（马戏节 07-29；DK 写错不会导表报错，只在真机丢图）
- [ ] **换英雄要同步换 `HeroSkin.DK_Model`（col8）**：皮肤沿用的是**本体模型**，换人=换模型（阿米娜 `DK_Role_Pirate` → 琥珀 `DK_Role_Elf`）。只改头像/立绘/卡面三个 2D DK 会留下前任的 3D 模型。（马戏节 07-29）
- [ ] **换英雄前先核兵种（Hero.Type）**：皮肤属性 PropType 是**按兵种**的（220011=猎人防御），换到不同兵种的英雄身上属性就挂错了。同兵种才能原样搬。（马戏节 07-29 阿米娜/艾琳娜/琥珀同为 Type=1 猎人，属性零改动）

## 二、i18n（换皮独立一步，导表不校验）

- [ ] **克隆新 ID 必按新 ID 建全部 TXT_ 自动 key**：客户端不读配置表单元格中文（那只是备注），走 `TXT_{表}_{字段}_{行ID}` 查 i18n bytes。导表绿≠标题不掉。界面文本空→去 CfgProtoTextEx.cs 查 UI 读哪个 key（如装饰阶梯 HUD 页签读 TXT_ChainPack_Name 不是 ActvName）。
- [ ] **逐语言扫，别只信中英**：①「cn+en 译好其余 14 语照抄英文」=伪完整，能骗过粗查 ②clone swap 只改 cn/en 其余语言残留源节日词（繁中「尼羅之輝」）③en 丢限定词会撞名（世界杯表情丢国家名→ARG/SUI 同角色撞名）。审计工具：`festival_i18n_completeness.py`（母版门控）+ `i18n_leak_audit.py --grep {节日}`（0 leak/0 空/0 撞名才过）。
- [ ] **并发 i18n 防孤儿**：基于旧 worktree 视图重复建规则 key 会造孤儿，动手前先 fetch 看队友是否已建。（深海 15018/15019 孤儿）
- [ ] **同名不同表的 key 族别猜，用真源反查**：岛屿皮肤 Skin__Skin 名 key=`TXT_Skin_Name_{id}`，英雄皮肤 Hero__HeroSkin=`TXT_HeroSkin_Name_{id}`——两表 id 空间重叠（都有 1016/1017），按错误 key 族建会撞已有英雄皮肤 key。（马戏节 07-10 实证）反查法=拿源行中文名 grep Text.tsv 拆复合 key。
- [ ] **「按官译逐语言替换人名/词条」的脚本，跑完必须逐语言复验目标词真的出现了**（2026-07-29 马戏节换英雄实证，已推到线上才发现）。换承接主体时常写「拿旧主体各语官译 → 新主体各语官译」做 replace，**但被替换的那行文本里，人名未必用的就是官译**——本案阿米娜在 sp/fr/de 官译是 `Amira`、kr 是 `아르미나`，而皮肤名里写的却是 `Amina`/`아미나`（当初译者用了英文名）→ replace 找不到匹配串 → **静默跳过，脚本零报错**，16 语里 4 语人名仍是前任。**复验法=扫目标行有没有「前任所有语言的名字变体」残留**（官译 + 英文名 + 各语拼写），别只看脚本有没有报错、也别只抽查 cn/en。
- [ ] **带数字的按钮/文案 key 跨语种对数字**：同一 key 部分语种把数字翻错能潜伏多代活动（大富翁单抽 `Text_ActvVoyage_Btn_1` cn=「探索1次」但 en/jp/it/pl/po/tr/th 七语=x10，从首版错到 2026-07-10 玩家才报）。机检法=按 key 逐语种扫阿拉伯数字是否一致（对照组 `Text_ActvVoyage_Btn_2` 全语种 x5 一致）。

## 三、美术 / DK / 客户端资源

- [ ] **换「承接主体」时先全仓清点该角色的形象散落在几张图上，别只改被点名的那张**（2026-07-29 马戏节皮肤英雄阿米娜→琥珀实证）。配置侧的姊妹条目见一节「改承接主体 ID 必须全表裸扫」——**美术侧同理，且更隐蔽**：一个角色的形象会散进卡面、道具图标、活动底图、活动 icon、皮肤三件规格图等多处，改漏了就出现「背包里的图标还是前任、卡面已是新人」。做法=`git ls-tree -r origin/<分支> --name-only | grep -iE "<节日词>|<相关ID>"` 列出全部候选 → **逐张目检有没有人物**（本案 40+ 张里真正含人物的只有 5 张：两卡面+两道具图标+BINGO底图；签到bg=卷轴帐篷、开箱bg=悬浮岛门头、装饰bg 都无人物，不用动）。清点结果写进换皮档案，避免下次重新目检。
- [ ] **「道具图标=卡面缩略」这类派生资源别重新 AI 出图，程序化复现**：纪念卡的 `icon_card_image_{id}`(256×256透明) 就是卡面本身做仿射变换+投影。**实测四角 TL(60,21) TR(208,31) BR(194,234) BL(46,224) 是平行四边形**（上下等宽、左右等高→是旋转约3.9°的仿射，不是透视），拿新卡面按同一组四角贴回原图即可，边框/投影/发光全保留、零 AI 随机偏差。工具=`KB\产出-本地化与美术\X3\马戏节\纪念卡\make_card_icon.py`。验收=新旧 bbox 与不透明占比应几乎相同（本案 bbox 完全一致、alpha 平均差 1.16/255）。**通用判据：先看目标资源是不是某张主图的机械派生，是就别让 AI 重画。**
- [ ] **改客户端资源前先 `git fetch` 看落后多少**：x3-project 主工作区常年落后（2026-07-29 实测**落后 474 个提交**），在陈旧工作区里改图会出现「文件明明在磁盘上、git 却报 untracked」的诡异现象（因为本地 HEAD 根本没有那批文件），且推上去会覆盖别人的新版。**正解=基于 `origin/<分支>` 开 sparse worktree 改**（`git worktree add --no-checkout` + `git sparse-checkout set client/Assets/Res/UI/Spirits/<子目录...>`）。⚠️**sparse 路径要带 `client/` 前缀**——仓库根是 `C:\x3-project`，`client/` 只是子目录不是独立仓（在 `client/` 下跑 `git ls-files` 显示的是相对路径，极易误判成仓库根）。
- [ ] **别用 bash 重定向从 git 里取二进制文件**：`git show origin/x:a.png > a.png` 在 Git Bash 下会做换行转换把 PNG 弄坏（报 `UnidentifiedImageError`）。要取就 checkout 出来，或用 `git cat-file`/PowerShell。
- [ ] **DK 注册是三处，`tableResInfo` 那处是机器人自动维护的、真正要人工做的是 Display/Path**（2026-07-29 琥珀换皮实证，纠正此前"新增 DK 必须手工登记 tableResInfo"的说法）。机制：
  | 项 | 谁维护 | 说明 |
  |---|---|---|
  | `tableResInfo.txt` | **机器人按 gdconfig 配置自动重生成** | 配置引用了某 DK → 自动加；不再引用 → 自动清 |
  | `Display_*.asset` | **人工** | DK 定义（key/type/desc/guid/exportCode） |
  | `Path_*.asset` | **人工** | **两处**：顶部 key 清单 + 下方 key→objPath 映射 |
  - 漏 Display/Path = 图在仓里但**不进包**，游戏里显示「没有资源」（马戏节头衔大图栽过）。
  - 🪤**「tableResInfo 有」≠「资源能用」**——机器人只补清单不补 Display/Path，光看 tableResInfo 会误判成已注册。
  - 🪤**机器人有时间差，别在它跑完前下结论**：改完配置立刻查 tableResInfo 会看到中间态（旧 DK 还没清、新 DK 还没补）。2026-07-29 我据中间态误判成"dev_festival 缺 amber 视频 DK"，等机器人跑完自己就对了。
  - ⚠️**换图沿用同名 DK 不受影响**（DK 名没变、三处登记都还在），只有**新增 DK** 才要人工注册 Display/Path。
- [ ] **英雄皮肤头像：脸占画高 33–40%，礼帽等大配饰允许被画幅切掉**。`hero_skin_derive.py` 默认参数从人物 bbox **顶部**起切，遇到高礼帽/羽毛会吃掉大半画面、脸只剩约 20% 且切在鼻子处（必被打回）。修法=把窗口下移：`--head-ratio 0.233 --head-pad-top -0.30 --head-pad-bottom 0`（琥珀 1009 实测值，其他英雄按帽子高度微调）。**脸的可读性优先于配饰完整性。**
- [ ] **x3-project 的 `dev` 是受保护分支，不能直接 push，必须走 MR**（gdconfig 的 dev 可以直推，两个仓策略不同，别按 gdconfig 的经验推 x3-project）。推 feature 分支时 refspec 要写全：`git push origin HEAD:refs/heads/feature/xxx`（只写 `HEAD:feature/xxx` 会报 "Neither worked... must fully qualify the ref"）。
- [ ] **派生规格图（立绘/英雄卡/头像）别拿白底主稿直接裁**：套现役 alpha 遮罩会漏底，白块从新轮廓的缝隙里透出来（曾三档规格图全废重跑）。正解=**先用已抠好的 RGBA 立绘**，且 **alpha = 人物alpha × 柔边遮罩（相乘，不是替换）**。
- [ ] **头像构图基准：脸占画高 33–40% 是可读区间**，低于 30% 会被打回；**大配饰（礼帽等）允许被画幅切掉**——曾因礼帽占太满、脸只剩 30% 被报问题，最终选 33% 那一档。

- [ ] **Item 道具 DK_Icon(col20)**：克隆道具的图标列带着源节日图（深海券 1200/1201 显示情人节信封/玫瑰→玩家报「道具没出」）。转盘类换皮过 10 项继承坑清单（背景/HUD/col33/消耗道具/奖池/RankCfg/OtherReward/券获取渠道/col16 残留名/DK_Icon）。
- [ ] **活动类 DK 双补**：手编 Path_Activity 后必补 Display_Activity（runtime 真源），否则 Ctrl+T 全量重导会清 Path-only 条目；Ctrl+T Save 前先 git pull。周卡背景多一处 tableResInfo(master)。
- [ ] **HUD 图标 vs 道具图标是两套 namespace**：ActvIcon(col21) 读 Path_Activity、道具 icon(col20) 读 Path_Item，跨 namespace 填了不显示。
- [ ] **DK/png 的 client 提交要跟配置同分支族走**：配置同步 dev 时 client 资源没跟=not found（深海 102993 icon 只在 dev_festival）。查 client DK 真实状态必用 `git grep -c "<DK>" <ref> -- <file>`（git show|grep 不可靠，四格矩阵法见 reference_x3_client_resources）。
- [ ] **新 client 资源「没生效」先想 AB 包**：症状=只新加的不对、老的都正常；数据全对（DK 解析对+bytes grep 到）就是 AB 没重建。修=正式构建出包/本地重打 AB。别再翻配置。
- [ ] **GIF 表情透明铁律**：PIL 存 GIF `optimize=False`，保 transparency==background==255，否则游戏黑底。（世界杯四强表情）
- [ ] **透明图必差分法验真透明**：gpt「transparent」会把棋盘格画进 RGB，须 remove_background 真抠。
- [ ] **克隆玩法验收「按钮显示道具=实扣道具」逐个核**：大富翁(Voyage)单抽按钮图标被 UIActvVoyage.cs 重复赋值覆盖成 LotteryItemID2(1058海神罗盘)、5连按钮图标从未被代码赋值全靠 prefab 烤死默认图——首版(75682e206)即错，2026-07-10 MR!709 修。共用 UI 代码修一次历代 Voyage 全受益（含马戏节大富翁克隆），但换皮验收仍要逐按钮对配置 LotteryItemID1/2；配置顺序本身没错时别去改表。
- [ ] **老图当构图锚生图，逐张检查源节日语义元素残留**：参考图里的节日专属道具/梗会被模型照搬进新图（马戏节实证：深海每日礼包 bg 的藏宝图+罗盘被原样带进马戏版）——c33 展示道具残留坑的美术版。prompt 写"replace ALL <源节日> elements"不够，出图后人工过一遍语义物件清单。
- [ ] **出图顺序=核心道具先行**（2026-07-10 用户纠正）：活动背景的中心物件多半是本节日抽奖道具（箱子/券/转盘），**先出道具定稿图→再拿道具图当参考锚出背景**；跳过这步=背景中心物件全错要整批重做（马戏节实证：10 张背景先出，3-4 张中心是深海寻宝语义全废）。玩法形态也要先锁死（转盘 vs 开箱）再出图。
- [ ] 🔴**画布同尺寸 ≠ 内容同占位**（2026-07-28 马戏节大富翁地块实证，比下面那条更前置）：规格化脚本 `_规格化脚本.py` 的做法是 **trim 透明边 → contain 到画布 → 居中**，这等于**主动把内容撑满画布**。深海原件是美术画的、自带留白（内容 bbox 只占画布 28-39%）；马戏是 AI 出图被紧裁后再撑满（39-55%）。**两边都是 184×224「规格正确」，但内容大了一圈** → 棋格坐标照抄深海就挤成一坨（用户实机截图报"岛屿挤在一起"）。
  - **判据**：除了对 W×H，还要对 **①内容 bbox 尺寸 ②不透明像素占比**——与复用源同槽位资源比，占比偏离超 ~5pt 就要查。测法：`a=Image.open(p).convert('RGBA').split()[3]; a.getbbox()` + 不透明像素计数。
  - **修法**：目标不是"填满画布"而是 **内容 bbox ≤ 复用源族的包络**；**从最大原件重建**（别拿已压到槽位尺寸的图再缩，二次重采样会糊）；**站地面的物件要底边对齐**（只缩不对齐 = 一排浮空/陷地的高低差；深海地块底边 198-204 → 基线取 201）。
  - 脚本范本=`KB\产出-本地化与美术\X3\马戏节\地块与图标\_地块规格化_内容包络版.py`（含改前/改后/复用源三列占比对照打印 + 自动备份）。⚠️**凡过老 `_规格化脚本.py` 的透明资产都有同样隐患，不止地块**。
  - 只修真正超标的：马戏五张里 `start` 39.0%（vs 深海 39.3%）本就正确、`mystery` 仅超 4pt，**只动了 lucky/diamond/treasure 三张**——先按「超标幅度 × 占多少个格」排序再决定修谁（`lucky` 铺 12/24 格是头号，`diamond` bbox 高 223/224 顶满画布是单张最离谱）。
- [ ] **出图管线必含「槽位尺寸规格化」终步**：X3 美术尺寸铁律=与复用源同槽位资源**同尺寸**（HUD icon 124×136/道具 icon 256/地块 184×224/活动 bg 540×500/铭牌 752×192，见 reference_x3_art_resource_spec）。AI 出图默认 1024-2048 原图，落盘≠可用——判据=与任务 ref[0] 参考图同 W×H，批量规格化脚本（透明图 trim+contain/实底图 cover 裁切）见马戏节 `_规格化脚本`（马戏节实证 07-14：40 张里 38 张未规格化，用户凭"跟要投放的东西不符"直觉抓出）。
- [ ] **外显配置行建完必回填美术排产清单（配置↔美术两清单勾稽）**：纪念卡卡面/铭牌图/皮肤头图标这类"配置一行+图一张"的外显资产，最容易掉在配置清单与美术排产的夹缝里——配置建了行≠图进了出图批次（马戏节实证 07-14：纪念卡 81 卡面+铭牌 106 图两张漏排，用户抓漏才补）。收口时按"占位 DK 清单"反查出图批次，一一对上。

## 四、数值

- [ ] **跨游戏换皮 ROI 用目标游戏口径**：X2 成就礼包 ≈2×，X3 福利/大奖档 ~700%/8×，别套源游戏。
- [ ] **券面价锚**：抽奖券 $0.25/张、宝石 $0.002、A 线 ROI≈2.25×（尼罗白皮书，见 numerical-manual.md）。
- [ ] **BP 满级门槛对齐活跃度**：任务无上限+满级太低+双 BP 共任务=上线即满级（深海 16400=世界杯 27%→D1 满级 1.8%）；夏日反例=门槛太高满级率 5.8% depth 没起来。克隆 BP 必重算 Count/满级分。
- [ ] **凡大奖是英雄皮肤，必核目标服玩家能否获得本体**：皮肤无本体=废奖（世界杯足球宝贝 5304001 vs 本体 50040 传奇限定）。延伸：消耗券→消耗活动是否部署；外显→客户端 DK 资源包；视频皮肤→客户端展示程序版本。

## 五、部署 / 上线

- [ ] **服列表别信脚本硬编码**：上 prod 前必查线上现存同类活动真实 servers[] 作 ground truth（iGame activity/list）。（世界杯 QF 漏 28 新服全撤回）
- [ ] **签到(Type14)按 7 天多期部署**，不能配一个长整段（整段=只签 7 天后面空）。
- [ ] **硬时长约束先查**：多阶段酒馆=阶段和须等于活动时长；每日礼包 Type29=固定 7 天；拼图=登录任务 max 天数下限；跨服(Type50 等)必须跨服+绝对时间对齐、最不灵活先排它。
- [ ] **子活动(TC=0) 要不要单独 iGame 部署看 AO TimeCycle 是否为空**：ActvGroupSchedule 只管 HUD 分组不管激活。（深海拼图 101828 差点漏部署）
- [ ] **配置生效 vs 服务器镜像**：部署时间早于配置 build 完成时间=跑旧配置；判据=LastDeployTime vs build 完成时刻。客户端所见非所得的改动（周卡奖池等读本地 Cfg 渲染）两端必须同批，别热更单边。
- [ ] **iGame 活动列表聚合类工具必须翻页**（bp_snowflakes 只读第 1 页漏 60 老服）。GMPrintServerActivityByCfgId 查单服活动不可靠，验证走 Mongo ServerActivity。

## 六、结算 / 运营（竞猜类专用）

- [ ] **礼包 id 跨轮复用必按 settle_from 时间窗隔离**，否则把上一轮下注算进来重复发奖；跨轮间隔近时用 created_at 时间戳非日粒度。
- [ ] **淘汰赛对阵等 ESPN 真实队名，绝不按占位序号推**；开球时间权威源=ESPN（只认 ET 锚+12h 换北京），别信 Wikipedia。
- [ ] 发奖验收=iGame 发件箱 receiverCount 对 CSV 行数（母邮件 status -2/子邮件 -1 不是失败）；GM 输入框上限 50 万字符/次。

## 七、分支 / 协作（改 gdconfig 前）

- [ ] `git branch --show-current`（并发 agent 会切分支）；配置审计读 `git show origin/{分支}:tsv/...` 远端真源，别读工作树。
- [ ] 选择性搬分支**禁 cherry-pick**（tsv driver base 远离目标→静默丢行）、禁全量 merge（带无关 commit）→ 确定性手工 application + row-id 审计（丢 0 撞 0）。
- [ ] 合并后程序化审计 (sheet, ID, field) 三方差异（夏日 1802.Score 被合并错覆盖，肉眼才发现）。
- [ ] 收口跑 c33/i18n/白名单全量扫描脚本（深海范本：`KB\产出-数值设计\X3_深海节\_配置扫描脚本_deepsea_scan.py`，换节日可复用）。

---
案例索引：世界杯=`KB\产出-数值设计\X3_世界杯\_世界杯_换皮清单.md`+交接总文档§4；深海=memory `project_x3_deepsea_festival.md`；夏日=`KB\产出-数值设计\X3_夏日恋语\_夏日恋语_换皮清单与复用说明.md`（唯一入口）；三节日全景审计=`KB\换皮档案\X3\_X3三节日换皮全景与坑梳理_20260709.md`。

- **克隆源 CID 界面资源残留（c33/c44 姊妹坑，2026-07-17 马戏福箱实证）**：克隆活动内容表（ActvCrafting 等）时，CID 行里的**界面 bg/特效 DK 字段**可能挂着克隆源自己都没换干净的历史残留（世界杯开箱 1516 的界面 bg = 新年 `NewYear_bg_5` 540×364，FullBg 全屏拉 3.6 倍糊成条纹）。换皮时对 CID 行**每个 DK_ 字段逐个问一遍「这是谁的图、尺寸配不配全屏用法」**，别默认克隆源自身是干净的。

- **美术DK落地有现成整套脚本别现写（2026-07-17 马戏节固化）**：`KB\产出-数值设计\X3_马戏节\配置克隆脚本\circus_dk_land.py`（规格化图→client：按锚点同目录拷图+克隆锚点meta换guid+Display/Path双注册，幂等）+ `circus_dk_swap.py`（gdconfig 占位DK行级断言换名，**换前先验新DK已在client Path注册**防换成白图）。换节日=改 MAPPING/SWAPS 表即可；锚点映射可从 x3-media task json 的 ref[0] 批量提取。

- **🔴Path_*.asset 是 keys/values 平行字典，DK注册必须两侧成对追加（2026-07-17 错位血案）**：`Res\Config\DisplayKey\Path_*.asset` 的 paths 字段=`keys:`(光杆DK名单)+`values:`(key+objPath成对)**按下标配对**。只往 keys 插行=从插入点起全部 DK 错位（主城图标全乱、感叹号满天飞）。正确姿势=keys 末尾+values 末尾同时追加（land脚本已修）；错位急救=`circus_dk_path_realign.py`（按 values 重建 keys+孤儿补对）。另：文件内含 YAML 折行（带空格文件名），逐行解析要做续行拼接。Display_*.asset 是自包含块列表，无此坑。

- **拜访活动(ActvType56)克隆三查（2026-07-17 马戏节实证）**：①VisitPack 表 DK_PackIcon=**每期专属门头陈列大图**（圣诞/埃及/新春各一张），克隆必残留上期图=「外显全错」观感头号来源；②DailyRewardDesc 列(TXT_注释)的自动 key `TXT_ActvVisitPack_DailyRewardDesc_<CID>` 必须进 Text 表，漏=界面中部说明整段空白；③装扮名一致性：RuleTips 规则文、邀请函 Item.Desc、VisitPack 说明三处的【装扮名】必须同名（马戏节撞过：规则写马戏庆典/邀请函写樱花）。另注意 Text 表存在**合并key行**（col0 多 key 用 | 串联共享一行译文）——新活动的 Title 类 key 若单独成行须自行填全语言，别指望蹭合并行。

- **新活动占号必查三处最新占用（2026-07-17 ActvType 号段撞车实证）**：并行 feature 分支各自注册新 ActvType 必撞（张力 BpFund 与扭蛋机同抢 82，合并才炸）。占号前对 **dev_festival 最新** 查三处：①`ActivityConst.TRIGGER_TYPE_*`（CSShared）②导表 `def/actvonline_def.py`+`PostProcessData.py` ③proto 的 ActivityItem tag。让号原则=后合者让。另：**proto 生成物（activity.cs 等）merge 冲突禁 regex 文本拼**——用 diff3 看结构，双方完整插入段=ours+theirs 逐行状态机解（cspb 与 client 两份是 MakeCopy 镜像，修一份 cmp 校验另一份）。

- **大富翁(ActvType28)地块图挂点+占位扫描盲区（2026-07-17 实证）**：地块图不在 Island 表（无美术列）也不在 AO 行，在 **ActvVoyageEvent.DKImg**（EventGroup×Level 给图，克隆节日=整组 DKImg 全是上期残留，马戏 48 行全中）。占位 DK 扫描别只按「行 ID ∈ 已知 ID 集」匹配——Event 类表行 ID=组号x1000+序号（207 组→207001），按**组列**过滤才扫得全。角色模型=主表 DKRoleMedel（3D 件，无新模型就沿用）。

- **BP(ActvType22)克隆两条免查规律（2026-07-28 马戏福箱通行证实证）**：①**`RuleTips 1017` 是全项目通用 BP 规则**（26 个 BP 共用，正文只讲积分/升级/买等级/买礼包，零节日专属词）→ **BP 克隆不必新建 RuleTips**，上面「RuleTips/带文本 ID 禁共用」那条对 BP 不适用（那条针对含玩法描述的活动专属规则）。②**`Pack.GuildGift(col40)` 与价格档位强绑定**（Price 107→203 / 111→204 / 105→202 / 115→205，各 300-400 行同构）→ 克隆同价位礼包照抄 col40 即可，不用另查。③BP 的**任务源在 `ActvBattlePassScore.BattlePassScore` col3**（管道分隔的 ActvScoreTask ID），绝大多数节日 BP 都是同一套通用任务 `501-506|601|602`（集结杀海兽/采集/情报）——**同节日配两条 BP 若都挂这套，玩家打一遍游戏两条同步涨分=免费奖励翻倍发**，要么错开积分源、要么接受并重算满级分（配置本身不报错，只有产品后果）。④要做「玩法驱动型 BP」的现成配方=`ActvScoreTask` 新行 `TaskType=440`(每消耗 1 个指定道具)+`Parameter1=<消耗道具ID>`，追加进 BattlePassScore col3 即可，**程序零改动**（推币机 BP 2201/2204 就是这么干的：每消耗 1 个夺宝币得 15 分）。

- **克隆整活动的落表脚本写法：只 append 不重写既有行（2026-07-28 固化）**：用 csv 读全表→改→写回，会把既有行的引号/空列表示改掉，`sync_xlsx_tsv --check` 立刻 mismatch、本地 gate 拒 commit。正确姿势=**按原始文本行 split('\t') 做字段替换再 join，新行 append 到文件尾**，既有字节零扰动（`git diff --stat` 应显示纯 insertions、0 deletions，这就是验收判据）。范本=`KB\产出-数值设计\X3_马戏节\配置克隆脚本\build_circus_boxpass.py`（含写前 ID 空闲断言、Reward seq Python 全表精确查 max、命中数≠1 即中止）。

- **🔴棋盘类玩法背景的两条构图铁律（2026-07-28 马戏大富翁实证，改了 3 轮才收敛；上面「老图当构图锚」「出图顺序=道具先行」管**中心物件**，这两条管**留白与细节的分布**）**

  **铁律一：留空的方向必须跟前景形状对齐 → 细节走「四角裁切 + 上下横带」，不是左右竖带。**
  - 反查 X3 两张原版就一目了然：**普通航海** `img_Activity_Monopoly_bg` 细节全在**四角**（左上礁岛/右上云团/右下礁岛，都被画框裁掉一半）+ **上下带**（底部帆船、小岛、海鸟），**中间横条空**；**深海** `img_Activity_Monopoly_deepsea_bg` 同构（珊瑚压下方与角上、顶部光柱+小潜艇）。
  - 🪤**我的错法**：把这个法则读成了"边上要有细节"→ 做成**左右两条竖带**，而棋盘恰恰是**横向铺满、纵向只占中段**——竖带正好压在棋盘最宽处。
  - **正确推理**：棋盘上方是标题/倒计时区、下方是奖励条/按钮区，**这两块本来就不放地块**，所以能塞细节的是上下，不是左右。**先量前景占哪块，再决定背景往哪塞**。
  - 中间横带（约高度 30%~70%）写死"必须全空、零物件"进 prompt，别指望模型自觉。

  **铁律二：边缘装饰的母题不能和棋子/地块同款。**
  - 深海边上是珊瑚、地块是岛屿——母题不同，所以细节再密也不打架；马戏版边上摆**红白条纹帐篷+气球**、棋盘地块**也是**红白条纹帐篷+气球 → 边缘装饰被玩家直接读成「更多地块」，越靠边越分不清哪个能踩（用户实机截图报"左右边不应该挂装饰"）。
  - **判据（出图前问一遍、出图后再验一遍）**：背景缩到实际尺寸、叠上真实前景棋子 sprite 并排看，**能不能一眼分出哪个能踩**。分不出就是母题撞了。
  - **修法优先级**：①换母题（马戏=改挂**舞台帷幕布幔**：平的挂布，物理上不可能站人，主题感还在，实测成立）②彻底清空只靠明暗暗角收边（**但会把节日辨识度一起清掉**，马戏 v3a 全空场实测就认不出是马戏节了，慎用）。**别只是"把装饰画小一点"**——母题一样时缩小只会变成"小一号的地块"。

  **两条的关系**：铁律一决定**往哪放**，铁律二决定**放什么**。只满足一条都不够——只改母题不改方位＝帷幕仍压棋盘边；只改方位不改母题＝上下带又出现一排"像地块的帐篷"。
  - **推广**：凡「棋盘/地块/格子/路线图类玩法」背景都适用（大富翁 Voyage / 拼图 / 寻宝路线图 / 航海之路）。**克隆参考图时要抄的是「细节分布 × 母题相斥」这两条关系，不是「边缘有细节」这个表象。**
  - 配套工具：对比页范本 `KB\产出-本地化与美术\X3\马戏节\活动背景\_对比页_大富翁背景v3.html`（客户端实际尺寸 + 真实地块 sprite 按实机宽度叠加 + 「只看边缘」遮罩开关），换活动改图片路径与 POS 数组即可复用。**判可读性一律看这个页，别看 1152×2048 原图**——原图上边缘冲突完全看不出来。

- **GPT 出图「alt 版」分辨率不保证与主图一致（2026-07-28 马戏背景实证）**：同一次 grfal 调用产出的主图与 `_alt1` 可能**不同尺寸**（实测 v4b=1152×2048、v4b_alt1=**941×1672**）。→ **落库/规格化前逐张查 `Image.open(f).size`，别默认同批同尺寸**；选中的若是小尺寸那张，压当前槽位可能够用但**放大余量已经没有**，要更大尺寸只能补送超分。这条是上面「出图管线必含槽位尺寸规格化终步」的前置：规格化脚本按固定目标尺寸跑不会报错，小图会被静默拉伸，**只有查原图尺寸才发现**。

- **🔴棋盘类背景 铁律三：棋子靠「色相对比」跳出来，不是靠背景压暗（2026-07-28 马戏大富翁提亮轮实证）**——补齐上面铁律一（往哪放）/铁律二（放什么）之后的第三个维度：**放完之后怎么让棋子读得出来**。
  - **反查**：深海/普通航海的棋子（岛屿、船，暖色）之所以一眼跳出来，**一半功劳是蓝色底**——冷背景 vs 暖前景是互补色对比，明度可以很亮也不糊。马戏从头到尾是**暖底配暖棋子**（红金 vs 红金），没有色相差可用，只能靠"把背景压暗"这一个手段硬撑 → 于是"要庆典感所以要亮"和"棋盘要读得出来"直接对撞。
  - **两条出路**：①**引入冷色**（天空走明亮蓝天 → 冷天空衬暖棋盘，既能亮又能跳，等于把深海那套原理搬过来）②**分区控明度**（下面这条）。
  - **🔑分区控明度（提亮任何棋盘类背景的通用做法）**：**亮度全推给「棋子不落的区域」——天空、上下横带的彩旗/灯/道具；棋子要落的中央区保持中间调**，可以更亮更干净，但不许炸白、不许上花纹。写 prompt 时把这条作为独立的 `ONE RESTRAINT` 段单列，别混在整体调色描述里，否则模型会全图统一提亮。
  - 🪤**反面教材**：马戏 v1 就是"整体高亮 + 中央塞满"→ 棋盘完全糊进背景，是本案第一次返工的根因。**"提亮"的需求永远要翻译成"分区提亮"，不能整体提。**

- **🔴x3-project 提交前必查暂存区，别只看自己 `git add` 了什么（2026-07-28 实证，当场翻车）**：主工作区常年有几十个别人/别的任务的在途改动，**其中 `client/Assets/Res/Config/ProtoGen/*.bytes` 可能已经处于「已暂存」状态**（谁跑过本地导表/`git checkout origin -- ProtoGen/` 都会把它们推进 index）。此时你 `git add <自己的文件>` 再 commit，**会把 27 个别人的 .bytes 一起打包进你的提交**——`git add` 只负责加，不负责清。
  - **正确姿势（每次 commit 前固定两步）**：① `git diff --cached --stat` 看**暂存区全貌**（不是 `git status`）② 出现 ProtoGen / `.claude/skills/*/memory/*.jsonl` 等无关项 → `git reset HEAD -- client/Assets/Res/Config/ProtoGen/` 踢掉，再 commit。
  - **已经误提交的修法**：`git reset --soft HEAD~1` → `git reset HEAD -- <无关目录>` → 重新 commit（**推送前发现就零成本**；推上去再修就要动远端历史）。所以**push 前一定先 `git show --stat HEAD` 复核这次到底提了什么**。
  - 这条与 [[reference_x3_project_repo]]「提交客户端改动时的暂存区排雷」是同一件事——那条写了要踢 ProtoGen，但没说清**它可能在你动手前就已经在暂存区**，导致「我只 add 了一个文件所以肯定只提了一个文件」的误判。


- **🔴ActvType=63（阶梯/链式礼包）的 `ActvOnline.ActvImg` 是死配置——换皮时别为它出图（2026-07-28 马戏装饰礼包实证）**：
  换皮扫残留时会看到 63 类活动 col22 还挂着上个节日的背景 DK（实测：装饰礼包 106104=`DK_img_Activity_deepsea_turntable_bg` 深海、门票礼包 103102=`DK_img_Activity_labor_bg_01` 拓荒），
  **看起来像必须修的残留，其实玩家一辈子看不到**——差点为此白出两张背景图。
  - **根因**：`ActvImg` 全仓只有一个消费点 `UIHelper.Activity.cs:246-248`（`SetActivityBaseInfo` 内部设面板 BG），而
    `ActivityChainPackCondition.cs:40` 里 `GetActivityUIType() => null` ——63 类**没有独立活动面板**，入口是商城 `UIRecharge`/独立礼包窗，
    根本走不到那段代码。日历入口也不读 ActvImg。
  - **63 类真正驱动界面视觉的是 ChainPack 表自己的两个字段**：`Icon`(col3 DK_图片)=礼包入口缩略图（`UIGiftPack.cs:201`）、
    `Video`(col8 DK_视频)=弹窗内嵌视频（`UITieredPack.cs:306-346`）。**换皮只需改这两个**。
  - **推广判据（适用于任何"这个 DK 字段要不要换皮"的疑问）**：先查该 ActvType 的 `GetActivityUIType()` 返不返回 null，
    再 grep 该字段在 `Assets/Scripts/` 的消费点（排除 `CfgProtos` 字段定义）。**没有消费点 = 死配置，不值得出图**。
    反过来，有独立面板的类型（推币机 `UIActivityCoinPusher.cs:86`、拓荒券 `UIActvLaborGacha.cs:93`、海妖转盘）才真的吃 ActvImg。
  - 🪤**为什么值得单独记一条**：换皮自查工具是按"关键词扫旧节日名"跑的，**它分不清死配置和活配置**，
    会把 63 类的 ActvImg 一并报成待修项。**扫描器报出来的每一条，落地前都要问一句「这个字段真的会被读吗」**。


- **🔴「复核暂存区」和「commit」必须拆成两条命令——串在一条里等于没复核（2026-07-28 当场二次翻车）**：
  上面那条已经写了「commit 前必看 `git diff --cached --stat`」，但我把它和 `git commit` 用 `&&` 串成了一条命令 →
  diff 的输出和提交结果**同时**才看到，**看到时已经提交完了**，照样把别人已暂存的 16 个文件（纪念卡图/签到图标/拼图背景/Display_Memory）打包进了我的提交。
  - **纪律**：`git add` → **单独一条** `git diff --cached --stat` → 人眼确认 → 再单独一条 `git commit`。复核的价值在于「能据此改变下一步动作」，串行执行就没有下一步了。
  - 修法照旧：`git reset --soft HEAD~1` → `git reset HEAD -- <无关路径>` → 重新 commit（推送前发现零成本）。
  - 补充一条排雷范围：不只 `ProtoGen/`，**美术目录**（`Assets/Res/UI/Spirits/**`）和 **DK 注册资产**（`Display_*.asset` / `Path_*.asset`）同样常年有别人在途的暂存改动，是同一类雷。


- **🔴活动「加档」之后必须回头补累充白名单——加档的人只改玩法表，白名单在另一张表上（2026-07-28 存钱罐实证，用户抓出）**：
  大富翁存钱罐从**单档改成三档复购**时，`ActvVoyage` 加了 `PiggyBankPackID2/3` 两列并配好了新礼包，
  但累充 `ActvOnline.RechargePointPackWhitelist` 只有最初的 `280002`，后加的 `280004/280005` 没跟 →
  **玩家买这两档的钱不计入累充积分**（活动本身完全正常，所以自测发现不了）。
  - **触发条件（不止存钱罐）**：任何"给已有活动**追加付费档位/新礼包**"的改动 —— BP 加档、扭蛋机加券、
    阶梯礼包加档、限时抢购填价格。**新包是后加的，白名单是早写死的，天然不同步。**
  - **另一种形态：新活动建的时候就漏（不是加档）——因为不同活动类型的付费包配在不同表 / 不同 ID 段**
    （2026-07-31 马戏节星光巡演实证，用户抓出）：
    · 普通 BP（`ActvBattlePass`）付费包＝**13xxxx 段**（巡游通行证 130047/130048、马戏福箱 130051/130052）→ 白名单里有 ✅
    · **BpFund 类型付费包配在 `ActvBpFund` 表的 `Pack` 列＝82xxxx 段**（星光巡演 820011/820012 集结通行证 $9.99/$19.99）→ **白名单里没有** 🔴
    配白名单的人按「BP = 13xxxx」的老结构去找，结构不同的这个自然就漏了。**同类还有 `ActvCrafting` / `ActvPioneerCity` 等玩法表自带 Pack 列的活动。**
  - **核查方法（差集扫描，别凭记忆逐个列）**：①取 AO 累充行 `col49 RechargePointPackWhitelist` 集合（⚠️**是 col49 不是 col50**，col50 是 `DK_Video`）
    ②从 `Pack__Pack.tsv` 按本节日关键词捞出全部候选 ③求差集并**逐条判该不该进** —— 免费档礼包（备注含「免费」/售价空）本就不该进，别误加。
    马戏节实测：候选 78 个 → 在白名单 54、不在 24，其中 22 个是免费档/别节日的正常不进，**真漏配就 820011/820012 这两个**。
  - **收口动作**：改完玩法表，按「该节日所有 `Price` 非空的包」对白名单做一次**全量差集**，别只补自己刚加的那个
    （本案全量扫 23 个付费包才敢说没有第三处缺口）。收集范围＝组143/144活动 + ChainPack.PackList +
    存钱罐 PiggyBankPackID1/2/3 + Pack 名带节日关键词。
  - 🪤**别被 `Pack.RechargeAmount`(累计充值额度) 带偏**：这列全表仅 179/2985 个包有值，
    已在白名单里的包大多也是空的 —— **它不是计分开关**，计分开关就是白名单本身。
  - 同源教训：此前同一张白名单已漏过福箱BP 130051/130052 + 扭蛋机 13029-13038 共12个，**是重复发生的类别**。


- **🔴繁体(zh)列体检：判据用「Big5 可编码性」，别用 opencc 单字转换（2026-07-29 实证，误报 993→33）**：
  想找"没转繁/转了一半"的 zh 单元格时，直觉写法 `s2t(ch) != ch` 会**误报 993 条**——
  `台/秘/群/丑/干/征/霉` 这些字在繁体里本就通用，opencc 只是给出 `臺/祕/羣/醜/幹/徵/黴` 这类**异体**形，原字并不算错。
  - **正判据**：`ch` 不能用 Big5 编码 **且** `s2t(ch)` 能用 Big5 编码 → 才是真·简化字。
    简化字（马/戏/时/抢/购/动/获/奖）根本不在 Big5 字集里，而 台/秘/群 都在。精确剩 33 条，零误报。
  - 工具已固化：`C:/ADHD_agent/skills/x3-dk-tools/audit_zh_traditional.py`
  - **修复纪律（同样重要）**：
    ① 用 `s2t`(仅字形) **不要用 `s2twp`**(台湾用词)——先统计全库既有 zh 的用词习惯再定：
       本项目是 查看47/激活90/點擊49，而 檢視/啟用/點選 各 0 次，用 s2twp 会把这些词全改掉造成用词分裂。
    ② "只残留个别简化字"的行**只替换那几个字**，不整条重转，否则会把已正确的繁体改成异体。
    ③ 用字对齐要单独查：本项目 著949 vs 着1，s2t 不会动 `着`，得手动补一条 `着->著`。
    ④ **`Text_union_language_cn="简体中文"` 是豁免项**——那是语言选择器里"简体中文"这个选项的名字，本就该显示简体。
  - 🪤**连带发现**：改 zh 会把这些行带进 `i18n_leak_audit --changed` 的扫描范围，
    从而暴露它们**其它语言也缺**（本次因此多补了限时抢购 UI/ErrCode 共 281 格）。
    换言之 **zh 体检是发现"整批文案只有中英"的一个有效入口**。

- **🔴i18n 扫描器按"活动ID集"圈范围会漏掉 UI/错误码 key（2026-07-29 实证）**：
  按 `TXT_<族>_<活动ID>` 匹配节日归属的扫描，**认不出不带 ID 的 key**：
  `TXT_ActvFlashSale_*`（UI 文案）、`Text_ErrCodeActivityFlashSale*`（toast）、
  `TXT_RuleTips_*_40003`（规则，ID 是 RuleTips 自己的编号不是活动ID）。
  本次限时抢购整批 18 个 key 就是这么漏掉的，第一轮补译报"马戏节 0 条缺失"其实是假绿灯。
  - **补法**：除 ID 集外，再按**功能前缀**扫一遍（活动的 C# 类名 / 玩法英文名，如 `FlashSale`/`Circus`/`Voyage`），
    并从 `ActvOnline.col13 ActvRule` 反查该活动引用的 RuleTips ID 一并纳入。

- **🔴新建"平行活动"(同节日再开一个同类型活动)的必查清单（2026-07-29 马戏节拆双累充实证）**：
  从现有活动整行克隆时，**这几个字段克隆过去就是错的**，必须逐个判：
  | 字段 | 为什么不能照抄 |
  |---|---|
  | `ActvRule`(col13) | 配置库铁律 RuleTips 禁共用；两活动奖励不同，规则文案也不同 → 新建一条 |
  | `Priority`(col35) | 同值会导致两个入口排序不确定 |
  | `ItemRecycle`(col44) | **最容易出事**：要回收的道具可能**已被别的活动声明过回收**，且转换目标不同。
      本案 1207 已由 AO103101 回收成 4200110，而累充模板是转 2022 —— 同时声明=冲突。查法：全表 grep 该道具ID 在 col44 出现过没有 |
  | `TopResource`(col33) | 顶部货币位要换成本活动真正发的道具 |
  | `ActvIcon`/`ActvImg` | 复用会导致两个入口图一样，玩家分不清（本案暂复用并已标注待补美术） |
  可以照抄的：`GroupId` / `CloseServerList` / `MailID`(各节日通用模板 101109) /
  `RechargePointPackWhitelist`(若设计上就是共用一份) / `PlayerLv` / `CloseType` / `ServerMergeShield`。
  另：累充类活动新建后，`ActvTask` 的 `TaskType=902` + `Parameter1=<新ContentID>` 必须同时对，
  漏了 Parameter1 就会退回全局累充(900)，两个活动进度互相污染。

- **🔴换皮活动的 i18n 常与源节日「共享同一行」，直接改 = 连源节日一起改掉（2026-07-29 马戏节实证）**：
  `tsv/i18n/Text__Text.tsv` 的 key 列会把多个 key 用 `|` 合并成一格共用一条文本。
  马戏拼图 101829 就与深海拼图 101828 共享 `TXT_ActvOnline_ActvName_101828|TXT_ActvOnline_ActvName_101829`，
  当时把名字改成马戏的，深海拼图会跟着变成「小丑的梦境」。
  - **正解＝拆行**：①原行 col0 只保留源节日 key（源文案一字不动）②给新活动 **append** 两条独立行
    （col0=key / col1=AI / col2 空 / col3-18 = 16 语），只 append 不重排既有行。
  - **查法**：改任何换皮活动的 i18n 前，先看该 key 所在行的 col0 里**有没有 `|`**；有就必须先拆。
  - 🪤**读这张表还有一坑**：key 有两种排布——**有的在 col0、有的在 col2**（col2 为空时 key 在 col0），
    只扫 col2 会漏掉一大半（换皮活动名基本都在 col0）。语言列固定 col3=cn col4=en …… col18=th。
  - **工具**（别再手写解析）：`~\.claude\skills\x3-config-export\scripts\i18n_lookup.py`
    ——按 ID 批量捞中英名/描述，自动处理上述两坑，并把「key 缺失 / 某语言为空」单列成
    「换皮漏改高发点」清单。用法 `python i18n_lookup.py 101026 101027 -o out.md`（中文结果必须 -o 写文件看，控制台是 GBK）。
  - **顺带**：出「给社区同学看的 What's New 文本」时，活动中英名一律用它现捞，别手写——本次就是靠它
    抓出 101829 沿用深海名 + 106104 活动名 16 语全空（同位的 106101夏日/106102尼罗/106103深海都有名字）。


- **🔴阶梯/链式礼包(ActvType=63)配完不显示：十有八九是漏了 `Pack__PackTypeInfo` 的商店入口行（2026-07-29 马戏装饰礼包实证，用户报"3080看不到"）**：
  63 类活动**没有自己的活动界面**（`ActivityChainPackCondition.GetActivityUIType()` 返回 null），
  唯一入口在**商城**，而商城列表由 `Pack__PackTypeInfo` 驱动 —— **以该礼包链的首档 `ChainPack.PackList[0]` 为 ID 建一行**。
  `GiftNavigator.cs:211` 注释原话："无行则不出现在商店"。
  - **最阴的地方：活动表根本不引用 PackTypeInfo**。两者的关联是隐式的（ChainPack.PackList[0] → PackTypeInfo.ID），
    所以顺着 `ActvOnline → ChainPack → Pack → Reward` 一路查下去**每一项都是对的**
    （IsOn=1 / 等级 / 开关服 / TimeCycle / 礼包内容 / 图标 / 视频 全绿），就是没有入口可点。
  - **建行格式**（照抄同类现成行，如深海 211016 / 夏日 210917）：
    `ID=首档PackID / IsOn=1 / Name=<TXT_商城名字> / MallType=9 / Icon=<DK> / Sort=9999 / Content=自身ID`
    `MallType=9` 经代码核实 = `GiftConst.MALL_TYPE_CHAIN`（连锁礼包），别猜。
    配套 i18n `TXT_PackTypeInfo_Name_<首档ID>`。
  - **🪤 要"配好但暂不上架"时：必须删行，`IsOn=0` 没用！**
    `GiftNavigator.TryBuildChainRoute`(L407-429) 的判定链是
    `PackList[0] → CPackTypeInfo.I(firstPackId) is not {} → Content 包含首档 → 建 route`，
    **全程不读 `IsOn`**。想藏起来只能让这一行不存在；活动本体(`ActvOnline.IsOn`/`ChainPack.IsOn`)保持 1 即可，两者互不影响。
  - **收口自查（每次新增 63 类活动都跑一遍）**：遍历所有 `ActvType=63` 的活动 →
    取 `ChainPackID → ChainPack.PackList[0]` → 查 `PackTypeInfo` 有没有该行 + i18n 齐不齐。
    本案一扫发现马戏节**两个** 63 类活动全漏（夏日/深海都有），不是孤例。


- **🔴礼包/家具展示图：动手前必须先把「实际投放的那件资产」拉出来看，别照名字让 AI 画（2026-07-29 装饰礼包实证，返工两轮）**：
  马戏节装饰礼包卖的是「滑稽宝箱」「滑稽画像」，名字听着就是马戏主题，我照字面让 AI 画成了
  **红白条纹金边藏宝箱** 和 **框里的马戏帐篷**。用户一看：「完全不对，你得拉投放家具的原图弄进来」。
  真实资产是 **愚人节复用的小丑主题**——玩偶匣里弹出戴彩条纹丑角帽的小丑 / 金框里的白脸小丑肖像+紫气球。
  - **根因**：换皮节日的道具**名字是新起的、资产是复用的**，两者不同源。`FurnitureDecorate.SourceDesc`
    这次白纸黑字写着「愚人节活动获取」，查一眼就不会错。
  - **正确查法（三步，别跳）**：
    `Item.UseParameter` → `FurnitureDecorate.ID` → 该行的 `Icon`(2D图标 DK) / `DKPrefabs`(3D预设 DK)
    → 在 `Path_*.asset` 反查 objPath → **打开真图看一眼**。
    本案：151023 → 31001054 → `DK_icon_jiaju_statue_2_AFD` → `Assets/Res/UI/Spirits/Furniture/Actv/icon_jiaju_statue_2_AFD.png`
  - **✅定型做法：AI 只出干净空场景，真资产用游戏内 PNG 合成进去**。
    出背景时 prompt 必须写死「⛔不许画任何家具/箱子/画框/道具」——画了就会和合成上去的真图打架。
    合成时给物件补一个椭圆软阴影"坐"到地面/贴到墙上，否则像硬贴。
  - **合成图再拿去生视频时，prompt 第一优先级是「锁住这些真资产不许动」**（不许重画/改脸/移动/缩放/形变），
    否则 AI 一重绘就把真资产改回它自己想象的样子，等于绕回原点。
    验收要**单独抠出资产区域逐帧比对**（区域内帧间变化 <0.05），只看整体动态查不出这个。

- **🔴UI 循环视频「动没动起来」要量化，别肉眼判（2026-07-29 实证）**：
  判据 = **帧间平均像素变化**：
  ```python
  g = cv2.cvtColor(cv2.resize(frame,(200,266)), cv2.COLOR_BGR2GRAY).astype(float)
  diffs.append(np.abs(g - prev).mean())   # 取 np.mean(diffs)
  ```
  本项目实测标尺：**深海版 0.22（好）／0.15 是及格线／0.067 明显偏静／0.02 等于静止图**。
  肉眼看 5s 循环很容易觉得"有点动就行"，实际差 10 倍。
  - 🪤**把动态写"轻"是主要死因**：v1 prompt 写了 `gentle/subtle ambient motion only` + `never change size or position`
    + `no parallax`，配合首尾帧锁定直接冻成静止图（0.02）。
    正确写法是逐条点名要动的元素并用 **OBVIOUS / clearly visible / continuous / steadily** 这类词，
    而不是笼统说 "add gentle ambient motion"。
