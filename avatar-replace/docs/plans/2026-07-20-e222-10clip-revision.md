# E222 10-Clip Revision Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Revise and regenerate clips 01, 02, 06, 08, 09, 10, 11, 12, 13, and 15 so clothing coverage, product orientation, continuity, annotations, and product colors match the approved QC rules.

**Architecture:** Update only per-clip prompts and the shared hidden-garment rule in `prompts/t2v_E222.json`. Generate each clip as one complete Seedance request through the personal VolcProvider runner, QC the raw result before applying exactly one subtitle layer, then replace only accepted files in the final delivery folder while preserving all historical versions.

**Tech Stack:** JSON prompts, Python batch runner, personal Volcengine Seedance API, ffmpeg/ffprobe, local subtitle post-processing.

---

### Task 1: Lock prompts and routing

**Files:**
- Modify: `C:\ADHD_agent\avatar-replace\prompts\t2v_E222.json`

1. Back up the current JSON.
2. Change the global hidden rule to: outerwear fully hides the product by default; a small skin-tone strap or product area may appear only during an explicit hand-driven proof action and must disappear after release.
3. Revise clips 01, 02, 06, 08, 09, 10, 11, 12, 13, and 15 using the approved per-clip list.
4. Ensure hidden clips do not receive product reference images; product-display clips retain front/back references.
5. Parse JSON and print the final composed prompts for all ten IDs.

### Task 2: Generate raw candidates

**Files:**
- Use: `C:\ADHD_agent\avatar-replace\scripts\batch_t2v.py`
- Create: `E:\222\t2v_成片_v5_10条修正版\`

1. Verify the runner imports only `VolcProvider` and the personal GRFal block remains active.
2. Run the ten IDs as complete one-shot generations with generated audio.
3. Verify each requested file exists and has H.264 video, AAC audio, and non-zero duration.

### Task 3: Content QC before subtitles

**Files:**
- Read: `E:\222\t2v_成片_v5_10条修正版\t2v_<id>.mp4`

1. Sample each full duration and inspect clothing start/end continuity.
2. Check hidden-product frames, proof-action frames, product front/back orientation, actor/dialogue assignment, props, colors, and clip-specific requirements.
3. Retry only failed IDs, following the two-fail circuit breaker.

### Task 4: Apply exactly one subtitle layer

**Files:**
- Use: `C:\ADHD_agent\avatar-replace\scripts\post_subtitle.py`
- Create: `E:\222\t2v_成片_v5_10条修正版_字幕\`

1. Detect whether each accepted raw clip already contains Seedance subtitles.
2. Preserve a correct single Seedance layer; otherwise burn one accurate post subtitle layer.
3. Visually verify no duplicate subtitles and confirm audio remains present.

### Task 5: Deliver accepted replacements

**Files:**
- Modify accepted IDs only: `E:\222\t2v_最终交付\t2v_<id>.mp4`
- Update: `E:\222\样片验收反馈.md`
- Update: `E:\222\项目记录_数字人带货视频.md`

1. Back up replaced final files into a dated historical folder.
2. Copy only QC-passed outputs into the final delivery folder.
3. Run ffprobe across all 16 final files.
4. Rebuild the dated ZIP and verify it contains 16 MP4 files plus the directory note.
5. Record the new product-visibility rule and per-clip outcomes in personal memory.
