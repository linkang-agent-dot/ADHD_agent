---
description: X3 x3-media — GRFal/生图生视频/切图时强制下载目录与 history 记录
alwaysApply: false
---

# x3-media 执行约束（随 skill 分发的 Cursor Rule）

当任务涉及 **生图、生视频、抠图、GRFal、切图/UI 拆解、技能图标、集卡、行军表情** 等，且应走 **x3-media** 工作流时：

1. **读 skill**：遵循本包 `SKILL.md`，并按 Type Router 读取对应 `references/type-*.md`。
2. **派子 agent 执行（强制）**：主 agent **不得**直接调用 `call_grfal.py` / `generate_2d.py` / `generate_video.py` / `generate_3d.py` 等后端脚本。所有任务必须派 `media-worker` 通用子 agent 后台异步执行（prompt 含 `TASK_ID` + `SKILL_ROOT` 两行；同一个 worker 服务 x2-media / x3-media / 未来其他媒体 skill，由 SKILL_ROOT 区分上下文）。详见 SKILL.md「执行模式」与 `references/dispatch-protocol.md`。N 张图 = N 个独立 `task_id` = N 个并发 background worker。
3. **认证**：优先 `GRFAL_COOKIE`；缺失则引导运行 `scripts/get_grfal_cookie.py` 并配置环境变量。worker 检测到 Cookie 过期时写 `status=needs_auth`，主 agent 引导用户刷新后用同一 task_id 重派。
4. **history（worker 职责）**：worker 每次成功生成后向本 skill 目录下 `state/history.jsonl` **追加一行** JSON（`ts`、`type_key`、模型、`downloaded` 路径或 `result` URL、后端 `grfal`/`art-skills`、`task_id`）。
5. **记忆 MCP（可选）**：仅当对话里**实际可用** `memory_search` / `memory_write` 时，任务开始检索、结束写入；**无 MCP 则跳过**——Skill 文案里的「hook」不代表 Cursor 已自动配置。
6. **交付物**：通过派发并发 worker 完成；主 agent 派完即回到对话，不要把「请运行我写的临时 xx.py」当主要交付。
7. **任务状态单一事实来源**：`state/tasks/<task_id>.json` 是任务全生命周期单一事实来源。主 agent 只写 `pending`，worker 写其他终态。主 agent 收到 task-notification 后只读 task json 播报，**禁止**接受 worker 的长文本输出污染主上下文。

**补救下载**（仅有清单、无重新生图）：
`python call_grfal.py --fetch-manifest <manifest.json> --download-dir <目录>`（需 Cookie）。此操作也建议派 worker，避免阻塞主对话。
