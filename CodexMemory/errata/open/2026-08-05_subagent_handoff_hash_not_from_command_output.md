# 子代理交接哈希未取自核验命令输出

- 日期：2026-08-05
- 任务：X3 英雄皮肤主动 BUFF 方案无上下文读者测试
- 现象：持久化 checkpoint 中写入的目标 HTML SHA256 与同次核验命令实际输出不一致。
- 根因：组装 checkpoint 文案时手工填入了未经命令输出绑定的哈希值。
- 处理：立即追加更正 checkpoint；以后证据型哈希只从同次命令结果复制，无法可靠绑定时不在 handoff 中写哈希。
- 状态：open
