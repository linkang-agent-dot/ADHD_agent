# X3 邮件导入表附件格式复核误判

- 日期：2026-08-03
- 任务：复核 BINGO 101830 误上线补偿导入表
- 现象：复核脚本把第三列按 JSON 解析，但 X3 导入表实际使用 `[1002*数量]`，触发 `JSONDecodeError`。
- 根因：混用了其他项目邮件导入格式与 X3 六列导入格式。
- 处理：按 bulk-mail-reissue / X3 路由的 `[道具ID*数量]` 格式解析；以后优先复用该 skill 的 dry-run，并从生成器 schema 读取格式，不自行推断。
- 状态：open
