# X3 补偿明细复核误用金额列名

- 日期：2026-08-03
- 任务：复核 BINGO 101830 误上线补偿导入表
- 现象：复核脚本假设玩家明细金额列为 `diamond_refund`，实际导出列名为 `diamonds_consumed`，触发 `KeyError`。
- 根因：未先读取导出文件表头，直接凭语义猜测字段名。
- 处理：先读取表头，再按实际字段完成明细与 iGame 导入表的金额交叉校验；以后对生成产物做二次校验时，以文件表头或导出脚本 schema 为准。
- 状态：open
