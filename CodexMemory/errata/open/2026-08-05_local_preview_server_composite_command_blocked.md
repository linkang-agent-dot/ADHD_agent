# 本地预览服务启动与轮询放在复合命令中被策略拒绝

- 日期：2026-08-05
- 任务：实际运行英雄主动 BUFF Demo
- 现象：在一条 PowerShell 命令里同时 `Start-Process python -m http.server`、轮询 `Invoke-WebRequest` 和输出状态，被执行策略整体拒绝。
- 根因：后台进程启动、网络请求、循环等待混在同一复合命令，策略无法安全拆分判断。
- 处理：先解析 Python 绝对路径，再单独启动隐藏服务；随后用独立只读请求验证。失败时不重复同一复合命令。
- 状态：open
