# 停止自建预览进程后的空结果被标记为失败

- 日期：2026-08-05
- 任务：切换本地 Demo 预览端口
- 现象：对本轮自建 PID 51160 执行 `Stop-Process` 后，同一命令继续 `Get-Process -ErrorAction SilentlyContinue` 验证，整体 exit 1 且无输出。
- 根因：进程停止/已退出后，验证查询的“零结果”影响复合命令退出码；再次把动作与空结果验证混在一条命令。
- 处理：停止动作后用独立命令验证；零结果视为已停止，不当作失败。后续预览服务使用新端口并单独检查 listener。
- 状态：open
