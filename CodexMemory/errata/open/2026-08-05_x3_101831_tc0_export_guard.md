# X3 101831 TC=0 被导表校验拦截

- 日期：2026-08-05
- 任务：将马戏节新拼图活动 `101831` 调整为 `IsOn=1 / TimeController=0`。
- 现象：TSV 定点修改成功，但全量运行 `ExportTable.py` 失败，报“活动ID:101831 时间控制器ID:0 不能是0”；随后读取错误记录模板时又误用了不存在的 `errata/open/README.md`。
- 根因：`ActvOnline` 导表规则仅允许特定活动类型、KVK 活动或 `ActvGroupSchedule` 子活动使用 TC=0，拼图类型 18 不在白名单；错误记录 README 的实际位置是 `errata/README.md`。
- 处理：未推送导表失败的配置；检查校验代码并采用活动 ID 级最小例外，只放行 `101831`，随后重新跑全量导表。README 路径改用 `C:\ADHD_agent\CodexMemory\errata\README.md`。
- 状态：resolved；采用 ID 级例外后，在最新 dev 基线上全量导表 exit 0，提交 `fb058e4c` 已推送 dev，Jenkins `#2476` SUCCESS；规律已补入共享 TimeCycle KB 和马戏节档案。

## 同轮工具错误

- 现象：引用反查时按不存在的 `tsv/Mail__Mail.tsv` 查询，组合命令 exit 1。
- 根因：未先按文件清单确认 X3 邮件表实际拆分名称。
- 处理：先用 `rg --files tsv | rg 'Mail'` 定位，确认邮件模板实际位于 `Mail__MailTemplate.tsv`，`101109` 存在。

- 现象：按 AGENTS.md 给出的 `.Codex/projects/.../reference_x3_timecycle.md` 路径读取失败。
- 根因：当前机器共享项目 memory 实际仍位于 `.claude/projects/...`，AGENTS.md 路径与现状漂移。
- 处理：用 `rg --files` 精确定位到 `.claude/projects/C--Users-linkang/memory/reference_x3_timecycle.md` 后更新；后续不假设迁移路径已经落盘。
