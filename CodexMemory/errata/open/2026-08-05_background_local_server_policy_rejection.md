# 后台本地验证服务被策略拦截

- 日期：2026-08-05
- 任务：验证 X3 节日外显内容排期合并页的离线自包含与浏览器渲染
- 现象：尝试用 PowerShell `Start-Process` 在临时隔离目录后台启动 `python -m http.server` 时，命令被执行策略拒绝。
- 根因：当前命令策略不接受该后台进程启动组合；不是 HTML 或 Python 服务本身报错。
- 处理：改为先建立隔离副本，再用受控的前台长任务启动临时 HTTP 服务，并在浏览器验收后终止服务。
- 状态：open
