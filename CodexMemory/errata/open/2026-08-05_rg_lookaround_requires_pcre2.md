# rg 默认正则不支持 look-around

- 日期：2026-08-05
- 任务：确认马戏节头像框 Item 80360 是否进入奖励投放链。
- 现象：用 `(?<!\d)80360(?!\d)` 精确排除长数字撞号时，rg 报 look-around 不支持并 exit 1。
- 根因：rg 默认 Rust regex 引擎不支持前后向断言。
- 处理：同类数字边界查询显式加 `--pcre2`，或改用制表符/非数字捕获边界。
- 状态：resolved；已改用 `rg --pcre2` 复查。
