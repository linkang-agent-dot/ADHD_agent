# 价格梯度补丁上下文与 errata 路径错误

- 日期：2026-08-05
- 任务：把 X3 全部外显价格梯度改为使用网页实图的纵向列表。
- 现象：首次 `apply_patch` 因 `_目录说明.md` 的现有描述与预期上下文不同而整包校验失败；随后又按 `open\README.md` 读取格式说明，但实际 README 位于 `errata\README.md`。
- 根因：未先读取目录说明的精确当前行；沿用了 AGENTS.md 中与磁盘现状不一致的 README 路径。
- 处理：先查询目标行，再把生成器替换、目录说明更新拆开执行；errata 格式固定读取父目录 README。
- 追加现象：Edge 无头截图命令返回前截图文件尚未落盘，紧随其后的 `Get-Item` 报不存在，但 Edge 随后成功写出文件。
- 追加处理：无头浏览器截图与文件检查分两次调用，避免异步落盘竞态。
- 状态：open
