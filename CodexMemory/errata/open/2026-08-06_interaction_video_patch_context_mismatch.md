# 专属互动视频接入补丁上下文不完整

- 日期：2026-08-06
- 任务：至尊获取与晋升 HTML Demo 接入既有互动视频
- 现象：两次 apply_patch 无法匹配 CSS 长行，补丁没有应用。
- 根因：压缩后的 CSS 全部处于超长单行，使用整段样式作为精确上下文容易因微小差异失配。
- 处理：改为在关键帧前追加更高优先级的视频样式，只精确替换较稳定的 HTML 与 JS 片段；视频资产无需转码，直接接入。
- 状态：resolved。
