# Chrome headless 截图落盘与即时检查竞态

- 日期：2026-08-06
- 任务：至尊获取与晋升 HTML Demo 自动截图验收
- 现象：Chrome 最终成功写出截图，但紧随命令的 `Get-Item` 先报文件不存在，完成提示随后才出现。
- 根因：Chrome headless 截图子过程在 PowerShell 返回控制后仍有短暂落盘延迟，即时读取产生竞态。
- 处理：不重跑页面生成；确认文件已经存在后单独读取。后续将截图命令与文件检查拆成两次调用，或轮询文件存在后再读。
- 状态：resolved。
