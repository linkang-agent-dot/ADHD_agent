# Chrome 自动化禁止打开本地核对 HTML

- 日期：2026-08-04
- 任务：渲染检查 BINGO 101830 补偿核对页
- 现象：浏览器安全策略拒绝导航到本地 `file://` HTML。
- 根因：当前浏览器控制面的 URL policy 禁止访问本地文件，并明确禁止通过其他浏览器控制面绕行。
- 处理：不绕过策略；改做 HTML 静态完整性、嵌入数据、脚本语法和外链检查，交付本地文件由用户双击核对。以后本地 HTML 自动渲染检查若无允许的 localhost 服务，应提前采用静态验证并明确局限。
- 状态：open
