# 航迹页文案纠错补丁上下文写错

- 日期：2026-08-05
- 任务：X3 航迹皮肤排期
- 现象：修正“坚屏→竖屏”时，补丁上下文误带diff前缀；随后因目标HTML位于单个超长行内，最小片段也无法作为独立行匹配，连续两次apply_patch验证失败。
- 根因：生成器把整段HTML压在单行，降低了小范围补丁的可维护性。
- 处理：在写出HTML前做明确的错字替换；后续生成器应将正文HTML拆成多行，便于局部补丁。
- 状态：resolved
