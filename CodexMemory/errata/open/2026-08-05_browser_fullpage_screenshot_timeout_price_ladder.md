# 极简价格图全页截图超时

- 日期：2026-08-05
- 任务：验收 X3 全部外显价格梯度一图
- 现象：页面重载后调用浏览器 `Page.captureScreenshot` 全页截图在5秒内超时，后续 finalize 未执行。
- 根因：浏览器截图命令超时；页面为纯静态HTML且此前同页全页截图已成功，非页面脚本或资源加载失败。
- 处理：不重复同类全页截图；改用新鲜DOM快照确认9张卡片与全宽Top N布局，再用普通视口截图或直接finalize收口。
- 状态：open
