# 必需目录与可选目录混查导致已取得结果仍标失败

- 日期：2026-08-05
- 任务：定位挖孔参考 Demo
- 现象：第一条 `Get-ChildItem` 已成功列出 KB 内三份 Demo，第二条尝试读取可选的 `C:\x3-project\AIDocs\dig-keys\interactive-demo` 因路径不存在，使整次调用 exit 1。
- 根因：将已知必需目录与未经验证的可选仓库目录放在同一命令，且没有先 `Test-Path` 分流。
- 处理：后续直接使用已确认存在的 `挖孔竖屏交互demo_v02_单文件版.html`；可选路径一律先单独解析存在性，不再影响主查询退出码。
- 状态：open
