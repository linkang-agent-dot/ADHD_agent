# X3 节日外显养成线页面先做模块定位与内部分层

- 日期：2026-08-04
- 任务：X3 节日外显模块养成线梳理 HTML
- 现象：首版以“10 个模块总览 + 每模块双北极星”为主体，用户指出应当每个模块独立页签，并优先讲清养成线定位及其内部细分，例如英雄皮肤的普通/史诗/限定、装饰的普通/史诗/限定、主城的传说/限定。
- 根因：把“列全模块与指标”误当成信息架构，没有先建立每条养成线的产品层级；模块级指标过早，掩盖了不同品质层实际承担的投放任务差异。
- 处理：HTML 改为一模块一页签，页内固定按“定位 → 子类型/品质层 → 投放职责 → 当前情况 → 双北极星”展开；用户已确认的分层明确标记，尚未确认的模块只给候选框架并标待核实。后续指标需要能下钻到具体子层，不只停留在模块总量。
- 二次纠错：页签版虽然补了目标定位与候选分层，但“当前情况”仍只是一条投放演进，没有清楚展示当前实际定位、已有数据表现和数字所暴露的问题。结构再次调整为“当前盘面（实际定位 + 数据卡 + 核心问题）→ 目标定位 → 内部分层 → 投放演进 → 双北极星”；历史基线、近期实绩和数据缺口必须显式分层。
- 三次纠错：英雄皮肤页只用了白皮书的整体饱和度与付费率，没有先查知识库中已经存在的“英雄皮肤分层 × 获取率”专项报告。专项口径是 `皮肤曾获得人数 / 对应英雄解锁人数`，成熟服 1000–1880、数仓实查 2026-07-29，共48款可获取皮肤；应优先于全体MAU饱和度用于判断皮肤本身的获取表现。后续查 X3 英雄皮肤数据，先查 `C:\ADHD_agent\KB\产出-数值设计\X3_8-10月节日需求\英雄皮肤分层与获取率.html` 及生成链 `skills\p2-festival-monitor\x3_skin_ownership.py`。
- 四次纠错：补了分层定义和汇总仍不够；用户要的是“每个分类具体有哪些皮肤”。模块分层交付必须列资产名单，至少包含具体名称、获取人数、目标分母与获取率；其他历史标签也要单列，不能藏在汇总或默认归入普通。
- 五次纠错：具体皮肤名单还必须逐款带上“所有已确认投放方式”，不能只列皮肤名和获取率；同一皮肤有礼包、常驻、排行或活动多路时要并列展示，来源未定位的明确写“待补”，禁止凭经验补全。
- 六次纠错：给英雄皮肤卡片补道具 ICON 时，应先查现成的“英雄皮肤资源知识库”，不能先去客户端目录或旧报告盲搜。正式资源库是名称/道具/ICON映射的第一入口，配置与客户端资源仅作缺项兜底核对。
- 七次纠错：HTML 虽然结构完整，但大量有效信息使用10–13px字号，用户明确反馈“真的看不清”。后续给用户阅读的 HTML 默认正文≥16px、有效说明≥14px，10–12px只允许装饰性编号；数据卡和明细卡优先减少列数、扩大字号，不用压小字换密度。
- 八次纠错：英雄皮肤补成逐件模板后，其余模块仍停留在汇总或少量样例，且旧白皮书数量已不能代表当前配置（例如行军皮肤只列 1 个样例，当前节日范围实际 14 个；头像框含世界杯国家款后为 64 个；聊天表情含国家助威款后为 53 个）。外显盘点必须对所有模块使用同一资产级模板，并以当前配置与正式资源库优先、历史白皮书只作基线；每件至少给名称/ID/ICON/投放路径，未知容器显式待补。
- 九次纠错：把其余模块补到“名称/ID/ICON/投放路径”后就认为已套用英雄皮肤模板，但漏了模板中的核心数据字段——逐件获取人数、对应分母与获取率。以后称“同模板”必须逐字段对齐，不可只对齐视觉卡片；没有现成数据时也必须显示“未查询/待跑数”，不能静默省略，更不能用模块级历史饱和度冒充单件获取率。
- 十次纠错：逐件获取率虽然完成217/217资产映射和数学校验，但只验证了“SQL能算通”，没有用世界杯纪念卡、深海纪念卡这类已知阳性样本做业务对照；页面把明确投放且已有回归记录的纪念卡算成0%，说明资产形态/change_type/活跃联接至少一处漏数。以后外显获取率交付前必须加入已知阳性对照（世界杯卡、深海卡、历史高持有外显），0值先过业务合理性审计，不能因字段完整和公式正确就放行。
- 附带工具错误：首次按 `errata\open\README.md` 读取模板失败；实际模板位于 `errata\README.md`，已改用正确路径。
- 附带工具错误：AGENTS 文本中的 `.Codex\projects\...` 知识库路径在本机不存在，真实共享 KB 位于 `.claude\projects\...`；后续先 `Test-Path` 再读。另一次 PowerShell `Get-ChildItem -Filter` 误传字符串数组，改为分别读取或用 `Where-Object`。
- 附带工具错误：用 `node - <path>` 从 stdin 做语法检查时误取 `process.argv[1]`（实际为 `-`），目标路径应取 `process.argv[2]`。另外初步口头估计“3款来源待补”，全量录完核对实际为4款（兔耳魅影、游艇假日、金海商使、黑金契约），最终以页面红标与校验数为准。
- 附带工具错误：其他外显接入后的首次 Node 校验沿用了旧字段假设：英雄图标 marker 实际为 `HERO_ITEM_ICONS_*`（非 `HERO_SKIN_ICONS_*`），其他模块结构为 `module → groups → items`（非直接数组），导致校验器误报 catalog block missing。已改为按生成脚本的真实 marker 和嵌套结构验证；以后校验前先从生成脚本读取 schema，禁止凭记忆复用断言。
- 附带工具错误：首次调用 `query_trino.execute_sql` 前未按 datain skill 把用户级 `DATAIN_API_KEY` 注入当前 PowerShell 进程，查询被鉴权前置检查拦截。无头/子进程查数前固定先执行 `$env:DATAIN_API_KEY=[Environment]::GetEnvironmentVariable('DATAIN_API_KEY','User')`，且禁止回显密钥。
- 附带工具错误：首版“目标外显资产 × 近30日活跃玩家”SQL直接把全量 `ods_user_asset`、活跃用户CTE和小型target VALUES三表联接，Trino未能及时把target动态过滤下推，126秒超时。后续先在资产CTE显式写 `asset_id IN (...)` 完成分区内强过滤，再与活跃集合联接；大批目标按模块/批次拆查询，禁止依赖优化器自行选择小表驱动。
- 附带工具错误：首次全量资产映射用一条宽前缀 SQL 拉 `dim_asset`，忽略了查询接口结果集截断，最终只映射 26/217 件。直接 Item ID 应从配置无条件加入；依赖名称的实体资产改成每20个名称一批反查，且在全量跑数前必须检查 mapped 数量，低覆盖结果禁止写入页面。
- 附带工具错误：修完 `dim_asset` 截断后，所有权聚合仍用一条可返回217组的 SQL；Datain查询接口只返回前100行，脚本又把未返回项当0，最终恰好出现100件非零、117件伪0，漏掉世界杯卡（真实当前活跃获得者1108人）和深海卡（499人）。所有可能超过100行的聚合结果必须分页或按≤60目标分批查询；未返回≠0，只有目标已成功查询且返回0聚合结果才可写0。必须用世界杯180079、深海180080作为阳性回归测试。
- 附带工具错误：逐卡获取率按前端 `toFixed(2)` 四舍五入显示，但纪念卡页签摘要用手工截断写成8.74%，与1484/16969=8.745359%应显示8.75%不一致。摘要数值必须从同一原始值使用同一格式化规则生成或验算，禁止人工抄写后只核逐卡数据。
- 附带工具错误：浏览器安全策略禁止自动化访问本机 `file:///` 报告，不能用浏览器截图做视觉验收，也不得切换其它浏览器自动化绕过；本轮改为静态核对实际 CSS 计算目标、48个内嵌图源及JS语法，最终视觉密度由用户打开成品确认。
- 状态：open

## 2026-08-05｜知识库与 TSV 探查工具绕道

- 现象：按用户贴出的 `.Codex` 路径读取 X3/数据分析 memory 失败；随后误读 `errata\open\README.md`，并用 PowerShell `ConvertFrom-Csv` 探查多行表头 TSV 时误匹配到类型行，触发重复列名错误。
- 根因：本机共享 memory 实际目录为小写 `.claude`；errata 说明位于根目录；X3 TSV 是多行表头，不能靠模糊 `-like` 判断字段行。
- 处理：改读 `.claude` 真源与 `errata\README.md`；TSV 探查改为精确识别字段名行后按列序解析，并把本轮错误保留为冷启动提醒。
- 状态：open

## 2026-08-05｜英雄皮肤专项数据无法新鲜重跑

- 现象：补齐 `query_trino.execute_sql(..., limit=1000)` 后重跑 `x3_skin_ownership.py`，运行环境报未检测到 `DATAIN_API_KEY`。
- 根因：当前 Codex 进程未继承 Datain APP KEY；不是 SQL 或数据脚本本身失败。
- 处理：保留页面现有 2026-07-29 英雄皮肤快照并显式标注日期；D35 英雄范围仅使用当前配置真源生成。脚本已修好静默 100 行截断风险，密钥恢复后可直接重跑刷新。
- 状态：open

## 2026-08-05｜Chrome headless 截图落盘竞态

- 现象：Chrome 已输出“bytes written”，同一 PowerShell 语句紧接的 `Get-Item` 却先执行并报文件不存在，组合命令返回失败。
- 根因：headless Chrome 主进程返回时截图子过程仍在完成落盘，存在短暂竞态。
- 处理：不据组合退出码误判截图失败；后续截图与文件验收拆成两次工具调用。
- 状态：open

## 2026-08-05｜组合验证命令掩盖 Node 失败码

- 现象：Node 正确检出页面仍有 11px 字号并 `exit(2)`，但后续隔离渲染与 `Get-Item` 成功，把整段 PowerShell 的最终退出码覆盖为0。
- 根因：把门禁断言和后续诊断串在同一 shell 调用，错误地只看组合命令最终退出码。
- 处理：定位 `.stamp` 的11px并提高到14px；最终门禁单独执行，不再在其后追加会覆盖退出码的命令。
- 状态：open

## 2026-08-05｜子代理等待参数低于工具下限

- 现象：调用 `wait_agent(timeout_ms=1000)` 被拒，工具要求最少10000ms。
- 根因：沿用了普通进程短轮询习惯，没有遵守协作工具的参数下限。
- 处理：改用10000ms及以上；后续调用前按工具 schema 校验范围。
- 状态：open

## 2026-08-05｜英雄品质分布验收预期硬编码错误

- 现象：页面从配置生成的品质分布为稀有7/史诗13/传奇22，但首次门禁把未计算的预估值8/14/20硬编码为期望，导致脚本失败。
- 根因：在已经有结构化配置数据时仍手填预期数量，没有先由独立聚合得出基线。
- 处理：以 Hero.Quality 的42行实际聚合为权威，改用总和与合法映射校验；页面展示稀有7/史诗13/传奇22，无普通。
- 状态：open

## 2026-08-05｜节日活动形式知识图谱路径漂移

- 现象：memory 索引给出的 `C:\ADHD_agent\KB\节日活动形式知识图谱.md` 不存在。
- 根因：真源已迁到 `KB\方法论\节日活动形式知识图谱.md`，索引未同步更新。
- 处理：用 `rg --files` 定位后改读当前真源；本案引用累计任务/七日活动规则时以新路径为准。
- 状态：open

## 2026-08-05｜把“排期”误解成单节日14日节奏

- 现象：用户要的是多个月份/多批节日的英雄皮肤内容排期，我先按单个节日D1-D14投放节奏设计。
- 根因：沿用了活动内时间窗口语境，没有结合对话主线“各月投什么英雄、三档不重复、两款新皮肤用什么基底”。
- 处理：撤销14日窗口方向；排期改为按月份列免费档/$19.99档/排行榜档英雄与设计基底，并增加同月不重复、跨月冷却和数据依据。
- 状态：open
- 2026-08-05：用 PowerShell 拼接 `python -c` 正则检查 HTML 时，正则中的 `[^\"]` 被 PowerShell 提前解析，命令直接报 ParserError。后续对自包含 HTML 的脚本编译与图片源检查改用单一 Node 脚本，避免 PowerShell → Python 双层引号转义。
- 2026-08-05：把9–12月排期做成“每月一张大卡片”，仍然不符合决策阅读顺序。用户明确要求：一句话结论先行 → 三张活动形式参考图 → 三档泳道甘特图。后续内容排期默认用“时间横轴×内容档位纵轴”的甘特结构，不用月卡堆叠。
- 2026-08-05：为定位排期脚本行号时，PowerShell 双引号让 `rg` 的组合正则被破坏成未闭合分组。后续含引号/括号的多模式搜索拆成多个 `rg -e '...'` 单引号参数，避免在 PowerShell 中拼复杂双引号正则。
- 2026-08-05：排行榜美术基底被错误写成“升级已有皮肤”。用户确认排行榜旗舰均应做全新当月节日主题；现有视频与最高BUFF规格不变，本轮唯一新增机制是皮肤技能可由玩家主动激活。排期文案禁止再把已有皮肤名称写成排行榜换皮基底。
- 2026-08-05：主城排期误报9月海盗船、10月宇宙飞船和贴图主城“缺视觉基底”，原因是只检索了产出KB，漏查用户指定的“X3验收”素材源。以后X3已有实机图先查验收目录，再判断缺图；排期图应嵌进对应甘特格子，不只放顶部图库。
- 2026-08-05：用户先允许“活动可以先不写”，随后补充两张正式投放活动图并明确要求纳入，最新口径覆盖前述暂缓：主城排期要加入庆典弹弹乐与节日围寻宝两套投放骨架，并把活动图直接放入甘特。
- 2026-08-05：一次同时修改主城生成脚本CSS、数据结构与文案的大补丁因长行上下文不一致整体失败。后续按“数据常量→行生成逻辑→模板/CSS→文档”拆小补丁，每段改前先重读准确上下文。
- 2026-08-05：把12月3款圣诞主城误拆成“存量搬运”独立泳道，并把9月海盗方案卡、10月飞船基础图误当成贴图主城参考。用户确认：删除搬运分类，12月3款归入“动态/套装”；9月、10月贴图主城均待补，现有海盗/飞船图只属于动态/套装。
