# X3 TSV 多行表头固定行号解析失效

- 日期：2026-08-06
- 任务：统计使用异国美酒的 DrawCards 配置行
- 现象：假设第 7 行是字段名并按列解析，实际 `DrawCards__DrawCards.tsv` 的字段说明含多行文本，字段名行被推后，得到错误计数 0。
- 根因：X3 TSV 表头可能包含带换行的说明区域，不能依赖固定物理行号。
- 处理：改为查找以 `ID\t` 开头的真实字段名行，或使用项目现成 TSV 解析器。
- 状态：open；后续解析 X3 TSV 先定位真实字段行，不硬编码表头行号。
