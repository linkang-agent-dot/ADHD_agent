# X3 周年美需更新脚本参数名未先核对
- 日期：2026-08-03
- 任务：把周年庆截图内容写入给 PM 的 HTML 美需清单
- 现象：调用临时更新脚本时传入 `-MainImage`，脚本实际参数为 `-MainCityImage`，PowerShell 在写文件前拦截。
- 初判根因：执行前没有先读取脚本 `param(...)` 块核对接口；后续调用本地脚本时，先读取参数定义或用 `Get-Command <script> -Syntax` 校验。
- 影响：原 HTML 未发生修改，仅延迟一次重跑。
- 状态：open

## 同轮附带错误
- 读取错误仓说明时假定 `open\README.md` 存在，实际目录无该文件；后续先枚举目录，再参考现有记录格式。
- 首次修正参数后，门头行替换仍对整份“9月+10月”文档做正则匹配，命中两个月各一行并被唯一性保护拦截；后续处理分月复合文档时，先按月份标题切片，再只在目标月份内替换。
