# 10 月美需图片未内嵌导致外部不可见

- 日期：2026-08-03
- 任务：更新 X3 2026 年 10 月万圣节美需独立 HTML
- 现象：HTML 中 11 张参考图使用本机相对路径；本机预览正常，但文件发给别人后图片无法加载。
- 用户纠正：图片必须内置到 HTML，确保别人只拿到单个文件也能查看。
- 根因：验证只检查了本地路径存在与本机渲染，没有按“可单文件转发”场景检查 HTML 的资源自包含性。
- 处理：把全部 PNG 转成 `data:image/png;base64,...` 内嵌；已将“HTML 图片默认 Base64 内嵌 + 隔离目录复验”写入 `C:\Users\linkang\CLAUDE.md` 全局铁律，后续默认执行。
- 状态：open
