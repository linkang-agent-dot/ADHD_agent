# 组合 rg 零匹配导致只读检查命令误报失败

- 日期：2026-08-06
- 任务：核查英雄主页角色展示与视频链路
- 现象：同一 PowerShell 命令中前面的源码读取和 `rg` 已成功，但最后一个 `rg` 在限定目录零匹配并返回 exit 1，使整条工具调用标记失败。
- 根因：把探索性、允许零匹配的搜索与其他读取合并执行，最终退出码继承了零匹配搜索。
- 处理：后续将允许零匹配的 `rg` 单独执行并显式把 exit 1 当“未找到”，或在 PowerShell 中按 `$LASTEXITCODE -eq 1` 归一化；不把它当代码错误。
- 状态：open
