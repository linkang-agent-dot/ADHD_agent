# PowerShell 变量后接冒号导致解析失败

- 日期：2026-08-05
- 任务：修正 X3 英雄皮肤三档定位长单行HTML模板
- 现象：断言报错字符串写成 `found $n:`，PowerShell 将冒号误判为变量作用域语法并抛出 ParserError；命令在读取和写入前即终止，目标文件未改动。
- 根因：双引号字符串中变量名后直接跟 `:` 时未使用 `${n}` 明确边界。
- 处理：将错误信息改为 `found ${n}:` 后重跑；涉及长单行模板继续使用逐短语计数断言，确保每个目标只替换一次。
- 状态：open
