# Codex 独立记忆分层

本目录是 Codex memory 的唯一真源，纳入 `C:\ADHD_agent` Git 仓库同步。`C:\Users\linkang\.codex\memory\` 仅保留兼容入口，不再写入正式记忆。

## 写入 CodexMemory
- Browser、Chrome、插件、MCP、沙箱等 Codex 运行时边界与恢复链。
- Codex 特有的误判模式、协作反馈、工具组合和验收经验。
- 从 Codex errata 复核后确认、但不适用于 Claude 的经验。

## 写入共享 KB
- P2/X2/X3/FCOL 等项目事实、配置引用链、数值结论、业务方法论。
- 与模型无关、换任何 agent 都应掌握的项目接管知识。

## Claude memory 的定位
Claude memory 是上游变化源，不是 Codex 的写入目标。每次会话开始检查增量；相关内容提炼后写成 Codex 版本，不盲目复制原文。
